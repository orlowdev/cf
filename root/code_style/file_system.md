# File System conventions

This document outlines the conventions for the file system used in C! for source code organization, including this repository.

## Rules

- All files and folders with C! code should be named in `lowercase_snake_case` - the module system heavily relies on this convention.
- The file extension for C! source code files is `.cf`. See `usr/` folder contents for editor extensions for your editor to get started.
