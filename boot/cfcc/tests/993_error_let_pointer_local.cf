# an inferred pointer local binds `const` (no `let` repointing)
# expect: error
data Inner = { Iarch v }
data Outer = { *Inner p }
pub const main = () -> {
	let Inner i = { v: 1 }
	let Outer o = { p: &i }
	let q = o.p
	return q.v
}
