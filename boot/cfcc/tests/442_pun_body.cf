# field puns `{ x, y }` fill each field from the in-scope variable of that name
# expect: exit 7
data Point = { Int x, Int y }
const mk = (Int x, Int y): Point -> ({ x, y })
pub const main = () -> {
	const Point p = mk(3, 4)
	return p.x + p.y
}
