# a generic type argument is inferred from the value argument (no explicit `[Int]`)
# expect: exit 9
data Box['T] = { 'T v }
const mk = ['T]('T x) Box['T] -> {
	const Box['T] b = { v: x }
	return b
}
pub const main = () -> {
	const Box[Int] b = mk(9)
	return b.v
}
