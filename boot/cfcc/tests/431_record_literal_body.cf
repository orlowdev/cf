# a record literal as a parenthesized expression body, typed by the return annotation
# expect: exit 7
data Point = { Int x, Int y }
const mk = (Int a, Int b): Point -> ({ x: a, y: b })
pub const main = () -> {
	const Point p = mk(3, 4)
	return p.x + p.y
}
