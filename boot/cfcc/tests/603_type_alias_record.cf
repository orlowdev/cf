# a `type` alias of a record type: construct + read fields through the alias name.
# expect: exit 30
data Point = { Int x, Int y }
type P = Point
pub const main = () -> {
	const P p = { x: 10, y: 20 }
	return p.x + p.y
}
