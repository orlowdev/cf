# literal fields are arbitrary expressions
# expect: exit 9
data Point = { Int x, Int y }
const mk = (Int a): Point -> ({ x: a + 1, y: a * 3 })
pub const main = () -> {
	const Point p = mk(2)
	return p.x + p.y
}
