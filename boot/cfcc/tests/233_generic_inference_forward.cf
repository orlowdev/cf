# a generic infers a call's type argument from its own type variable, transitively
# expect: exit 22
pub const id = ['T]('T x): 'T -> x
pub const wrap = ['U]('U x): 'U -> id(x)
pub const main = () -> wrap(19) + wrap(3)
