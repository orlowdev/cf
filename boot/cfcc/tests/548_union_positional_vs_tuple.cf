# a positional two-Int payload `Two(Int, Int)` stays distinct from a one-tuple payload
# expect: exit 7
union B = { Two(Int, Int) }
const get = (B b): Int -> match b {
	B.Two(a, c) -> a + c,
}
pub const main = () -> get(B.Two(3, 4))
