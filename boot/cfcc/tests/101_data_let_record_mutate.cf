# a `let` record is mutable: a field may be reassigned with `p.field = expr`
# expect: exit 14
data Point = { Int x, Int y }
pub const main = () -> {
	let Point p = { x: 3, y: 4 }
	p.x = 10
	return p.x + p.y
}
