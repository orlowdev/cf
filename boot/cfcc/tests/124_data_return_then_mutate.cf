# a returned record is owned by the caller and may be mutated
# expect: exit 15
data Point = { Int x, Int y }
const mk = (Int a) Point -> {
	const Point p = { x: a, y: a }
	return p
}
pub const main = () -> {
	let Point p = mk(5)
	p.x = 10
	return p.x + p.y
}
