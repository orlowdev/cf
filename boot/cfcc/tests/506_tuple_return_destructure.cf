# the multi-value return: a function returns a tuple, the caller destructures it
# expect: exit 5
const divmod = (Int a, Int b): (Int, Int) -> (a / b, a % b)
pub const main = () -> {
	const (q, r) = divmod(17, 5)
	return q + r
}
