# The prefix value form `defer f(x)` evaluates to its tapped argument x while
# scheduling f(x); binding it captures x.
# expect: exit 7
const tap = (Int x) Int -> x
const run = () Int -> {
	const Int v = defer tap(7)
	return v
}
pub const main = () -> run()
