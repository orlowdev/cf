# a record built inside a callee (trapped residue — only a scalar returns)
# expect: exit 6
# args: x y
data Box = { Int lo, Int hi }
const span = (Int n): Int -> {
	const Box b = { lo: n, hi: n * 3 }
	return b.hi - b.lo
}
pub const main = (Int argc) -> span(argc)
