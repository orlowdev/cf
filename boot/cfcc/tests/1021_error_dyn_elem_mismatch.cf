# a `[T]` argument whose element type differs from the parameter's is rejected.
# expect: error
data A = { Iarch x }
data B = { Iarch y }
const f = ([A] xs): Iarch -> 0
pub const main = () -> {
	let [B] bs = []
	return f(bs)
}
