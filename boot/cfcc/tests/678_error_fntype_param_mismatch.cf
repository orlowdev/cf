# A function value whose PARAMETER type differs from the function-type parameter is
# rejected: `wide` takes a `Uarch` where an `(Int) -> Int` is expected.
# expect: error
const apply = ((Int) -> Int f, Int x): Int -> f(x)
const wide = (Uarch n): Int -> 0
pub const main = () -> apply(wide, 5)
