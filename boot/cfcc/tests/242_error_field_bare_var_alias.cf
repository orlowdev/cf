# a record field must be a fresh value, not a bare record variable (alias rejected)
# expect: error
data Point = { Int x, Int y }
data Line = { Point a, Point b }
pub const main = () -> {
	const Point p = { x: 1, y: 2 }
	const Line l = { a: p, b: p }
	return l.a.x
}
