# The top-level restriction holds for the pipe-tap form too: no `defer` in a loop.
# expect: error
const f = (Int x): Int -> x
pub const main = () -> {
	loop {
		let Int y = 1 |> defer f
		break
	}
	return 0
}
