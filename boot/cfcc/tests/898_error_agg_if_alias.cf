# an aggregate `if` branch that is a bare variable would ALIAS it — rejected (§6)
# expect: error
data P = { Iarch x }
const pick = (P a, P b, Iarch c): P -> if c > 0 then a else b
pub const main = () -> 0
