# A chain may continue on the next line with a leading `|>`
# expect: exit 14
const inc = (Int x) -> x + 1
const sum = (Int a, Int b) -> a + b
pub const main = () -> {
	return 3
	  |> inc
	  |> sum(10)
}
