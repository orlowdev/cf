# a bare int literal adopts the other operand's fixed-width type (==, >, &)
# expect: exit 208
pub const main = () -> {
	let Uint8 b = Uint8(200)
	let Iarch hits = 0
	if b == 200 then hits = hits + 1
	if b > 100 then hits = hits + 1
	let Uint8 masked = b & 15
	return hits * 100 + Iarch(masked)
}
