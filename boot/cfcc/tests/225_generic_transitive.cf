# a generic that calls another generic, forwarding its type variable
# expect: exit 21
pub const id = ['T]('T x) 'T -> x
pub const twice = ['U]('U x) 'U -> id['U](id['U](x))
pub const main = () -> twice[Int](21) + twice[Int](0)
