# a record-returning function whose body is a call returning that record
# expect: exit 42
data P = { Int v }
const mk = (Int n) P -> {
	const P p = { v: n }
	return p
}
const wrap = (Int n) P -> mk(n)
pub const main = () -> {
	const P p = wrap(42)
	return p.v
}
