# A closure is a valid pipe target: `5 |> add` calls add(5), mutating the captured base.
# expect: exit 105
const run = () Int -> {
	let Int base = 100
	const add = (Int k) -> {
		base = base + k
		return base
	}
	return 5 |> add
}
pub const main = () -> run()
