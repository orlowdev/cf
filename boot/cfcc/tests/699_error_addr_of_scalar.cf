# `&` of a scalar value is illegal — the operand of `&` must be a record or union (§6.4).
# expect: error
data Point = { Int x }
const use = (*Point p): Int -> p.x
pub const main = () -> {
	const Int n = 5
	return use(&n)
}
