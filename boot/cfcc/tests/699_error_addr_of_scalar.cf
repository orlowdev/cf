# `&` of a scalar value is illegal — the operand of `&` must be a record or union (§6.4).
# expect: error
data Point = { Iarch x }
const use = (*Point p): Iarch -> p.x
pub const main = () -> {
	const Iarch n = 5
	return use(&n)
}
