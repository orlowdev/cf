# array elements are arbitrary Int expressions
# expect: exit 18
const dbl = (Int n): Int -> n * 2
pub const main = () -> {
	const Int a = 3
	const [3 Int] xs = [a + 1, dbl(a), 8]
	return xs[0] + xs[1] + xs[2]
}
