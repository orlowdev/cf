# explicit record construction `Point({ … })` in a return body
# expect: exit 7
data Point = { Int x, Int y }
const mk = (Int a, Int b): Point -> Point({ x: a, y: b })
pub const main = () -> {
	const Point p = mk(3, 4)
	return p.x + p.y
}
