# A record component in a function type is a later brick — rejected cleanly.
# expect: error
data Point = { Iarch x, Iarch y }
const apply = ((Point) -> Iarch f, Iarch x): Iarch -> x
pub const main = () -> 0
