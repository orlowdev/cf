# value-level record spread in a data literal is not supported yet
# expect: error
data Point = { Int x, Int y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	const Point b = { ...a, y: 5 }
	return b.x
}
