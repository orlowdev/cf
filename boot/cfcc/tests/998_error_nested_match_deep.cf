# expect: error
# a nested member sub-pattern is one level only — it cannot sub-match its own payload
union Inner = { Wrap(Iarch), None }
union Outer = { Box(Inner) }
pub const f = (Outer o): Iarch -> match o {
	Outer.Box(Wrap(x)) -> x,
	_ -> 0,
}
pub const main = () -> f(Outer.Box(Inner.None))
