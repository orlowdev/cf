# a spliced payload member boxes the whole union (a nullary member coexists)
# expect: exit 42
union P = { Has(Int) }
union Q = { ...P, None }
const eval = (Q q): Int -> match q {
	Q.Has(v) -> v,
	Q.None -> 0,
}
pub const main = () -> {
	const Q q = Q.Has(42)
	return eval(q)
}
