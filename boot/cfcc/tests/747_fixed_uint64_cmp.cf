# 64-bit unsigned compare (cultl/cugtl): a sum crossing 2^31 stays correct in the `l` register
# expect: exit 1
pub const main = () -> {
	const Uint64 s = Uint64(2000000000) + Uint64(2000000000)
	return Int(s > Uint64(2000000000))
}
