# count even elements with a value-if in the body
# expect: exit 3
pub const main = () -> {
	const [6 Int] xs = [1, 2, 3, 4, 5, 6]
	let Int evens = 0
	for x in xs {
		evens += if x % 2 == 0 then 1 else 0
	}
	return evens
}
