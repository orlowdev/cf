# Iarch and Int64 share a representation but are distinct types (§2) — they interconvert
# only through an explicit cast
# expect: exit 5
pub const main = () -> {
	const Int64 a = Int64(Iarch(5))
	const Iarch b = Iarch(a)
	return Int(b)
}
