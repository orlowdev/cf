# binding a record position is a later brick (skip it and index instead)
# expect: error
data P = { Int x }
const mk = (Int a): P -> P({ x: a })
pub const main = () -> {
	const (n, p) = (1, mk(2))
	return n
}
