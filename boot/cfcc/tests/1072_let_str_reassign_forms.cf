# a `let Str` is reassigned any Str-valued expression — a literal, an interpolation, and
# another Str variable — ending at "xyz" (length 3)
# expect: exit 3
pub const main = () -> {
	let Str s = "a"
	const Iarch n = 42
	s = "n=${n}"
	const Str other = "xyz"
	s = other
	return s.len
}
