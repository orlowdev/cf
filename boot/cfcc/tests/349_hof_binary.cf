# A binary function value `(Int, Int) Int` folded over three arguments.
# expect: exit 42
const fold2 = ((Int, Int) Int f, Int a, Int b, Int c) Int -> f(f(a, b), c)
const add = (Int x, Int y) -> x + y
pub const main = () -> fold2(add, 10, 20, 12)
