# a record assembled via spread is an ordinary record — passes through a function
# expect: exit 6
data Point = { Int x, Int y }
data Labeled = { ...Point, Int z }
const total = (Labeled p): Int -> p.x + p.y + p.z
pub const main = () -> {
	const Labeled p = { x: 1, y: 2, z: 3 }
	return total(p)
}
