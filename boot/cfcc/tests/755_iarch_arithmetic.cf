# Iarch reuses the fixed-width arithmetic machinery (signed 64-bit): (1000000 * 1000) / 7
# = 142857142; exit byte = 142857142 mod 256 = 182
# expect: exit 182
pub const main = () -> {
	const Iarch p = Iarch(1000000) * Iarch(1000)
	return Int(p / Iarch(7))
}
