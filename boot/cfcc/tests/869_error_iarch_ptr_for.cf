# a `*[Iarch]` pointer has no comptime length — `for … in` over it is rejected (index with `p[i]`)
# expect: error
const bad = (*[Iarch] xs): Iarch -> {
	let s = 0
	for x in xs {
		s = s + x
	}
	return s
}
pub const main = () -> 0
