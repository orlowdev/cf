# feed one call's destructured result into another call's tuple return
# expect: exit 3
const dm = (Int a, Int b): (Int, Int) -> (a / b, a % b)
pub const main = () -> {
	const (q, _) = dm(17, 5)
	const (q2, r2) = dm(q * 3, 4)
	return q2 + r2
}
