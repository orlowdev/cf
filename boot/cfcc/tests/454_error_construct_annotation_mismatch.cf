# the constructed type must match the binding annotation
# expect: error
data Point = { Int x, Int y }
data Vec = { Int x, Int y }
pub const main = () -> {
	const Vec v = Point({ x: 1, y: 2 })
	return v.x
}
