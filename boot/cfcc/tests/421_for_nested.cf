# nested `for` loops with distinct loop variables
# expect: exit 45
pub const main = () -> {
	const [2 Int] xs = [2, 3]
	const [2 Int] ys = [4, 5]
	let Int acc = 0
	for a in xs {
		for b in ys {
			acc += a * b
		}
	}
	return acc
}
