# three positions: Int, record, Int — offsets and per-position load widths line up
# expect: exit 9
data Pt = { Int x, Int y }
const mk = (Int a, Int b): Pt -> Pt({ x: a, y: b })
pub const main = () -> {
	const t = (2, mk(3, 4), 0)
	return t[0] + t[1].x + t[1].y
}
