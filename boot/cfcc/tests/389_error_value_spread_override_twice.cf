# overriding the same field twice is still an error under a spread
# expect: error
data Point = { Int x, Int y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	const Point b = { ...a, x: 5, x: 6 }
	return b.x
}
