# a bare `return { … }` is ambiguous with a block — must be parenthesized
# expect: error
data Point = { Int x, Int y }
const mk = (Int a): Point -> {
	return { x: a, y: a }
}
pub const main = () -> 0
