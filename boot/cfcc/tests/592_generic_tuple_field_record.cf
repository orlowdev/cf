# the `'T` tuple element may be instantiated at a record, then indexed and field-accessed
# expect: exit 25
data Point = { Int x, Int y }
data Box['T] = { (Int, 'T) p }
pub const main = () -> {
	const Box[Point] b = { p: (5, Point({ x: 8, y: 12 })) }
	return b.p[0] + b.p[1].x + b.p[1].y
}
