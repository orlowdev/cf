# a parameter can be passed as a call argument (argc is 4 with three args)
# args: a b c
# expect: exit 8
const twice = (Iarch x): Iarch -> x + x
pub const main = (Iarch argc, *[Str] argv) -> twice(argc)
