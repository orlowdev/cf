# `&&` short-circuits: a false left operand skips the right, so boom (infinite
# recursion, which would stack-overflow) is never called and this returns 0
# expect: exit 0
const boom = (Int n) Int -> boom(n)
pub const main = () -> 0 && boom(1)
