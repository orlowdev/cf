# a float literal whose integer part exceeds the Iarch range still lexes as a float
# (its magnitude is decoded by strtod, not the integer accumulator).
# expect: exit 7
pub const main = () -> if 9300000000000000000.5 > 1.0 then 7 else 0
