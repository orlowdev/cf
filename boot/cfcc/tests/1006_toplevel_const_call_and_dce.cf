# a const's value may call a function; an unreferenced const is pruned (DCE)
# expect: exit 25
const unused = 999
const base = 10
const bump = () -> base + 5
const derived = bump()
pub const main = () -> derived + base
