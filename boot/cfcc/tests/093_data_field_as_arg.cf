# a field read is an Int, so it may be a call argument
# expect: exit 12
data Point = { Int x, Int y }
const twice = (Int n) Int -> n + n
pub const main = () -> {
	const Point p = { x: 6, y: 1 }
	return twice(p.x)
}
