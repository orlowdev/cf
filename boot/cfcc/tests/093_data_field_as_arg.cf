# a field read is an Iarch, so it may be a call argument
# expect: exit 12
data Point = { Iarch x, Iarch y }
const twice = (Iarch n): Iarch -> n + n
pub const main = () -> {
	const Point p = { x: 6, y: 1 }
	return twice(p.x)
}
