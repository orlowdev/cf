# a bare spread `{ ...a }` copies every field
# expect: exit 3
data Point = { Iarch x, Iarch y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	const Point b = { ...a }
	return b.x + b.y
}
