# a function taking both a record and an Int parameter
# expect: exit 42
data Box = { Int w, Int h }
const scale = (Box b, Int k): Int -> (b.w + b.h) * k
pub const main = () -> {
	let Box b = { w: 3, h: 4 }
	return scale(b, 6)
}
