# a function taking both a record and an Iarch parameter
# expect: exit 42
data Box = { Iarch w, Iarch h }
const scale = (Box b, Iarch k): Iarch -> (b.w + b.h) * k
pub const main = () -> {
	let Box b = { w: 3, h: 4 }
	return scale(b, 6)
}
