# mutating one record does not disturb another of the same type
# expect: exit 120
data Box = { Int v }
pub const main = () -> {
	let Box a = { v: 10 }
	let Box b = { v: 20 }
	a.v = 100
	return a.v + b.v
}
