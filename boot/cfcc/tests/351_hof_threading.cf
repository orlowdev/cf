# A function-value parameter is itself a value and may be passed on to another HOF.
# expect: exit 42
const apply = ((Int) -> Int f, Int x): Int -> f(x)
const relay = ((Int) -> Int g, Int y): Int -> apply(g, y)
const inc = (Int n) -> n + 1
pub const main = () -> relay(inc, 41)
