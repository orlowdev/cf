# A defer scheduled before a loop still fires at the `return` after the loop.
# expect: exit 105
data Cell = { Int v }
const run = () Cell -> {
	let Cell c = { v: 0 }
	defer { c.v = c.v + 100 }
	loop {
		c.v = 5
		break
	}
	return c
}
pub const main = () -> run().v
