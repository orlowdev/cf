# a concrete tuple field rides through a generic record's instantiation unchanged
# expect: exit 16
data Box['T] = { (Int, Int) meta, 'T v }
pub const main = () -> {
	const Box[Int] b = { meta: (3, 4), v: 9 }
	return b.meta[0] + b.meta[1] + b.v
}
