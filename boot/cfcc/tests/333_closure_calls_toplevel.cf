# A closure body may call an ordinary top-level function, passing a captured value.
# expect: exit 36
const square = (Int x) -> x * x
const run = () Int -> {
	let Int n = 6
	const f = () -> square(n)
	return f()
}
pub const main = () -> run()
