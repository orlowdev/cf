# three fields at offsets 0, 4, 8 — precedence still applies to field reads
# expect: exit 7
data V = { Int a, Int b, Int c }
pub const main = () -> {
	const V v = { a: 1, b: 2, c: 3 }
	return v.a + v.b * v.c
}
