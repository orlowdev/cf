# a record tuple element must be fresh — aliasing an existing record is rejected
# expect: error
data P = { Int x }
const mk = (Int a): P -> P({ x: a })
pub const main = () -> {
	const P p = mk(1)
	const t = (0, p)
	return t[0]
}
