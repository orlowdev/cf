# A function value whose signature differs from the function-type parameter is rejected:
# `mk` returns `Uarch` where an `(Iarch) -> Iarch` is expected.
# expect: error
const apply = ((Iarch) -> Iarch f, Iarch x): Iarch -> f(x)
const mk = (Iarch n): Uarch -> n
pub const main = () -> apply(mk, 5)
