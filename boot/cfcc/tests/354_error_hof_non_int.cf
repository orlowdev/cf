# A function value whose signature differs from the function-type parameter is rejected:
# `mk` returns `Uarch` where an `(Int) -> Int` is expected.
# expect: error
const apply = ((Int) -> Int f, Int x): Int -> f(x)
const mk = (Int n): Uarch -> n
pub const main = () -> apply(mk, 5)
