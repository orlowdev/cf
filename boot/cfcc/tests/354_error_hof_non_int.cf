# A function value must be all-`Int` params and an `Int` return in M0.
# expect: error
const apply = ((Int) Int f, Int x) Int -> f(x)
const mk = (Int n) Uarch -> n
pub const main = () -> apply(mk, 5)
