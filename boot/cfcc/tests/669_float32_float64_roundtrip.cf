# Float64 <-> Float32 conversions: 3.5 narrows to a single and widens back exactly.
# expect: exit 7
pub const main = () -> {
	const Float64 d = 3.5
	const Float32 s = Float32(d)
	const Float64 back = Float64(s)
	return Int(back * 2.0)
}
