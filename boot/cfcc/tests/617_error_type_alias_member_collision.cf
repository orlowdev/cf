# a `type` alias colliding with a union MEMBER name is rejected — otherwise the bare
# member declaration would be silently rewritten (a mis-lowering the gate forbids).
# expect: error
union Light = { Red, Green, Blue }
type Green = Int
pub const main = () -> 0
