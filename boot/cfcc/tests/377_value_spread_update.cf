# value-level spread copies a record and overrides one field (immutable update)
# expect: exit 20
data Point = { Int x, Int y }
pub const main = () -> {
	const Point a = { x: 3, y: 4 }
	const Point b = { ...a, y: 10 }
	return a.x + a.y + b.x + b.y
}
