# `Int` does not satisfy a union bound; body is bound-agnostic so the bound is the sole cause
# expect: error
union Color = { Red, Green }
pub const f = [Color 'T]('T c): Int -> 0
pub const main = () -> {
	return f[Int](5)
}
