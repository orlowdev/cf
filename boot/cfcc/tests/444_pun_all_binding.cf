# an all-pun literal in an annotated binding
# expect: exit 30
data Point = { Int x, Int y }
pub const main = () -> {
	const Int x = 10
	const Int y = 20
	const Point p = { x, y }
	return p.x + p.y
}
