# an all-pun literal in an annotated binding
# expect: exit 30
data Point = { Iarch x, Iarch y }
pub const main = () -> {
	const Iarch x = 10
	const Iarch y = 20
	const Point p = { x, y }
	return p.x + p.y
}
