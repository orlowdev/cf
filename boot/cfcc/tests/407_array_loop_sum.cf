# iterate a fixed array by index, driven by `.len`
# expect: exit 10
pub const main = () -> {
	const [4 Int] xs = [1, 2, 3, 4]
	let Int sum = 0
	let Int i = 0
	loop {
		if i >= xs.len then break
		sum += xs[i]
		i += 1
	}
	return sum
}
