# a boxed union crosses function boundaries by pointer (param + return)
# expect: exit 2
union Node = { IntLit(Int), Nil }
pub const mk = (Int n) Node -> Node.IntLit(n)
pub const tag_of = (Node x) Int -> match x {
	Node.IntLit -> 2,
	Node.Nil -> 0,
}
pub const main = () -> tag_of(mk(9))
