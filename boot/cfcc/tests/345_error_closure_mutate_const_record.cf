# Capturing a `const` record is fine, but mutating its field through the closure
# is rejected.
# expect: error
data P = { Int x }
const run = () Int -> {
	const P p = { x: 1 }
	const f = () -> {
		p.x = 2
		return p.x
	}
	return f()
}
pub const main = () -> run()
