# Capturing a `const` record is fine, but mutating its field through the closure
# is rejected.
# expect: error
data P = { Iarch x }
const run = (): Iarch -> {
	const P p = { x: 1 }
	const f = () -> {
		p.x = 2
		return p.x
	}
	return f()
}
pub const main = () -> run()
