# literal fields may be given in any order — they map to declaration order
# expect: exit 12
data Point = { Int x, Int y }
pub const main = () -> {
	const Point p = { y: 4, x: 3 }
	return p.x * p.y
}
