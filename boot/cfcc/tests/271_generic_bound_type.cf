# a bound on a generic DATA type parameter `data Tagged[Color 'T]`
# expect: exit 5
union Color = { Red, Green, Blue }
data Tagged[Color 'T] = { 'T c, Int n }
pub const main = () -> {
	const Tagged[Color] t = { c: Color.Green, n: 5 }
	return t.n
}
