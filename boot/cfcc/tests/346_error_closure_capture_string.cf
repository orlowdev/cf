# Closures capture only `Iarch` and record variables — a `Str` capture is rejected.
# expect: error
const run = (): Iarch -> {
	const Str s = "hi"
	const f = () -> s.len
	return f()
}
pub const main = () -> run()
