# Closures capture only `Int` and record variables — a `Str` capture is rejected.
# expect: error
const run = () Int -> {
	const Str s = "hi"
	const f = () -> s.len
	return f()
}
pub const main = () -> run()
