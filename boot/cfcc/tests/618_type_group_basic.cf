# a grouped-params `type`: the callee `(Opts o)` splats to `(Int a, Int b)`; the caller
# passes ONE `{ a: …, b: … }` literal, desugared to positional field values.
# expect: exit 30
type Opts = { Int a, Int b }
const sum = (Opts o): Int -> a + b
pub const main = () -> sum({ a: 10, b: 20 })
