# a field value is an ordinary Int expression: it may read a parameter
# expect: exit 10
data Point = { Int x, Int y }
const mk = (Int n): Int -> {
	const Point p = { x: n, y: n }
	return p.x + p.y
}
pub const main = () -> mk(5)
