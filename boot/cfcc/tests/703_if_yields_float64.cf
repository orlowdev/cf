# an if-expression may yield a Float64 (its branches merge through an 8-byte slot)
# expect: exit 4
pub const pick = (Iarch c): Float64 -> if c then 1.5 else 2.5
pub const main = () -> Iarch(pick(1) + pick(0))
