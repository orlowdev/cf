# an if-expression may yield a Float64 (its branches merge through an 8-byte slot)
# expect: exit 4
pub const pick = (Int c): Float64 -> if c then 1.5 else 2.5
pub const main = () -> Int(pick(1) + pick(0))
