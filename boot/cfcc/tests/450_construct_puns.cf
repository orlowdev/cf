# construction combines with field puns
# expect: exit 11
data Point = { Int x, Int y }
const mk = (Int x, Int y): Point -> Point({ x, y })
pub const main = () -> {
	const Point p = mk(5, 6)
	return p.x + p.y
}
