# a negative literal arm and a fixed-width `Int8` scrutinee
# expect: exit 15
const sign = (Int8 n): Iarch -> match n {
	-5 -> 1,
	0 -> 2,
	_ -> 3,
}
pub const main = () -> sign(Int8(-5)) * 10 + sign(Int8(0)) + sign(Int8(9))
