# a pun reads a local binding, not only a parameter
# expect: exit 10
data Point = { Int x, Int y }
const mk = (Int a): Point -> {
	const Int x = a + 1
	const Int y = a * 2
	return ({ x, y })
}
pub const main = () -> {
	const Point p = mk(3)
	return p.x + p.y
}
