# a function builds a record and returns it (by arena pointer); the caller reads it
# expect: exit 10
data Point = { Int x, Int y }
const mk = (Int a) Point -> {
	const Point p = { x: a, y: a }
	return p
}
pub const main = () -> {
	const Point q = mk(5)
	return q.x + q.y
}
