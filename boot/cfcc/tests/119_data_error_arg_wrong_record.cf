# passing the wrong record type for a record parameter is rejected
# expect: error
data A = { Int x }
data B = { Int y }
const f = (A a) Int -> a.x
pub const main = () -> {
	let B b = { y: 1 }
	return f(b)
}
