# a record return may not be a bare parameter (it would alias the caller's argument)
# expect: error
data P = { Int x }
const id = (P p): P -> p
pub const main = () -> {
	const P a = { x: 1 }
	const P b = id(a)
	return b.x
}
