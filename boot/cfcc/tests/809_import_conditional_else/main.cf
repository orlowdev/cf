# the condition is false (target is not Linux), so the `else` branch is selected
# expect: exit 42
import "comptime" as { os, Os }

if os.target == Os.Linux then import "nope" as { v }
else import "impl" as { v }

pub const main = () -> v()
