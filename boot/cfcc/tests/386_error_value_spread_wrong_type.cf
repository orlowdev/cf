# the spread source must be the same record type as the target
# expect: error
data Point = { Int x, Int y }
data Vec = { Int x, Int y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	const Vec b = { ...a }
	return b.x
}
