# destructure a record position out of a tuple-returning call
# expect: exit 13
data P = { Int x, Int y }
const mk = (Int a, Int b): P -> P({ x: a, y: b })
const split = (Int n): (Int, P) -> (n, mk(n, n + 1))
pub const main = () -> {
	const (k, p) = split(6)
	return k + p.y  # 6 + 7
}
