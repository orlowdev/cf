# a returned record flows straight into another function's record parameter
# expect: exit 42
data P = { Iarch x, Iarch y }
const mk = (Iarch a, Iarch b): P -> {
	const P p = { x: a, y: b }
	return p
}
const sum = (P p): Iarch -> p.x + p.y
pub const main = () -> sum(mk(30, 12))
