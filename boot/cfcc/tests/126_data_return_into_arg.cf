# a returned record flows straight into another function's record parameter
# expect: exit 42
data P = { Int x, Int y }
const mk = (Int a, Int b): P -> {
	const P p = { x: a, y: b }
	return p
}
const sum = (P p): Int -> p.x + p.y
pub const main = () -> sum(mk(30, 12))
