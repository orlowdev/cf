# the loop variable is an ordinary Iarch usable in expressions
# expect: exit 14
pub const main = () -> {
	const [3 Iarch] xs = [1, 2, 3]
	let Iarch sum = 0
	for x in xs {
		sum += x * x
	}
	return sum
}
