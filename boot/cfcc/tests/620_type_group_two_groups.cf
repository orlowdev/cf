# two group literals passed back-to-back, preserving order (a + b then c + d).
# expect: exit 24
type Lo = { Int a, Int b }
type Hi = { Int c, Int d }
const g = (Lo l, Hi h): Int -> a + b + c + d
pub const main = () -> g({ a: 2, b: 4 }, { c: 8, d: 10 })
