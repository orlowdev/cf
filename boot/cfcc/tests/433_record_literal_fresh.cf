# a directly-returned literal is a fresh value (mutating one copy leaves others alone)
# expect: exit 105
data Point = { Int x, Int y }
const mk = (Int a): Point -> ({ x: a, y: a })
pub const main = () -> {
	const Point p = mk(5)
	let Point q = mk(9)
	q.x = 100
	return p.x + q.x
}
