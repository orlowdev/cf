# a directly-returned literal is a fresh value (mutating one copy leaves others alone)
# expect: exit 105
data Point = { Iarch x, Iarch y }
const mk = (Iarch a): Point -> ({ x: a, y: a })
pub const main = () -> {
	const Point p = mk(5)
	let Point q = mk(9)
	q.x = 100
	return p.x + q.x
}
