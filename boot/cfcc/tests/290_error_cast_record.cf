# a numeric cast takes a scalar integer — casting a record (aggregate) is rejected.
# expect: error
data Point = { Int x, Int y }
pub const main = () -> {
	const Point p = { x: 1, y: 2 }
	return Uarch(p)
}
