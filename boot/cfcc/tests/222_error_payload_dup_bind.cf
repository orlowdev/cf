# a payload binding name may not repeat in one pattern
# expect: error
union P = { Pair(Iarch, Iarch), Nil }
pub const main = () -> {
	const P p = P.Nil
	return match p { P.Pair(x, x) -> x, P.Nil -> 0 }
}
