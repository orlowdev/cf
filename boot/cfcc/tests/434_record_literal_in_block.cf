# `return ({ … })` works inside a block body too
# expect: exit 9
data Point = { Int x, Int y }
const mk = (Int a): Point -> {
	const Int d = a * 2
	return ({ x: a, y: d })
}
pub const main = () -> {
	const Point p = mk(3)
	return p.x + p.y
}
