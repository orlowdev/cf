# `return ({ … })` works inside a block body too
# expect: exit 9
data Point = { Iarch x, Iarch y }
const mk = (Iarch a): Point -> {
	const Iarch d = a * 2
	return ({ x: a, y: d })
}
pub const main = () -> {
	const Point p = mk(3)
	return p.x + p.y
}
