# a heterogeneous tuple: an Int and a fresh record; index the record then read a field
# expect: exit 47
data P = { Int x, Int y }
const mk = (Int a, Int b): P -> P({ x: a, y: b })
pub const main = () -> {
	const t = (5, mk(42, 100))
	return t[0] + t[1].x
}
