# a nested-tuple return flows into a matching nested-tuple parameter (same interned shape)
# expect: exit 10
const mk = (): ((Iarch, Iarch), Iarch) -> ((3, 4), 3)
const use = (((Iarch, Iarch), Iarch) t): Iarch -> t[0][0] + t[0][1] + t[1]
pub const main = () -> use(mk())
