# A function value over `Float64` — the callable and its result ride float registers.
# expect: exit 4
const apply = ((Float64) -> Float64 f, Float64 x): Float64 -> f(x)
const half = (Float64 n): Float64 -> n / 2.0
pub const main = () -> Int(apply(half, 8.0))
