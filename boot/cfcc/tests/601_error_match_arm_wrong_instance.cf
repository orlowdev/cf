# a match arm naming a DIFFERENT instance than the scrutinee is rejected
# expect: error
union U['T] = { Pair((Int, 'T)), Empty }
const sum = (U[Int] u): Int -> match u {
	U[Unit].Pair(p) -> p[0],
	U.Empty -> 0,
}
pub const main = () -> sum(U[Int].Pair((1, 2)))
