# `_` ignores a payload field; only the bound one is read
# expect: exit 42
union P = { Pt(Int, Int, Int) }
pub const mid = (P p) Int -> match p { P.Pt(_, y, _) -> y }
pub const main = () -> mid(P.Pt(1, 42, 3))
