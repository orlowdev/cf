# a generic function whose parameter, return, and local annotation are generic types
# expect: exit 9
data Box['T] = { 'T v }
const mk = ['T]('T x): Box['T] -> {
	const Box['T] b = { v: x }
	return b
}
pub const main = () -> {
	const Box[Iarch] b = mk[Iarch](9)
	return b.v
}
