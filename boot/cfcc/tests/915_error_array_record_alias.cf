# a record array element must be freshly built, not an alias of existing storage
# expect: error
data P = { Iarch x }
const mk = (Iarch a): P -> ({ x: a })
pub const main = () -> {
	const P q = mk(5)
	const [2 P] ps = [q, mk(2)]
	return 0
}
