# payload binding in a match arm is a later brick (M1.2b)
# expect: error
union Node = { IntLit(Int), Nil }
pub const main = () -> {
	const Node n = Node.IntLit(5)
	return match n { Node.IntLit(v) -> v, Node.Nil -> 0 }
}
