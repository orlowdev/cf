# a generic type instantiated with an argument that violates its bound
# expect: error
union Color = { Red, Green, Blue }
data Tagged[Color 'T] = { 'T c, Int n }
pub const main = () -> {
	const Tagged[Int] t = { c: 1, n: 5 }
	return t.n
}
