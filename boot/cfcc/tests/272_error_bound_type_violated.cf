# a generic type instantiated with an argument that violates its bound
# expect: error
union Color = { Red, Green, Blue }
data Tagged[Color 'T] = { 'T c, Iarch n }
pub const main = () -> {
	const Tagged[Iarch] t = { c: 1, n: 5 }
	return t.n
}
