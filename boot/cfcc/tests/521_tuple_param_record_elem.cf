# a tuple parameter carrying a record element; index it and read fields
# expect: exit 25
data P = { Int x, Int y }
const mk = (Int a, Int b): P -> P({ x: a, y: b })
const sum = ((Int, P) t): Int -> t[0] + t[1].x + t[1].y
pub const main = () -> sum((5, mk(8, 12)))
