# an aggregate `if`'s branches must yield the same type
# expect: error
data A = { Iarch x }
data B = { Iarch y }
const mkA = (): A -> A({ x: 1 })
const mkB = (): B -> B({ y: 2 })
const bad = (Iarch c): A -> if c > 0 then mkA() else mkB()
pub const main = () -> 0
