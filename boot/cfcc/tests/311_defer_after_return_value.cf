# A defer fires AFTER the return value is fixed: `return c.v` snapshots 7 into the
# result before the defer zeroes the field, so the caller still sees 7.
# expect: exit 7
data Cell = { Int v }
const run = () Int -> {
	let Cell c = { v: 7 }
	defer { c.v = 0 }
	return c.v
}
pub const main = () -> run()
