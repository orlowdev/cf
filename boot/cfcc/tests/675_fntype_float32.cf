# A function value over `Float32` — a single-precision callable and result.
# expect: exit 3
const apply = ((Float32) -> Float32 f, Float32 x): Float32 -> f(x)
const half = (Float32 n): Float32 -> n / Float32(2.0)
pub const main = () -> Int(apply(half, Float32(6.0)))
