# break then a trailing return yields an early result from a loop
# expect: exit 7
const first_over = (Int cap): Int -> {
	let i = 0
	loop {
		if i * i > cap then break
		i = i + 1
	}
	return i
}
pub const main = () -> first_over(40)
