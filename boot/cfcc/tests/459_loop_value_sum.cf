# a value-yielding loop: accumulate 0..9, then `<- sum` breaks with the total
# expect: exit 45
pub const main = () -> {
	let sum = 0
	let i = 0
	let r = loop {
		if i >= 10 then <- sum
		sum = sum + i
		i = i + 1
	}
	return r
}
