# an import CYCLE is legal: module a imports b, b imports a, and their functions are
# mutually recursive. even(10) = 1, even(7) = 0 → 1.
# expect: exit 1
import "a" as { even }

pub const main = () -> even(10) + even(7)
