# a record parameter may be passed on to another function (still by pointer)
# expect: exit 99
data P = { Int x }
const inner = (P p) Int -> p.x
const outer = (P p) Int -> inner(p)
pub const main = () -> {
	let P p = { x: 99 }
	return outer(p)
}
