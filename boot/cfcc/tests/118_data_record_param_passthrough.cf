# a record parameter may be passed on to another function (still by pointer)
# expect: exit 99
data P = { Iarch x }
const inner = (P p): Iarch -> p.x
const outer = (P p): Iarch -> inner(p)
pub const main = () -> {
	let P p = { x: 99 }
	return outer(p)
}
