# a private helper feeds the exported constructor — it is mangled apart on flatten
const bias = () -> 2

pub data Point = { Iarch x, Iarch y }

pub const origin = (): Point -> Point({ x: bias(), y: 0 })
