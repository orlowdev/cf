# a generic record may have a tuple field with a `'T` element, substituted per instantiation
# expect: exit 30
data Box['T] = { (Int, 'T) p }
pub const main = () -> {
	const Box[Int] b = { p: (10, 20) }
	return b.p[0] + b.p[1]
}
