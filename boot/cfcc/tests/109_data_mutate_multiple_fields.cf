# several fields of one record, each mutated
# expect: exit 42
data V = { Int a, Int b, Int c }
pub const main = () -> {
	let V v = { a: 0, b: 0, c: 0 }
	v.a = 40
	v.b = 2
	v.c = v.a + v.b
	return v.a + v.b - v.c + 42
}
