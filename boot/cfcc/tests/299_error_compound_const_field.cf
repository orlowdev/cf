# a `const` record's field cannot be mutated — including via a compound op
# expect: error
data Point = { Int x, Int y }
pub const main = () -> {
	const Point p = { x: 1, y: 2 }
	p.x += 1
	return p.x
}
