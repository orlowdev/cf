# whole-vector reassignment of a `[T]` field is a later brick — rejected cleanly.
# expect: error
data Ctx = { [Iarch] xs, Iarch pos }
const swap = (*Ctx c, [Iarch] ys): Iarch -> {
	c.xs = ys
	return 0
}
pub const main = () -> 0
