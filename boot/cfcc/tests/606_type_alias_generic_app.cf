# an alias of a generic application `Box[Int]` — the mangled instance splices in.
# expect: exit 42
data Box['T] = { 'T v }
type IntBox = Box[Int]
pub const main = () -> {
	const IntBox b = { v: 42 }
	return b.v
}
