# the group literal matches fields by NAME, so a caller may write them out of order.
# expect: exit 3
type Two = { Int a, Int b }
const s = (Two t): Int -> a + b
pub const main = () -> s({ b: 2, a: 1 })
