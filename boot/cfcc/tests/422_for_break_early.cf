# a guarded break exits the loop mid-iteration
# expect: exit 30
pub const main = () -> {
	const [4 Int] xs = [10, 20, 30, 40]
	let Int result = 0
	for x in xs {
		result = x
		if x > 25 then break
	}
	return result
}
