# the stringified content of an expression hole is correct: `${a + b}` with a=40,b=2
# renders "42"; the first byte is '4' (ASCII 52)
# expect: exit 52
pub const main = () -> {
	let Iarch a = 40
	let Iarch b = 2
	const Str m = "${a + b}"
	return Iarch(m[0])
}
