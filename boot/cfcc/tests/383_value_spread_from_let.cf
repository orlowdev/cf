# the spread source may be a `let` record (its fields are read, not moved)
# expect: exit 7
data Point = { Int x, Int y }
pub const main = () -> {
	let Point a = { x: 1, y: 2 }
	a.x = 5
	const Point b = { ...a }
	return b.x + b.y
}
