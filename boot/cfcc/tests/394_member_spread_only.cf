# a union whose only entry is a spread inherits every member
# expect: exit 2
union Small = { A, B }
union Clone = { ...Small }
const rank = (Clone c): Int -> match c {
	Clone.A -> 1,
	Clone.B -> 2,
}
pub const main = () -> {
	const Clone x = Clone.B
	return rank(x)
}
