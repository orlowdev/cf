# The passed function value must match the parameter's arity.
# expect: error
const apply = ((Iarch) -> Iarch f, Iarch x): Iarch -> f(x)
const add = (Iarch a, Iarch b) -> a + b
pub const main = () -> apply(add, 5)
