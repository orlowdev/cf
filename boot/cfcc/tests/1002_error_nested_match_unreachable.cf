# expect: error
# a guarded arm after a full catch-all for the same member is unreachable
union M = { A(Iarch), B }
pub const f = (M m): Iarch -> match m {
	M.A(n) -> n,
	M.A(0) -> 1,
	M.B -> 2,
}
pub const main = () -> f(M.B)
