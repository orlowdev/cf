# A closure is not a first-class value in M0 — it can only be called.
# expect: error
const use = (Iarch x) -> x
const run = (): Iarch -> {
	let Iarch i = 0
	const f = () -> {
		i = i + 1
		return i
	}
	return use(f)
}
pub const main = () -> run()
