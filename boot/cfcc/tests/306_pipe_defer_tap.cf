# `x |> defer f` ≡ `defer f(x)`: it schedules f(x) at scope exit and evaluates to
# the tapped value x, which passes through. Here inc(3) is deferred (discarded) and
# the tap yields 3.
# expect: exit 3
const inc = (Int x) -> x + 1
pub const main = () -> {
	return 3 |> defer inc
}
