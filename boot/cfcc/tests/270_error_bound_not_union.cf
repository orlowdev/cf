# a bound must name a UNION, not a data record (type_system §8.5: constraints are unions)
# expect: error
data Point = { Int x, Int y }
pub const f = [Point 'T]('T p): Int -> 0
pub const main = () -> {
	return f[Point](0)
}
