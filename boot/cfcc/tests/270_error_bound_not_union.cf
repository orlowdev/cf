# a bound must name a UNION, not a data record (type_system §8.5: constraints are unions)
# expect: error
data Point = { Iarch x, Iarch y }
pub const f = [Point 'T]('T p): Iarch -> 0
pub const main = () -> {
	return f[Point](0)
}
