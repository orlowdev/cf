# a union may span lines (the idiomatic AST-node form); match dispatches on the tag
# expect: exit 2
union NodeKind = {
	IntLit,
	BinOp,
	Nil,
}
pub const main = () -> {
	const NodeKind k = NodeKind.Nil
	return match k {
		NodeKind.IntLit -> 0,
		NodeKind.BinOp -> 1,
		NodeKind.Nil -> 2,
	}
}
