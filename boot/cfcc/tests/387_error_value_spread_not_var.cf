# the spread source must be a bare record variable, not a call
# expect: error
data Point = { Int x, Int y }
const mk = (Int a): Point -> {
	const Point q = { x: a, y: a }
	return q
}
pub const main = () -> {
	const Point b = { ...mk(1), y: 5 }
	return b.x
}
