# a `'T` element in a tuple field needs the enclosing data type to declare it —
# `R` is not generic, so the `'T` is an undeclared type variable
# expect: error
data R = { (Iarch, 'T) p }
pub const main = () -> 0
