# v1 closures capture only `Int` variables — capturing/mutating an enclosing record
# is rejected.
# expect: error
data P = { Int x }
const run = () Int -> {
	let P p = { x: 1 }
	const f = () -> {
		p.x = 2
		return 0
	}
	const a = f()
	return p.x
}
pub const main = () -> run()
