# a generic union with a tuple payload constructs and matches (arms use the BASE name)
# expect: exit 30
union U['T] = { Pair((Int, 'T)), Empty }
const sum = (U[Int] u): Int -> match u {
	U.Pair(p) -> p[0] + p[1],
	U.Empty -> 0,
}
pub const main = () -> sum(U[Int].Pair((10, 20)))
