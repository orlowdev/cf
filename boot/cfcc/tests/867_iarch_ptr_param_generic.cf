# a `*[Iarch]` param inside a generic function (is_words survives monomorphization)
# expect: exit 42
const first_or = ['T](*[Iarch] xs, 'T tag): Iarch -> xs[0]
pub const main = () -> {
	let [2 Iarch] a = [42, 0]
	return first_or[Iarch](a, 5)
}
