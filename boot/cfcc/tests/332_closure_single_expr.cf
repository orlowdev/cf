# A single-expression closure body that reads a captured variable.
# expect: exit 42
const run = (): Iarch -> {
	let Iarch x = 21
	const dbl = () -> x + x
	return dbl()
}
pub const main = () -> run()
