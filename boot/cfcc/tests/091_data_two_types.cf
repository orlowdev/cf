# two distinct record types coexist
# expect: exit 30
data Point = { Int x, Int y }
data Rect = { Int w, Int h }
pub const main = () -> {
	const Point p = { x: 2, y: 3 }
	const Rect r = { w: 4, h: 6 }
	return p.x * p.y + r.w * r.h
}
