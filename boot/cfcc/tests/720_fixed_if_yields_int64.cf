# an if yielding a 64-bit Int64 exercises the 8-byte merge slot with an `l` value
# expect: exit 112
pub const pick = (Iarch c): Int64 -> if c then Int64(70000) else Int64(1)
pub const main = () -> Iarch(pick(1))
