# a union payload argument must match the declared payload type
# expect: error
data Point = { Int x, Int y }
union Shape = { Dot(Point), Empty }
pub const main = () -> {
	const Shape s = Shape.Dot(5)
	return 0
}
