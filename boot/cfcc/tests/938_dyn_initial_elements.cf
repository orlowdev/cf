# a `[T]` bound to an initial literal, then grown further
# expect: exit 60
pub const main = () -> {
	let [Iarch] xs = [10, 20]
	xs = [...xs, 30]
	let Iarch acc = 0
	for x in xs {
		acc = acc + x
	}
	return acc
}
