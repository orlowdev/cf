# break and continue steer a `for` loop (continue advances to the next element)
# expect: exit 70
pub const main = () -> {
	const [5 Int] xs = [10, 20, 30, 40, 50]
	let Int total = 0
	for x in xs {
		if x == 30 then continue
		if x == 50 then break
		total += x
	}
	return total
}
