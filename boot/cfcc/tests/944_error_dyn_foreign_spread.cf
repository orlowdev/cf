# cfcc grows a `[T]` only in place: the spread source must be the assigned name
# expect: error
pub const main = () -> {
	let [Iarch] xs = [1, 2]
	let [Iarch] ys = [3, 4]
	ys = [...xs, 5]
	return 0
}
