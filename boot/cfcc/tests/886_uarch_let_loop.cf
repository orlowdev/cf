# a `let Uarch` accumulator mutated in a loop (Uarch is now a fully operable integer)
# expect: exit 10
pub const main = () -> {
	let Uarch sum = 0
	let Uarch i = 0
	loop {
		if Iarch(i) >= 5 then break
		sum = sum + i
		i = i + Uarch(1)
	}
	return Iarch(sum)
}
