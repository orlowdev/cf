# v1 does not support closures nested inside closures.
# expect: error
const run = () Int -> {
	let Int i = 0
	const outer = () -> {
		const inner = () -> {
			i = i + 1
			return i
		}
		return inner()
	}
	return outer()
}
pub const main = () -> run()
