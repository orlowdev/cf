# a type-switch instantiated at a width with no arm and no `_` is rejected
# expect: error
pub const f = [Int 'T]('T x): Iarch -> match x { Int.Int8(v) -> 8 }
pub const main = () -> f[Int32](Int32(1))
