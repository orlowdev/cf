# a `const [T]` is an immutable snapshot: reads work, no growth
# expect: exit 6
pub const main = () -> {
	const [Iarch] xs = [1, 2, 3]
	let Iarch acc = 0
	for x in xs {
		acc = acc + x
	}
	return acc
}
