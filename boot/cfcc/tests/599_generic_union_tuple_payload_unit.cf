# the generic union's tuple payload element may be `Unit`
# expect: exit 7
union U['T] = { Pair((Iarch, 'T)), Empty }
const first = (U[Unit] u): Iarch -> match u {
	U.Pair(p) -> p[0],
	U.Empty -> 0,
}
pub const main = () -> first(U[Unit].Pair((7, ())))
