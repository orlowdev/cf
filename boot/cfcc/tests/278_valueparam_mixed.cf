# a value parameter and a type parameter in one generic head: `[Iarch n, 'T]`. The value
# slot is present (and mangled) but unused here; the type slot still specializes normally.
# expect: exit 7
pub const keep = [Iarch n, 'T]('T x): 'T -> x
pub const main = () -> {
	return keep[3, Iarch](7)
}
