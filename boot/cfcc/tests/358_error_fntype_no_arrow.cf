# A function type separates its return with `->` — `(Iarch) Iarch` is rejected.
# expect: error
const apply = ((Iarch) Iarch f, Iarch x): Iarch -> f(x)
const inc = (Iarch n): Iarch -> n + 1
pub const main = () -> apply(inc, 5)
