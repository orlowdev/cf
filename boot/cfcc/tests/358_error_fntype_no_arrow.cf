# A function type separates its return with `->` — `(Int) Int` is rejected.
# expect: error
const apply = ((Int) Int f, Int x): Int -> f(x)
const inc = (Int n): Int -> n + 1
pub const main = () -> apply(inc, 5)
