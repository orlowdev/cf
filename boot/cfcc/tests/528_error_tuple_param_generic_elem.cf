# a `'T` element in a tuple parameter needs the enclosing function to declare it —
# `f` is not generic, so the `'T` is an undeclared type variable
# expect: error
const f = (('T, Iarch) p): Iarch -> p[1]
pub const main = () -> 0
