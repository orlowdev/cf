# Capturing a `const` is fine, but mutating it through the closure is not.
# expect: error
const run = (): Iarch -> {
	const c = 5
	const f = () -> {
		c = c + 1
		return c
	}
	return f()
}
pub const main = () -> run()
