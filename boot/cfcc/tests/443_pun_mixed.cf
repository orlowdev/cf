# puns and explicit `field: value` entries may be mixed
# expect: exit 6
data V = { Int a, Int b, Int c }
pub const main = () -> {
	const Int a = 1
	const Int c = 3
	const V v = { a, b: 2, c }
	return v.a + v.b + v.c
}
