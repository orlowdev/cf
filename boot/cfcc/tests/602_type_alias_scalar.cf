# a plain comptime `type` alias of a scalar: `Count` is erased to `Int` everywhere —
# param, return, and local annotation positions all splice the target.
# expect: exit 7
type Count = Int
const bump = (Count n): Count -> n + 1
pub const main = () -> {
	const Count c = 6
	return bump(c)
}
