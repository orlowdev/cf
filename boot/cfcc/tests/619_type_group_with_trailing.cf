# a group literal alongside an ordinary trailing argument, in order.
# expect: exit 36
type Pt = { Iarch x, Iarch y }
const f = (Pt p, Iarch k): Iarch -> x + y + k
pub const main = () -> f({ x: 10, y: 20 }, 6)
