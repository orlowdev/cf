# a whole record cannot be reassigned (mutate a field) — compound form rejected too
# expect: error
data Point = { Iarch x, Iarch y }
pub const main = () -> {
	let Point p = { x: 1, y: 2 }
	p += 1
	return p.x
}
