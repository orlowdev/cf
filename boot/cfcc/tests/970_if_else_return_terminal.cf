# `if c then return a else return b` as the whole body — terminal, no trailing return
# expect: exit 13
pub const absv = (Iarch n): Iarch -> {
	if n < 0 then return 0 - n else return n
}
pub const main = () -> absv(-8) + absv(5)
