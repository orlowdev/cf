# A chain reads left-to-right and is left-associative: sum(10, inc(inc(3)))
# expect: exit 15
const inc = (Int x) -> x + 1
const sum = (Int a, Int b) -> a + b
pub const main = () -> {
	return 3 |> inc |> inc |> sum(10)
}
