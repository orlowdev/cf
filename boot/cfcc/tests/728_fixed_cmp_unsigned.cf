# unsigned comparison uses the unsigned condition (cult…): Uint8 200 > 100 is 1, whereas a
# SIGNED reading (200 = -56) would make it 0 — proving signedness drives the compare
# expect: exit 1
pub const main = () -> {
	const Uint8 a = 200
	return Iarch(a > Uint8(100))
}
