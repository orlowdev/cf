# unsigned comparison uses the unsigned condition (cult…): Uint8 200 > 100 is true, whereas a
# SIGNED reading (200 = -56) would make it false — proving signedness drives the compare
# expect: exit 1
pub const main = () -> {
	const Uint8 a = 200
	return if a > Uint8(100) then 1 else 0
}
