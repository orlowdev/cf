# growing a `[T]` field in place (`c.f = [...c.f, e]`) is a later brick — rejected cleanly.
# expect: error
data Ctx = { [Iarch] xs, Iarch pos }
const push = (*Ctx c, Iarch x): Iarch -> {
	c.xs = [...c.xs, x]
	return 0
}
pub const main = () -> 0
