# an `if` may itself yield a Bool (merged through the scalar slot), then feed another `if`
# expect: exit 1
pub const main = () -> {
	const Bool b = if 5 > 3 then true else false
	return if b then 1 else 0
}
