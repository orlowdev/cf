# a `'T` tuple-parameter element instantiated at a record is indexed and field-accessed
# expect: exit 20
data Point = { Iarch x, Iarch y }
const px = ['T]((Iarch, 'T) p): Iarch -> p[0]
pub const main = () -> px[Point]((20, Point({ x: 1, y: 2 })))
