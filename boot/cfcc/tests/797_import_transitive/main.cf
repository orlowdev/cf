# transitive imports: main imports a, a imports b, and a's exported function calls b's.
# The whole chain flattens — 0 + 2 + 40 = 42.
# expect: exit 42
import "a" as { f }

pub const main = () -> f(0)
