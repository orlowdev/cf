# a tuple parameter is passed by pointer and indexed at comptime positions
# expect: exit 30
const addp = ((Int, Int) p): Int -> p[0] + p[1]
pub const main = () -> {
	const t = (10, 20)
	return addp(t) + addp((0, 0))
}
