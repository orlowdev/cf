# two independent records of the same type keep separate storage
# expect: exit 21
data Point = { Int x, Int y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	const Point b = { x: 10, y: 20 }
	return a.x + b.y
}
