# A `return` reached inside a loop body fires the pending defer just like any other.
# expect: exit 105
data Cell = { Int v }
const run = () Cell -> {
	let Cell c = { v: 5 }
	defer { c.v = c.v + 100 }
	loop {
		return c
	}
	return c
}
pub const main = () -> run().v
