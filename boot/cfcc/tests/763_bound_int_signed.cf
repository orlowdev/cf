# the std numeric union `Int` (§8.6) is a generic BOUND: `[Int 'T]` accepts any signed width
# (here Int8), and the body narrows via a cast to the operable Iarch
# expect: exit 100
pub const widen = [Int 'T]('T x): Iarch -> Iarch(x)
pub const main = () -> widen[Int8](Int8(100))
