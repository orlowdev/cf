# a heterogeneous (Int, Str) return, destructured into an Int and a Str
# expect: exit 8
const tag = (Int n): (Int, Str) -> (n, "hey")
pub const main = () -> {
	const (n, s) = tag(5)
	return n + s.len
}
