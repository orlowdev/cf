# a record tuple element can be bound to a `const` local by direct index too
# expect: exit 20
data P = { Iarch x }
const mk = (Iarch a): P -> P({ x: a })
pub const main = () -> {
	const t = (0, mk(20))
	const p = t[1]
	return p.x
}
