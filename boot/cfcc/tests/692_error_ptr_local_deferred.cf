# A `*Point` LOCAL is deferred to a later brick (pointers live in params/returns for now).
# expect: error
data Point = { Int x, Int y }
pub const main = () -> {
	const Point pt = { x: 1, y: 2 }
	const *Point q = pt
	return 0
}
