# a by-value `[N T]` param checks the argument's element type (a wrong-width element read
# through the wrong access would misread)
# expect: error
const third = ([3 Iarch] xs): Iarch -> xs[2]
pub const main = () -> {
	const [3 Int32] a = [1, 2, 3]
	return third(a)
}
