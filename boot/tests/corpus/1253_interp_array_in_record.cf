# an array-typed record field stringifies through the recursive path: `B({ ns: [1, 2] })` (17 bytes)
# expect: exit 17
data B = { [Iarch] ns }
pub const main = () -> {
	const B b = B({ ns: [1, 2] })
	const Str m = "${b}"
	return m.len
}
