# Bool true -> "true", length 4
# expect: exit 4

import "std/mem/arena/growing_arena" as growing_arena
const main_body! = (): Iarch -> {
	let Bool f = true
	const Str m = "${f}"
	return m.len
}

pub const main = () -> {
	const a = growing_arena.of(4096)
	return main_body!() in a
}
