# Bool true -> "true", length 4
# expect: exit 4

# check: root
const main_body! = (): Iarch -> {
	let Bool f = true
	const Str m = "${f}"
	return m.len
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
