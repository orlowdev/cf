# block scoping still rejects shadowing a name that is in an ENCLOSING open scope (only
# sibling/closed reuse is allowed).
# expect: error
pub const main = () -> {
	const Iarch x = 1
	if x == 1 then {
		const Iarch x = 2
		return x
	}
	return x
}
