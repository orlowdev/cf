# a returned tuple whose positions are computed by value-`if`s (min, max)
# expect: exit 10
const minmax = (Iarch a, Iarch b): (Iarch, Iarch) -> (if a < b then a else b, if a < b then b else a)
pub const main = () -> {
	const (lo, hi) = minmax(7, 3)
	return lo + hi
}
