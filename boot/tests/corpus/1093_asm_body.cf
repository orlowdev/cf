# an `-> asm "…"` naked assembly function: `${n}` resolves to the argument register, the
# body runs verbatim (double via `add`), and a QBE-lowered caller `bl`s it.
# expect: exit 42
const dbl = (Iarch n): Iarch -> asm "
	add x0, ${n}, ${n}
	ret
"
pub const main = () -> dbl(21)
