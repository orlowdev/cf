# a type-switch arm naming a width outside the bound's union is rejected (Uint8 ∉ Int)
# expect: error
pub const f = [Int 'T]('T x): Iarch -> match x { Int.Uint8(v) -> 8, _ -> 0 }
pub const main = () -> f[Int8](Int8(1))
