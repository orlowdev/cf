# several members reached through one namespace, one of them calling a module-private
# helper internally — 100 - (30 + 28) = 42
# expect: exit 42
import "math" as math

pub const main = () -> math.answer() - math.add(30, 28)
