# a tuple interpolation hole renders `(1, true)` — a Bool element rides the same stringifier (9 bytes)
# expect: exit 9
pub const main = () -> {
	const t = (1, true)
	const Str m = "${t}"
	return m.len
}
