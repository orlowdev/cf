# a boxed-union interpolation hole renders `E.Right(1)` via a runtime tag dispatch (10 bytes)
# expect: exit 10
union E = { Left(Iarch), Right(Iarch) }
pub const main = () -> {
	const E e = E.Right(1)
	const Str m = "${e}"
	return m.len
}
