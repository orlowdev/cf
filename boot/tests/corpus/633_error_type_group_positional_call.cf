# a grouped-params callee must be called with a `{…}` literal, not positional args.
# expect: error
type Opts = { Iarch a, Iarch b }
const sum = (Opts o): Iarch -> a + b
pub const main = () -> sum(10, 20)
