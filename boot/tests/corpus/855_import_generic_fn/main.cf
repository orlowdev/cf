# a generic data type + a generic function, both imported and instantiated
# expect: exit 99
import "boxfn" as { Box, unbox }
pub const main = () -> {
	const Box[Iarch] b = { v: 99 }
	return unbox[Iarch](b)
}
