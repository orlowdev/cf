# a dynamic-ARRAY tuple element (previously a rejected "later brick"): the array header pointer rides
# in the position's 8-byte slot, spelled `[Elem]` so `t[0][i]` indexes into it. t[0][1]=20 + t[1]=5.
# expect: exit 25
pub const main = () -> {
	const t = ([10, 20, 30], 5)
	return t[0][1] + t[1]
}
