# an if-expression may yield a fixed-width value (merged through the scalar slot)
# expect: exit 44
pub const pick = (Iarch c): Int16 -> if c != 0 then Int16(300) else Int16(7)
pub const main = () -> Iarch(pick(1))
