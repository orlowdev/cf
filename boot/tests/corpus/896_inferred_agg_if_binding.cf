# an inferred aggregate binding from an `if` (`const n = if ...`) retypes the local
# expect: exit 7
data Node = { Iarch tag }
const mk = (Iarch t): Node -> Node({ tag: t })
pub const main = () -> {
	const n = if 1 > 0 then mk(7) else mk(9)
	return n.tag
}
