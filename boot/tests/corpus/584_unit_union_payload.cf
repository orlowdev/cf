# a union member may carry a `Unit` payload; construct and match it
# expect: exit 7
union U = { Wrap(Unit), Empty }
pub const main = () -> {
	const U u = U.Wrap(())
	return match u {
		U.Wrap(x) -> 7,
		U.Empty -> 0,
	}
}
