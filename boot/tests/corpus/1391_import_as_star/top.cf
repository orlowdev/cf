# `as *` (module_system §5): splice `impl_darwin`'s WHOLE exported surface into `top` FLAT — no
# nesting, no enumeration. Consumers of `top` reach `top::one`/`top::two` directly (not
# `top::impl_darwin::one`). This is the barrel pattern that avoids re-listing members by hand.
pub import impl_darwin as *
