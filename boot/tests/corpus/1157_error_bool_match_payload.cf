# a Bool member is nullary — a payload pattern `Bool.True(x)` is rejected (not an OOB crash)
# even when it is the first arm
# expect: error
pub const main = () -> {
	const Bool b = true
	return match b { Bool.True(x) -> x, Bool.False -> 0 }
}
