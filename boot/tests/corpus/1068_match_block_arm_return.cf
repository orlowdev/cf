# a block arm may `return` from the enclosing function (a terminal arm)
# expect: exit 2
union K = { A, B, C }
const classify = (K k): Iarch -> {
	match k {
		K.A -> { return 1 },
		K.B -> { return 2 },
		K.C -> { return 3 },
	}
	return 0
}
pub const main = () -> classify(K.B)
