# A capture-free closure is a valid function value — it has a runtime code address.
# expect: exit 36
const apply = ((Iarch) -> Iarch f, Iarch x): Iarch -> f(x)
pub const main = () -> {
	const sq = (Iarch n) -> n * n
	return apply(sq, 6)
}
