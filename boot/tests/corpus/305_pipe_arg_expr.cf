# The piped value is the LAST argument; earlier args are ordinary expressions
# expect: exit 9
const sub = (Iarch a, Iarch b) -> a - b
pub const main = () -> {
	# sub(2 * 5, 1) = 10 - 1 = 9  (piped `1` fills b, not a)
	return 1 |> sub(2 * 5)
}
