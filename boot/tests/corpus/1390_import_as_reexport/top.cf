# a `pub import ... as <name>` reexports the module AS a namespace under the renamed alias `console`
# (not the last segment `impl_darwin`), so an importer of `top` reaches it through `top::console`.
pub import impl_darwin as console
