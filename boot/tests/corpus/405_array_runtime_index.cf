# the index may be a runtime value
# expect: exit 20

import std::mem::arena::fixed_arena
const main_body! = (): Iarch -> {
	const [3 Iarch] xs = [10, 20, 30]
	let Iarch i = 1
	return xs[i]
}

pub const main = () -> {
	const a = fixed_arena::of(4096)
	return main_body!() in a
}
