# `Bool.False` is the explicit qualified form of the `false` literal (§8.6), the same value
# as the sugar — it drives the `else` branch of an `if`
# expect: exit 5
pub const main = () -> {
	const Bool b = Bool.False
	return if b then 1 else 5
}
