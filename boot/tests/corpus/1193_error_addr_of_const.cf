# `&` on a `const` is forbidden — a `*T` parameter only ever points into a `let` (memory_model §6).
# expect: error
data Point = { Iarch x }
const bump = (*Point p): Iarch -> {
	p.x = 5
	return p.x
}
pub const main = () -> {
	const Point c = { x: 1 }
	return bump(&c)
}
