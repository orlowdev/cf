# literal fields are arbitrary expressions
# expect: exit 9
data Point = { Iarch x, Iarch y }
const mk = (Iarch a): Point -> ({ x: a + 1, y: a * 3 })
pub const main = () -> {
	const Point p = mk(2)
	return p.x + p.y
}
