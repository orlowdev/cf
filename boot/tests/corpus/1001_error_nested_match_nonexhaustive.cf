# expect: error
# a member covered only by a guarded arm is not exhaustive without a `_`
union M = { A(Iarch), B }
pub const f = (M m): Iarch -> match m {
	M.A(0) -> 1,
	M.B -> 2,
}
pub const main = () -> f(M.B)
