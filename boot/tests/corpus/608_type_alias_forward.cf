# order-independent: the alias is used textually before its `type` decl (collection
# precedes substitution — closer to cf's Resolve arc than cfcc's other features).
# expect: exit 8
const twice = (Count n): Count -> n + n
pub const main = () -> twice(4)
type Count = Iarch
