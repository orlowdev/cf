# A binding annotation must name a KNOWN type — an undeclared name is rejected instead of
# silently falling through to the initializer's inferred type.
# expect: error
pub const main = (): Iarch -> {
	const Nope n = 5
	return 0
}
