# `for x in xs` iterates a scalar-element `[Iarch]` param (the loop var is the element).
# expect: exit 6
const sumd = ([Iarch] xs): Iarch -> {
	let Iarch acc = 0
	for x in xs {
		acc = acc + x
	}
	return acc
}
pub const main = () -> {
	let [Iarch] xs = [1, 2, 3]
	return sumd(xs)
}
