# operating on 'T with `+` fails when instantiated with a record
# expect: error
data Box = { Iarch v }
pub const inc = ['T]('T x): 'T -> x + 1
pub const main = () -> {
	const Box b = { v: 1 }
	return inc[Box](b)
}
