# the loop variable cannot collide with an existing local (no shadowing)
# expect: error
pub const main = () -> {
	const Iarch x = 9
	const [3 Iarch] xs = [1, 2, 3]
	for x in xs {
		return x
	}
	return 0
}
