# only one value-level spread is allowed per literal
# expect: error
data Point = { Iarch x, Iarch y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	const Point c = { ...a, ...a }
	return c.x
}
