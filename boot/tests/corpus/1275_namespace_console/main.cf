# A namespace import composes with the std root and the console barrel's reexport: `io.print`
# reaches the darwin console implementation. Exit 0 proves the whole chain compiles and runs.
# expect: exit 0
import "std/io/console" as io
pub const main = (): Iarch -> {
	io.print("namespace console\n")
	return 0
}
