# A namespace import composes with the std root and the console barrel's reexport: `console::print`
# reaches the darwin console implementation. Exit 0 proves the whole chain compiles and runs.
# expect: exit 0
import std::io::console
pub const main = (): Iarch -> {
	console::print("namespace console\n")
	return 0
}
