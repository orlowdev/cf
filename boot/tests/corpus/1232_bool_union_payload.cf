# a `Bool` union payload, recovered by `match` and used in the arm.
# expect: exit 7
union M = { B(Bool), Z }
pub const main = () -> {
	const M m = M.B(true)
	return match m {
		M.B(f) -> if f then 7 else 0,
		M.Z -> 0
	}
}
