# expect: error
# a nested member sub-pattern needs a union-typed payload field
union M = { A(Iarch) }
pub const f = (M m): Iarch -> match m {
	M.A(Foo) -> 1,
	_ -> 0,
}
pub const main = () -> f(M.A(3))
