# a statement-position match must still be exhaustive (cover every member or add `_`)
# expect: error
union K = { A, B, C }
pub const main = () -> {
	const K k = K.A
	match k {
		K.A -> { },
		K.B -> { },
	}
	return 0
}
