# fixed-width arithmetic wraps to the type's width (2's-complement): Uint8 200+100 = 300,
# which wraps to 44 in 8 bits. exit = 44
# expect: exit 44
pub const main = () -> {
	const Uint8 a = 200
	const Uint8 b = 100
	return Iarch(a + b)
}
