# mutate a fixed-width field through a `*C` pointer (sub-word storew/loadw round-trip)
# expect: exit 42
data C = { Uint8 n }
const bump = (*C c): Iarch -> {
	c.n = c.n + Uint8(1)
	return Iarch(c.n)
}
pub const main = () -> {
	let cc = C({ n: 41 })
	return bump(&cc)
}
