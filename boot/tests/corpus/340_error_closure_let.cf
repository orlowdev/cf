# A closure must be bound with `const` (no reassignable closures).
# expect: error
const run = (): Iarch -> {
	let Iarch i = 0
	let f = () -> {
		i = i + 1
		return i
	}
	return i
}
pub const main = () -> run()
