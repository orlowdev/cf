# a `[Uint8]` renders its bytes as NUMBERS (not text): `[72, 105]`, so m[1] is '7' (ASCII 55)
# expect: exit 55
pub const main = () -> {
	let [Uint8] xs = [72, 105]
	const Str m = "${xs}"
	return Iarch(m[1])
}
