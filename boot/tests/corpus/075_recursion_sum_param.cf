# recursive sum 0..n over a parameter (arg count is 4 with three args): 4+3+2+1+0 = 10
# args: a b c
# expect: exit 10
const sum = (Iarch n): Iarch -> if n == 0 then 0 else n + sum(n - 1)

pub const main = ([Str] args) -> sum(Iarch(args.len))
