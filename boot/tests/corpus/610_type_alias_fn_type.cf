# a function-type alias used as a higher-order-function parameter type.
# expect: exit 41
type IntFn = (Iarch) -> Iarch
const apply = (IntFn f, Iarch x): Iarch -> f(x)
const inc = (Iarch n) -> n + 1
const dbl = (Iarch n) -> n * 2
pub const main = () -> apply(inc, 20) + apply(dbl, 10)
