# a `[Int 'T]` bound rejects an unsigned width — Uint8 is not a member of the signed union Int
# expect: error
pub const widen = [Int 'T]('T x): Iarch -> Iarch(x)
pub const main = () -> widen[Uint8](Uint8(5))
