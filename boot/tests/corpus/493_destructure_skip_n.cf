# `_n` drops n positions
# expect: exit 5

const main_body! = (): Iarch -> {
	const (_2, last) = (1, 2, 5)
	return last
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
