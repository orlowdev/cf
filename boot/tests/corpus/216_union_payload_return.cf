# a boxed union crosses function boundaries by pointer (param + return)
# expect: exit 2
union Node = { IntLit(Iarch), Nil }
pub const mk = (Iarch n): Node -> Node.IntLit(n)
pub const tag_of = (Node x): Iarch -> match x {
	Node.IntLit -> 2,
	Node.Nil -> 0,
}
pub const main = () -> tag_of(mk(9))
