# a type that recurses THROUGH an array (a record with a `[Self]` field) is not inline-stringifiable
# expect: error
data N = { Iarch v, [N] kids }
pub const main = () -> {
	const N n = N({ v: 1, kids: [] })
	const Str m = "${n}"
	return m.len
}
