# a `[T]` param is a READ-ONLY borrow — growing it (reassign) is rejected.
# expect: error
data Tok = { Iarch v }
const grow = ([Tok] ts): Iarch -> {
	ts = [...ts, Tok({v: 1})]
	return 0
}
pub const main = () -> 0
