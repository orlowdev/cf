# a `_` arm covers any width without its own arm (here Int32 falls through to `_`)
# expect: exit 99
pub const f = [Int 'T]('T x): Iarch -> match x {
	Int.Int8(v) -> 8,
	_ -> 99,
}
pub const main = () -> f[Int32](Int32(1))
