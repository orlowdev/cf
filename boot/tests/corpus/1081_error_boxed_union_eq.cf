# `==` compares tag-only unions; a payload (boxed) union is recovered with `match`
# expect: error
union N = { Lit(Iarch), Nil }
pub const main = () -> {
	const N a = N.Nil
	const N b = N.Nil
	if a == b then return 1
	return 0
}
