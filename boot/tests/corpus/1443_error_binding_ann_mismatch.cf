# A binding's named annotation is ENFORCED — a `Str`-annotated binding cannot hold an
# `Iarch` value (the annotation was previously decoration on the `type_of` fall-through).
# expect: error
pub const main = (): Iarch -> {
	const Iarch x = 5
	const Str s = x
	return 0
}
