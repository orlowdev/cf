# a by-VALUE record param stays read-only — mutate through a `*Record` instead
# expect: error
data P = { Iarch x }
const f = (P p): Iarch -> {
	p.x = 9
	return p.x
}
pub const main = () -> f(P({ x: 1 }))
