# a TYPE reached through a reexported namespace (module_system §4/§5): `Point` flattens into the
# program whole (types are never mangled), so it is constructed bare; the sibling VALUE `mk` in the
# same nested namespace is reached qualified — `barrel::impl::mk`. Types and values ride the same nested
# namespace, split by casing.
# expect: exit 49
import barrel

pub const main = () -> {
	const Point p = Point({ x: 40, y: 2 })
	return p.x + p.y + barrel::impl::mk()
}
