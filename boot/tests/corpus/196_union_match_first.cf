# a tag-only union: construct a member, match it (first arm)
# expect: exit 10
union Color = { Red, Green, Blue }
pub const main = () -> {
	const Color c = Color.Red
	return match c {
		Color.Red -> 10,
		Color.Green -> 20,
		Color.Blue -> 30,
	}
}
