# A float-annotated binding's value must carry that exact width — a `Float64` binding cannot
# hold a `Float32` value (this previously slipped typecheck into a QBE store-class error).
# expect: error
pub const main = (): Iarch -> {
	const Float32 f = Float32(1.5)
	const Float64 g = f
	return 0
}
