# a `Float64` TUPLE element (previously a rejected "later brick"): stored `stored` into its 8-byte
# slot and read via `t[k]` with `loadd`. 40 + 3 = 43.
# expect: exit 43
pub const main = () -> {
	const t = (40.5, 3)
	return Iarch(t[0]) + t[1]
}
