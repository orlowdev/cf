# a closure captures a fixed-width `Int32` by reference and reads it
# expect: exit 14
const run = (): Iarch -> {
	const Int32 v = Int32(9)
	const f = (Iarch n) -> n + Iarch(v)
	return f(5)
}
pub const main = () -> run()
