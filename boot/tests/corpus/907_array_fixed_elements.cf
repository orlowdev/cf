# a `[N Int32]` array: fixed-width elements, index read + `for` sum
# expect: exit 102
pub const main = () -> {
	const [4 Int32] xs = [10, 20, 30, 42]
	let Iarch acc = 0
	for x in xs {
		acc = acc + Iarch(x)
	}
	return acc
}
