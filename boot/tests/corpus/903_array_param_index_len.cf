# indexing and `.len` on a by-value array parameter, fed an array-returning call
# expect: exit 7
const build = (): [4 Iarch] -> [1, 2, 3, 4]

const at2plus_len = ([4 Iarch] xs): Iarch -> xs[2] + xs.len

pub const main = () -> at2plus_len(build())
