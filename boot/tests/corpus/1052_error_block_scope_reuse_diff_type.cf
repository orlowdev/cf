# a reused sibling-block name must keep its type (both share one stack slot); Iarch then Str is
# rejected.
# expect: error
pub const main = () -> {
	if 1 == 1 then {
		const Iarch v = 5
		return v
	}
	if 1 == 0 then {
		const Str v = "hi"
		return Iarch(v.len)
	}
	return 0
}
