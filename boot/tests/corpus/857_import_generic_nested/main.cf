# a nested generic application Box[Box[Iarch]] over an imported generic type
# expect: exit 5
import boxfn::{ Box }
const mkinner = (Iarch n): Box[Iarch] -> ({ v: n })
pub const main = () -> {
	const Box[Box[Iarch]] outer = { v: mkinner(5) }
	return outer.v.v
}
