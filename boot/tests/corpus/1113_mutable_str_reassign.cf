# A mutable `let Str` repoints to a fresh Str (its cell holds the header pointer). The new
# string's length is read back after the reassignment.
# expect: exit 5
pub const main = () -> {
	let Str s = "ab"
	s = "hello"
	return Iarch(s.len)
}
