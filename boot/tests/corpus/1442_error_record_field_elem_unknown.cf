# A record's `[Elem]` field element must name a known type at the declaration — an unused
# `[Nope]` field is rejected like a plain `Nope` field.
# expect: error
data R = { [Nope] xs }

pub const main = (): Iarch -> 0
