# an array tuple element answers `.len` directly (`t[0].len`, the header length at +8). 4 + 8 = 12.
# expect: exit 12
pub const main = () -> {
	const t = ([1, 2, 3, 4], 8)
	return Iarch(t[0].len) + t[1]
}
