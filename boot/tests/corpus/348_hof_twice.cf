# The function-value parameter may be applied more than once: twice(inc, 5) = inc(inc(5)).
# expect: exit 7
const twice = ((Iarch) -> Iarch f, Iarch x): Iarch -> f(f(x))
const inc = (Iarch n) -> n + 1
pub const main = () -> twice(inc, 5)
