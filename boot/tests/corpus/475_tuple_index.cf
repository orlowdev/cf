# a tuple is indexed at comptime literal positions
# expect: exit 40

const main_body! = (): Iarch -> {
	const t = (10, 20, 30)
	return t[0] + t[2]
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
