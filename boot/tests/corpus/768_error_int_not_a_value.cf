# `Int` is a numeric union BOUND (§8.6), not a value type — using it as a concrete type errors
# (a bare integer is `Iarch`; a width is a concrete type). This pins `Int` as bound-only.
# expect: error
pub const main = () -> {
	const Int x = 5
	return Iarch(x)
}
