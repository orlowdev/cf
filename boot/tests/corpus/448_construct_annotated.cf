# explicit construction in an annotated binding
# expect: exit 7
data Point = { Iarch x, Iarch y }

const main_body! = (): Iarch -> {
	const Point p = Point({ x: 3, y: 4 })
	return p.x + p.y
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
