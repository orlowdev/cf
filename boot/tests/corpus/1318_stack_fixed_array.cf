# A `$[N T]` fixed-size array lives on the stack, indexed like any array, reclaimed at frame exit.
# expect: exit 60
const work = (): Iarch -> {
	const [3 Iarch] $xs = [10, 20, 30]
	return $xs[0] + $xs[1] + $xs[2]
}
pub const main = () -> work()
