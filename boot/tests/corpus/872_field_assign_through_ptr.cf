# mutate a record field through a `*Record` pointer param (a writable borrow)
# expect: exit 6
data P = { Iarch x }
const bump = (*P p): Iarch -> {
	p.x = p.x + 1
	return p.x
}
pub const main = () -> {
	let q = P({ x: 5 })
	return bump(&q)
}
