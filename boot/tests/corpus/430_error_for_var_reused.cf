# a loop variable is function-scoped in cfcc — reusing it across sibling `for`s collides
# expect: error
pub const main = () -> {
	const [2 Iarch] xs = [1, 2]
	const [2 Iarch] ys = [3, 4]
	for i in xs {
		return i
	}
	for i in ys {
		return i
	}
	return 0
}
