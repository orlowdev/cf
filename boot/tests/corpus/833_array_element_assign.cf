# element assignment mutates a `let` array in place
# expect: exit 24

const main_body! = (): Iarch -> {
	let [3 Iarch] xs = [1, 2, 3]
	xs[1] = 20
	return xs[0] + xs[1] + xs[2]
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
