# a trailing comma in the pattern is allowed
# expect: exit 7

# check: root
const main_body! = (): Iarch -> {
	const (a, b,) = (3, 4)
	return a + b
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
