# A closure body may call an ordinary top-level function, passing a captured value.
# expect: exit 36
const square = (Iarch x) -> x * x
const run = (): Iarch -> {
	let Iarch n = 6
	const f = () -> square(n)
	return f()
}
pub const main = () -> run()
