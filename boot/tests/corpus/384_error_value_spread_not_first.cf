# a value-level spread must be the first entry
# expect: error
data Point = { Iarch x, Iarch y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	const Point b = { x: 9, ...a }
	return b.x
}
