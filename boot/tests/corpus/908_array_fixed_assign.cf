# element assignment into a `let [N Int16]` array stores at the element width
# expect: exit 54
pub const main = () -> {
	let [3 Int16] xs = [1, 2, 3]
	xs[1] = Int16(50)
	return Iarch(xs[0]) + Iarch(xs[1]) + Iarch(xs[2])
}
