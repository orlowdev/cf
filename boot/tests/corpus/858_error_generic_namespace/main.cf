# a generic type reached through a TYPE NAMESPACE is a deferred combo — must reject, not crash
# expect: error
import "box" as Bx
pub const main = () -> {
	const Bx.Box[Iarch] b = { v: 1 }
	return b.v
}
