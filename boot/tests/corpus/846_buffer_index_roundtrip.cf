# write two bytes into a `[N Uint8]` buffer and read them back (Uint8 element, loadub/storeb)
# expect: exit 177
pub const main = () -> {
	let [4 Uint8] buf
	buf[0] = 72
	buf[1] = 105
	return Iarch(buf[0]) + Iarch(buf[1])
}
