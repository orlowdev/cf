# a string match whose arms yield a Str (mergeable), then read its length
# expect: exit 3
pub const kind = (Str s): Str -> match s {
	"i" -> "int",
	"f" -> "float",
	_ -> "?",
}
pub const main = () -> kind("i").len
