# a record position binds `const` only — a `let` would alias the tuple's storage mutably
# expect: error
data P = { Iarch x }
const mk = (Iarch a): P -> P({ x: a })
pub const main = () -> {
	let (n, p) = (1, mk(2))
	return n
}
