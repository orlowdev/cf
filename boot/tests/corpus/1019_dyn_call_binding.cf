# `const [T] xs = f()` adopts a `[T]`-returning call's fresh header pointer, then indexes it.
# expect: exit 9
const nums = (): [Iarch] -> {
	let [Iarch] xs = []
	xs = [...xs, 4]
	xs = [...xs, 5]
	return xs
}
pub const main = () -> {
	const [Iarch] xs = nums()
	return xs[0] + xs[1]
}
