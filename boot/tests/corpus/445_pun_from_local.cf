# a pun reads a local binding, not only a parameter
# expect: exit 10
data Point = { Iarch x, Iarch y }
const mk = (Iarch a): Point -> {
	const Iarch x = a + 1
	const Iarch y = a * 2
	return ({ x, y })
}
pub const main = () -> {
	const Point p = mk(3)
	return p.x + p.y
}
