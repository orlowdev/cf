# a string CONSTRUCTED at runtime (interpolation) is residue that dies in the frame, so the builder
# must be marked `!`.
# check: alloc
# expect: exit 3
const greet! = (Iarch n): Iarch -> {
	const Str s = "n=${n}"
	return Iarch(s.len)
}
pub const main = () -> greet!(1)
