# a statement-position `match` on a Bool dispatches on its tag (False=0/True=1) for effect
# expect: exit 9
pub const main = () -> {
	const Bool b = 3 > 5
	let Iarch r = 0
	match b {
		Bool.True -> { r = 1 },
		Bool.False -> { r = 9 },
	}
	return r
}
