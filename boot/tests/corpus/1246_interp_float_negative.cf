# a negative float leads with `-`: `-0.25`, so m[0] is ASCII 45
# expect: exit 45

const main_body! = (): Iarch -> {
	let Float64 x = -0.25
	const Str m = "${x}"
	return Iarch(m[0])
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
