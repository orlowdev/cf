# a function type may take a `Str` argument: `(Str) -> Iarch`. `applys(len1, "hello")` = 5.
# expect: exit 5
const len1 = (Str s): Iarch -> s.len
const applys = ((Str) -> Iarch g, Str t): Iarch -> g(t)
pub const main = () -> applys(len1, "hello")
