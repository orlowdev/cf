# an opaque `*[Str]` param is NOT indexable — only `*[Uint8]` byte pointers are.
# indexed in a helper (not `main`, whose return annotation would error for a different reason).
# expect: error
const first = (*[Str] argv): Iarch -> Iarch(argv[0])
pub const main = () -> 0
