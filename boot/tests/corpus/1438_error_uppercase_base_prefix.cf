# A base prefix is lowercase-only (ebnf § Numbers): `0XFF` is a spelling error the lexer rejects
# outright — not a `0` followed by an `XFF` identifier.
# expect: error
pub const main = () -> {
	const x = 0XFF
	return x
}
