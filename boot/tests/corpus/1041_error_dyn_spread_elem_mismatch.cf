# an appended element in a `[...src, e]` spread must match the source's element type.
# expect: error
pub const main = () -> {
	const [Iarch] xs = [1, 2]
	const [Iarch] two = [...xs, "hi"]
	return 0
}
