# a nested tuple `((1, 2), 3)` renders recursively (11 bytes) — the stringifier descends positions
# expect: exit 11
pub const main = () -> {
	const t = ((1, 2), 3)
	const Str m = "${t}"
	return m.len
}
