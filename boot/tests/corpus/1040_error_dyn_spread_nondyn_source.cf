# a `[...src, e]` spread source must be a `[T]` dynamic array — a `[N T]` fixed array is rejected.
# expect: error
pub const main = () -> {
	const [3 Iarch] fx = [1, 2, 3]
	const [Iarch] two = [...fx, 4]
	return 0
}
