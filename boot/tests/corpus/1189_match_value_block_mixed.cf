# An expression `match` may mix bare-expression arms and value-block arms freely.
# expect: exit 42
union M = { A, B, C }
pub const main = () -> {
	let M m = M.B
	let Iarch x = match m {
		M.A -> 1,
		M.B -> {
			let Iarch t = 40
			<- t + 2
		},
		M.C -> 3
	}
	return x
}
