# a value can be passed as a call argument (arg count is 4 with three args)
# args: a b c
# expect: exit 8
const twice = (Iarch x): Iarch -> x + x

pub const main = ([Str] args) -> twice(Iarch(args.len))
