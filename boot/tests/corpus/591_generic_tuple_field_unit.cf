# the `'T` tuple element may be instantiated at `Unit`
# expect: exit 12
# check: root
data Box['T] = { (Iarch, 'T) p }

const main_body! = (): Iarch -> {
	const Box[Unit] b = { p: (12, ()) }
	return b.p[0]
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
