# a record whose only entry is a spread is valid — it inherits every field
# expect: exit 30
data Point = { Iarch x, Iarch y }
data Clone = { ...Point }

const main_body! = (): Iarch -> {
	const Clone c = { x: 10, y: 20 }
	return c.x + c.y
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
