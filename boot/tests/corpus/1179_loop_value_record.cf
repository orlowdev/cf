# a value-yielding `loop` may now yield a RECORD (an 8-byte arena pointer through the merge slot);
# binding it then field-accessing the result dispatches
# expect: exit 7
data Bag = { Iarch v }
pub const main = () -> {
	let Bag r = loop {
		<- Bag({ v: 7 })
	}
	return r.v
}
