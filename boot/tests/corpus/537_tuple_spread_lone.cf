# a lone `(...t)` stays a tuple — a copy of t (not grouping)
# expect: exit 3

import std::mem::arena::growing_arena
const main_body! = (): Iarch -> {
	const t = (1, 2)
	const u = (...t)
	return u[0] + u[1]
}

pub const main = () -> {
	const a = growing_arena::of(4096)
	return main_body!() in a
}
