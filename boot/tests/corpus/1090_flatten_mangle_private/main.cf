# two modules each define a PRIVATE `helper` with the same name — the flatten mangles them
# apart per module, so each module's own `helper` is called. 40 + 2 = 42.
# expect: exit 42
import a::{ fa }
import b::{ fb }

pub const main = () -> fa() + fb()
