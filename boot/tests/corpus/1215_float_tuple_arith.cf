# two `Float64` tuple positions read and combined in float arithmetic. 1.5 + 2.75 = 4.25 -> 4.
# expect: exit 4

const main_body! = (): Iarch -> {
	const t = (1.5, 2.75)
	return Iarch(t[0] + t[1])
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
