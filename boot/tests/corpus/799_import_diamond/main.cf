# a diamond: main imports a and b, both of which import c. c is loaded once (deduped by
# canonical path), so its `base` is a single mangled function. 20 + 22 = 42.
# expect: exit 42
import "a" as { av }
import "b" as { bv }

pub const main = () -> av() + bv()
