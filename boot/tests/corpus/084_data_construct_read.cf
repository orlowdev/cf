# a record is declared, constructed with a data literal, and read by field
# expect: exit 7
data Point = { Iarch x, Iarch y }
pub const main = () -> {
	const Point p = { x: 3, y: 4 }
	return p.x + p.y
}
