# a bounded 'T is not directly operable — arithmetic on it (without match) is a type error
# expect: error
union Shape = { Circle(Iarch), Square(Iarch) }
pub const bad = [Shape 'T]('T s): Iarch -> s + 1
pub const main = () -> {
	return bad[Shape](Shape.Circle(3))
}
