# a tuple mixing an Iarch and a Str; the Str position answers `.len`
# expect: exit 8

import std::mem::arena::growing_arena
const main_body! = (): Iarch -> {
	const t = (3, "hello")
	return t[0] + t[1].len
}

pub const main = () -> {
	const a = growing_arena::of(4096)
	return main_body!() in a
}
