# a record's tuple field can be destructured directly
# expect: exit 30
data R = { (Iarch, Iarch) p }

const main_body! = (): Iarch -> {
	const r = R({ p: (10, 20) })
	const (a, b) = r.p
	return a + b
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
