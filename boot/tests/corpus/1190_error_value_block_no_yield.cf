# A block used as a value must reach a `<- v`; a branch block that never yields is rejected.
# expect: error
pub const main = () -> {
	let Iarch x = if true then { let Iarch y = 5 } else { <- 3 }
	return x
}
