# expect: error
# a value const may not begin with `(` (that opens a function's parameter list) —
# a disclaimed cfcc narrowing; write the value without the leading parens
const paren = (1 + 2)
pub const main = () -> paren
