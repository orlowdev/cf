# a generic type applied with the wrong number of type arguments
# expect: error
union Maybe['V] = { Just('V), Nothing }
pub const main = () -> {
	const Maybe[Iarch, Iarch] m = Maybe[Iarch, Iarch].Nothing
	return 0
}
