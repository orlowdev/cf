# `for … in` over an array of records is a later brick (index with xs[i])
# expect: error
data P = { Iarch x }
const mk = (Iarch a): P -> ({ x: a })
pub const main = () -> {
	const [2 P] ps = [mk(1), mk(2)]
	let Iarch t = 0
	for p in ps { t = t + p.x }
	return t
}
