# expect: error
# in a module with a destructured import (which defers bare-name resolution to
# typecheck), a self-referential const must still be rejected — not silently
# compiled to infinite self-recursion
import dep::{ k }
const spin = spin
pub const main = () -> k + spin
