# a union payload may be a `Str` (first-class Str); match binds it
# expect: exit 5
union Node = { Wrap(Str), Nil }
const f = (Node n): Iarch -> match n {
	Node.Wrap(s) -> s.len,
	Node.Nil -> 0
}
pub const main = () -> f(Node.Wrap("hello"))
