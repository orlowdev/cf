# traversing a reexported namespace to a member it does not export is rejected: `io` (impl) has
# `thing`, not `missing`, so the nested path `barrel::impl::missing` resolves to nothing.
# expect: error
import barrel

pub const main = () -> barrel::impl::missing()
