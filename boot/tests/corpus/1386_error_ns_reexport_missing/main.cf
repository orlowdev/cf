# traversing a reexported namespace to a member it does not export is rejected: `io` (impl) has
# `thing`, not `missing`, so the nested path `foo.io.missing` resolves to nothing.
# expect: error
import "barrel" as foo

pub const main = () -> foo.io.missing()
