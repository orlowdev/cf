# a mutation's value may read the record's current fields
# expect: exit 40
import "std/mem/arena/fixed_arena" as fixed_arena
data Point = { Iarch x, Iarch y }

const main_body! = (): Iarch -> {
	let Point p = { x: 5, y: 0 }
	p.y = p.x * 8
	return p.y
}

pub const main = () -> {
	const a = fixed_arena.of(4096)
	return main_body!() in a
}
