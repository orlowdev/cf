# two destructurings in a row reuse the machinery without name clashes
# expect: exit 100

# check: root
const main_body! = (): Iarch -> {
	const (a, b) = (10, 20)
	const (c, d) = (30, 40)
	return a + b + c + d
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
