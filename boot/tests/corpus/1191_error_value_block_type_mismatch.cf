# The value blocks of a `match`'s arms must all yield the same type (here Iarch vs Str).
# expect: error
union M = { A, B }
pub const main = () -> {
	let M m = M.A
	let Iarch x = match m {
		M.A -> { <- 1 },
		M.B -> { <- "no" }
	}
	return x
}
