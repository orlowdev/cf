# a value-position block arm with no `<- v` is rejected — a block used as a value must yield
# expect: error
union K = { A, B }
pub const main = () -> {
	const K k = K.A
	const Iarch r = match k {
		K.A -> { const Iarch x = 5 },
		K.B -> 2,
	}
	return r
}
