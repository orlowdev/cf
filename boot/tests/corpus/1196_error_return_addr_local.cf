# region-outlives: returning `&local` hands the caller a pointer into a residence that dies when
# this call returns (memory_model §6) — rejected.
# expect: error
data Point = { Iarch x }
const leak = (): Point -> {
	let Point local = { x: 7 }
	return &local
}
pub const main = () -> leak().x
