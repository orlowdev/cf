# a pointer/buffer capture has no addressable value slot to borrow — still a later brick
# expect: error
const run = (*[Uint8] buf): Iarch -> {
	const f = (Iarch i) -> Iarch(buf[i])
	return f(0)
}
pub const main = () -> 0
