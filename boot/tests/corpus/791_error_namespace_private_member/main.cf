# a module-private function is not reachable through the value namespace — the
# qualified call stays unresolved and is rejected at typecheck
# expect: error
import math

pub const main = () -> math::secret()
