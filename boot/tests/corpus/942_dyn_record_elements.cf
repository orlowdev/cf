# record elements (8-byte arena pointers) in a dynamic array
# expect: exit 23
data P = { Iarch x, Iarch y }
pub const mk = (Iarch a, Iarch b): P -> P({ x: a, y: b })
pub const main = () -> {
	let [P] ps = []
	ps = [...ps, mk(3, 4)]
	ps = [...ps, mk(10, 20)]
	return ps[0].x + ps[1].y
}
