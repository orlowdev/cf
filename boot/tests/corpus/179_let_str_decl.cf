# a `let Str` local is declarable and reassignable (no longer const-only) — declare `s`,
# reassign it, and read the new length
# expect: exit 5
pub const main = () -> {
	let Str s = "x"
	s = "hello"
	return s.len
}
