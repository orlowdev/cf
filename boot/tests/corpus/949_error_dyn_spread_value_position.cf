# a `[...src, e]` spread is only valid as growth, not in a value/return position
# expect: error
pub const build = (): Iarch -> {
	let [Iarch] xs = [1, 2]
	return [...xs, 3]
}
pub const main = () -> build()
