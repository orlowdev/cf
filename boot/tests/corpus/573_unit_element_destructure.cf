# destructuring binds a unit position like any other
# expect: exit 6

const main_body! = (): Iarch -> {
	const (a, u) = (6, ())
	return a
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
