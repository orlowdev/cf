# itoa of 0 -> "0"; m[0] is '0' (ASCII 48)
# expect: exit 48

const main_body! = (): Iarch -> {
	let Iarch z = 0
	const Str m = "${z}"
	return Iarch(m[0])
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
