# an EXPRESSION match arm naming a nonexistent union member is rejected, not crashed
# expect: error
union U = { A, B(Iarch) }

pub const main = (): Iarch -> {
	const U u = U.A

	return match u { U.Nope(x) -> x, _ -> 0 }
}
