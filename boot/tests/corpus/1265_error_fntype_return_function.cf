# A function type cannot RETURN a function (currying): the result would be a bare code pointer with
# no first-class binding/apply form, so it is rejected — a later brick.
# expect: error
const apply = ((Iarch) -> (Iarch) -> Iarch f, Iarch x): Iarch -> 0
pub const main = () -> 0
