# compound assignment reaches a DEEP field target: `p.a.v += 9` reads-then-writes through the chain
# expect: exit 10
data V = { Iarch v }
data P = { V a }
pub const main = () -> {
	let P p = { a: { v: 1 } }
	p.a.v += 9
	return p.a.v
}
