# a module-private type is not reachable through the type namespace — the qualified
# name stays unresolved and is rejected
# expect: error
import shapes

pub const main = () -> {
	const shapes::Secret s = shapes::Secret({ s: 1 })
	return s.s
}
