# a heterogeneous (Iarch, Str) return, destructured into an Iarch and a Str
# expect: exit 8
const tag = (Iarch n): (Iarch, Str) -> (n, "hey")
pub const main = () -> {
	const (n, s) = tag(5)
	return n + s.len
}
