# an array tuple element must be a fresh array literal or call result, not an aliased binding — rejected.
# expect: error
pub const main = () -> {
	const [Iarch] xs = [1, 2]
	const t = (xs, 3)
	return 0
}
