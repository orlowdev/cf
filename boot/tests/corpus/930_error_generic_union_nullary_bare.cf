# a NULLARY generic union member has no payload to infer from, so it still needs explicit
# type args (`Maybe[Iarch].Nothing`, not bare `Maybe.Nothing`) — a clean genesis narrowing
# expect: error
union Maybe['T] = { Just('T), Nothing }
pub const main = () -> {
	const Maybe[Iarch] m = Maybe.Nothing
	return 0
}
