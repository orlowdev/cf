# both-ways check: a `!` marker on a colorless function (pure scalar arithmetic, no residue) is a
# compile error — the effect is exact.
# check: alloc
# expect: error
const nope! = (Iarch x): Iarch -> x + x
pub const main = () -> nope!(3)
