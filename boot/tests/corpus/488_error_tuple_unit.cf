# `main` yields the process exit code, an Iarch — the unit value `()` is not an Iarch,
# so returning it from `main` is rejected (though `()` is a first-class value elsewhere now)
# expect: error
pub const main = () -> {
	return ()
}
