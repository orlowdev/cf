# a pointer (to a record) cannot be used in an integer expression (on a callee reached from main)
# expect: error
data R = { Iarch a }
const addp = (*R p): Iarch -> p + 1

pub const main = () -> {
	let R r = { a: 0 }
	return addp(&r)
}
