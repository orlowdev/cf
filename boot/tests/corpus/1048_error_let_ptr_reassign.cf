# a pointer `let` local cannot be reassigned (repointing is forbidden — const pointer semantics).
# expect: error
data P = { Iarch x }
const bump = (*P q): Iarch -> {
	let *P r = q
	r = q
	return r.x
}
pub const main = () -> 0
