# a closure captures a `Float64` by reference and reads it
# expect: exit 8
const run = (): Iarch -> {
	const Float64 x = 3.5
	const f = (Iarch n) -> n + Iarch(x)
	return f(5)
}
pub const main = () -> run()
