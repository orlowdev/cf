# match the last member — exercises the whole compare-chain ladder
# expect: exit 30
union Color = { Red, Green, Blue }
pub const main = () -> {
	const Color c = Color.Blue
	return match c {
		Color.Red -> 10,
		Color.Green -> 20,
		Color.Blue -> 30,
	}
}
