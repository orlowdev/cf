# a union member may carry a tuple payload; construct, match, and index it
# expect: exit 30
union B = { Wrap((Iarch, Iarch)), Empty }
pub const main = () -> {
	const B b = B.Wrap((10, 20))
	return match b {
		B.Wrap(p) -> p[0] + p[1],
		B.Empty -> 0,
	}
}
