# signed 8-bit arithmetic wraps: Int8 100+50 = 150, which as a signed byte is -106; the
# process exit takes it modulo 256 = 150
# expect: exit 150
pub const main = () -> {
	const Int8 a = 100
	return Iarch(a + Int8(50))
}
