# a `Uarch` record field
# expect: exit 7
import "std/mem/arena/fixed_arena" as fixed_arena
data Box = { Uarch n }

const main_body! = (): Iarch -> {
	const b = Box({ n: Uarch(7) })
	return Iarch(b.n)
}

pub const main = () -> {
	const a = fixed_arena.of(4096)
	return main_body!() in a
}
