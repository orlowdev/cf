# a `Bool` record field is a word slot; read it back and branch on it
# expect: exit 5
data Flag = { Bool on, Iarch n }
pub const mk = (): Flag -> Flag({ on: true, n: 5 })
pub const main = () -> {
	const Flag f = mk()
	return if f.on then f.n else 0
}
