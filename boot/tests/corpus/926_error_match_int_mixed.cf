# an integer match cannot mix a union-member arm with integer literals
# expect: error
union Color = { Red, Green }
const f = (Iarch n): Iarch -> match n { 0 -> 1, Color.Red -> 2, _ -> 3 }
pub const main = () -> f(0)
