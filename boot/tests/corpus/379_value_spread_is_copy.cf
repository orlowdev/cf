# the spread is a genuine copy — mutating the copy leaves the source untouched
# expect: exit 101
data Point = { Iarch x, Iarch y }
pub const main = () -> {
	const Point a = { x: 1, y: 2 }
	let Point b = { ...a }
	b.x = 100
	return a.x + b.x
}
