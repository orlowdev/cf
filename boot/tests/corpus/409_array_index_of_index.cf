# an element value can itself index the array
# expect: exit 2

const main_body! = (): Iarch -> {
	const [3 Iarch] xs = [1, 2, 0]
	return xs[xs[0]]
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
