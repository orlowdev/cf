# expect: error
# a top-level const value must be a scalar or Str — an aggregate is a later brick
data Point = { Iarch x, Iarch y }
const origin = Point({ x: 0, y: 0 })
pub const main = () -> origin.x
