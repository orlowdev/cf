# a record body may carry a trailing comma
# expect: exit 42
data Point = { Iarch x, Iarch y, }

const main_body! = (): Iarch -> {
	const Point p = { x: 40, y: 2 }
	return p.x + p.y
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
