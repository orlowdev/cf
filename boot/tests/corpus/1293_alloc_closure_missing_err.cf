# both-ways: a closure that traps residue colors its enclosing function, so the missing marker is an
# error.
# expect: error
data P = { Iarch v }
const run = (): Iarch -> {
	const mk = (Iarch x): Iarch -> {
		const P p = { v: x }
		return p.v
	}
	return mk(5)
}
pub const main = () -> run()
