# two guarded yields: the first guard that holds wins (i reaches 3 -> yields 30)
# expect: exit 30
pub const main = () -> {
	let i = 0
	let r = loop {
		if i == 3 then <- 30
		if i == 5 then <- 50
		i = i + 1
	}
	return r
}
