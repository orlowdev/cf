# destructuring binds a nested-tuple position by name
# expect: exit 8

# check: root
const main_body! = (): Iarch -> {
	const (pair, z) = ((3, 5), 0)
	return pair[0] + pair[1]
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
