# Str as a first-class param and return type (an `l` header pointer flows through functions)
# expect: exit 4
const echo2 = (Str s): Str -> s
const length = (Str s): Iarch -> s.len
pub const main = () -> length(echo2("hey!"))
