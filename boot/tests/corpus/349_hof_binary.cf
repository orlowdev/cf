# A binary function value `(Iarch, Iarch) Iarch` folded over three arguments.
# expect: exit 42
const fold2 = ((Iarch, Iarch) -> Iarch f, Iarch a, Iarch b, Iarch c): Iarch -> f(f(a, b), c)
const add = (Iarch x, Iarch y) -> x + y
pub const main = () -> fold2(add, 10, 20, 12)
