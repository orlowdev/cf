# a heterogeneous (Iarch, Str) record field
# expect: exit 8
data R = { (Iarch, Str) t }

const main_body! = (): Iarch -> {
	const r = R({ t: (5, "abc") })
	return r.t[0] + r.t[1].len
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
