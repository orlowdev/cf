# the constructed type must match the return type
# expect: error
data Point = { Iarch x, Iarch y }
data Vec = { Iarch x, Iarch y }
const f = (): Vec -> Point({ x: 1, y: 2 })
pub const main = () -> 0
