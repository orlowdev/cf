# Bool false -> "false", length 5
# expect: exit 5

# check: root
const main_body! = (): Iarch -> {
	let Bool f = false
	const Str m = "${f}"
	return m.len
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
