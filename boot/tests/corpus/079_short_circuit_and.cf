# `&&` short-circuits: a False left operand skips the right, so boom (infinite
# recursion, which would stack-overflow) is never called and this yields False
# expect: exit 0
const boom = (Iarch n): Bool -> boom(n)
pub const main = () -> if false && boom(1) then 1 else 0
