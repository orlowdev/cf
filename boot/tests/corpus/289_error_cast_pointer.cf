# a numeric cast takes a scalar — casting a pointer (`*[Uint8]`) to an integer is
# rejected (type_system §4: no pointer<->integer conversion).
# expect: error
pub const main = () -> {
	return Iarch("hi".bytes)
}
