# a FIXED array `[N T]` cannot be grown in place — `fx = [...fx, e]` spreads from a fixed source,
# which is rejected (only a dynamic `[T]` may be copy-appended or grown).
# expect: error
pub const main = () -> {
	let [3 Iarch] fx = [1, 2, 3]
	fx = [...fx, 4]
	return 0
}
