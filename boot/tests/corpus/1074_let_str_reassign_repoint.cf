# reassigning a `let Str` REPOINTS its slot — a prior alias keeps the old header intact
# (t sees "AB" length 2, s repointed to "WXYZ" length 4)
# expect: exit 6
pub const main = () -> {
	let Str s = "AB"
	const Str t = s
	s = "WXYZ"
	return t.len + s.len
}
