# bind a pointer field to a local, then mutate through the borrow
# expect: exit 99
data Inner = { Iarch v }
data Outer = { *Inner p }
pub const main = () -> {
	let Inner i = { v: 9 }
	let Outer o = { p: &i }
	const q = o.p
	q.v = 99
	return i.v
}
