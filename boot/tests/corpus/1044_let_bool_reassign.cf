# a tag-only union (`Bool`) `let` local can be REASSIGNED (word slot). `done` flips false→true;
# the `if` then observes it. Exit 7.
# expect: exit 7
pub const main = () -> {
	let Bool done = false
	done = true
	if done then return 7
	return 0
}
