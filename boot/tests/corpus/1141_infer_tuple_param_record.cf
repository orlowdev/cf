# A `'T` tuple-parameter position is inferred at a RECORD argument and field-accessed through it.
# expect: exit 5
data P = { Iarch x }
const snd = ['T]((Iarch, 'T) p): 'T -> p[1]
pub const main = () -> {
	let P q = snd((9, P({ x: 5 })))
	return q.x
}
