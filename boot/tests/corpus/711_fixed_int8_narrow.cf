# narrowing to Int8 truncates: Int8(200) is the signed byte -56; the process exit code
# takes that value modulo 256, i.e. 200. Exercises a fixed-width parameter and return.
# expect: exit 200
pub const conv = (Iarch n): Int8 -> Int8(n)
pub const main = () -> Iarch(conv(200))
