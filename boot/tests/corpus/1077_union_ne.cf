# `!=` on a tag-only union — k (C) != A and != B, falls to 30
# expect: exit 30
union K = { A, B, C }
const classify = (K k): Iarch -> {
	if k == K.A then return 10
	if k == K.B then return 20
	return 30
}
pub const main = () -> classify(K.C)
