# importing the same module under two statements loads it once (dedup by prefix)
# and binds names from each — 40 + 2 = 42
# expect: exit 42
import "a" as { one }
import "a" as { two }

pub const main = () -> one() + two()
