# a valueless `return` is sugar for `return ()` — the unit value (type_system §6.1)
# expect: exit 5
const noop = (Iarch x): Unit -> {
	return
}
pub const main = () -> {
	const u = noop(1)
	return 5
}
