# a record assembled via spread is an ordinary record — passes through a function
# expect: exit 6
data Point = { Iarch x, Iarch y }
data Labeled = { ...Point, Iarch z }
const total = (Labeled p): Iarch -> p.x + p.y + p.z
pub const main = () -> {
	const Labeled p = { x: 1, y: 2, z: 3 }
	return total(p)
}
