# a comptime condition must read `os.target`/`arch.target` — an unknown namespace is rejected
# expect: error
import comptime::{ os, Os }
if platform.target == Os.Darwin then import "a" as { v }
else import "b" as { v }
pub const main = () -> 0
