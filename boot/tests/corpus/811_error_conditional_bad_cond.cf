# a comptime condition must read `os::target`/`arch::target` — an unknown namespace is rejected
# expect: error
import std::comptime::os
import std::comptime::arch
import std::sys::{ Os }

if platform::target == Os.Darwin then import a::{ v }
else import b::{ v }
pub const main = () -> 0
