# a tag-only union value is a word, so binding it to another const is a plain copy
# expect: exit 20
union Color = { Red, Green, Blue }
pub const main = () -> {
	const Color a = Color.Green
	const Color b = a
	return match b {
		Color.Red -> 10,
		Color.Green -> 20,
		Color.Blue -> 30,
	}
}
