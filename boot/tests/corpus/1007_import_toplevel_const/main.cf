# a destructured import brings top-level value consts into scope as bare names;
# flatten renames them across the module boundary and each auto-calls its nullary
# const function at the reference site
# expect: exit 51
import consts::{ answer, tab }
pub const main = () -> answer + tab
