# `main([Str] args)` — the freestanding _start builds a real cf `[Str]` from argc/argv;
# `args.len` counts argv[0] plus the three args = 4
# args: a b c
# expect: exit 4
pub const main = ([Str] args) -> Iarch(args.len)
