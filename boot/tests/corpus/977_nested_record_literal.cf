# an inline nested record literal — the field type guides the bare `{ … }`
# expect: exit 43
data V = { Iarch v }
data Pair = { V a, V b }
pub const main = () -> {
	const Pair p = { a: { v: 3 }, b: { v: 40 } }
	return p.a.v + p.b.v
}
