# a tuple parameter carrying a record element; index it and read fields
# expect: exit 25
data P = { Iarch x, Iarch y }
const mk = (Iarch a, Iarch b): P -> P({ x: a, y: b })
const sum = ((Iarch, P) t): Iarch -> t[0] + t[1].x + t[1].y
pub const main = () -> sum((5, mk(8, 12)))
