# a match arm may yield a Float64 (arms merge through an 8-byte slot)
# expect: exit 3
union Sel = { A, B }
pub const pick = (Sel s): Float64 -> match s {
	Sel.A -> 1.5,
	Sel.B -> 3.5,
}
pub const main = () -> Iarch(pick(Sel.B))
