# a string LITERAL is static read-only data, not residue — binding one allocates nothing and does
# not color the function.
# check: alloc
# expect: exit 5
const lit = (): Iarch -> {
	let s = "hello"
	return Iarch(s.len)
}
pub const main = () -> lit()
