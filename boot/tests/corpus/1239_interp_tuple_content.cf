# the rendered `(1, true)` has the Bool's `t` at index 4 (ASCII 116) — content, not just length
# expect: exit 116

const main_body! = (): Iarch -> {
	const t = (1, true)
	const Str m = "${t}"
	return Iarch(m[4])
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
