# a type crosses two module hops: b defines the record, a imports it and builds it,
# main imports both the builder (from a) and the type (from b) to name the local.
# expect: exit 42
import a::{ mk }
import b::{ Point }

pub const main = () -> {
	const Point p = mk()
	return p.x + p.y
}
