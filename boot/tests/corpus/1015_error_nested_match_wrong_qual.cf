# expect: error
# a nested member qualifier must name the FIELD's union, not another union
union Op   = { Add, Sub }
union Expr = { Bin(Op, Iarch, Iarch) }
pub const f = (Expr e): Iarch -> match e {
	Expr.Bin(Expr.Add, l, r) -> l,
	_ -> 0,
}
pub const main = () -> f(Expr.Bin(Op.Add, 1, 2))
