# a function may build a tuple local and return it (arena-backed, escapes the frame)
# expect: exit 12
const pair = (Iarch n): (Iarch, Iarch) -> {
	const t = (n, n * 2)
	return t
}
pub const main = () -> {
	const (a, b) = pair(4)
	return a + b
}
