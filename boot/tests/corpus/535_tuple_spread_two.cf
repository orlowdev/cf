# two spreads concatenate both tuples
# expect: exit 6

# check: root
const main_body! = (): Iarch -> {
	const a = (1, 2)
	const b = (3, 0)
	const c = (...a, ...b)
	return c[0] + c[1] + c[2] + c[3]
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
