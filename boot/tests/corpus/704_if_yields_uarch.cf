# an if-expression may yield a Uarch (a 64-bit value through the merge slot)
# expect: exit 5
pub const pick = (Iarch c): Uarch -> if c != 0 then Uarch(5) else Uarch(9)
pub const main = () -> Iarch(pick(1))
