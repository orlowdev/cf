# a local binding of the same name SHADOWS a comptime value parameter (inner binding wins) —
# the value param must NOT be folded over the local
# expect: exit 5
pub const val = [Iarch n](): Iarch -> {
	let Iarch n = 5
	return n
}
pub const main = (): Iarch -> val[8]()
