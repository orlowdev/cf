# a local computed from a value (arg count is 3 with two args)
# args: a b
# expect: exit 6
pub const main = ([Str] args) -> {
	const argc = Iarch(args.len)
	const doubled = argc * 2
	return doubled
}
