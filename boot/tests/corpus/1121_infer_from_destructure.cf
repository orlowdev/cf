# a generic function's type argument is inferred from a destructure binder (`id(a)`)
# expect: exit 42
const id = ['T]('T v): 'T -> v
pub const main = (): Iarch -> {
	const (a, b) = (40, 2)
	return id(a) + id(b)
}
