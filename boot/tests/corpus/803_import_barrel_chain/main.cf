# a reexport CHAIN: a re-exports from b, b re-exports from c. main imports through a,
# and the name is followed all the way to c's definition.
# expect: exit 42
import a::{ v }

pub const main = () -> v()
