# a spread carries a record element's pointer into the new tuple
# expect: exit 27
data P = { Iarch x }
const mk = (Iarch a): P -> P({ x: a })
pub const main = () -> {
	const t = (2, mk(25))
	const u = (...t, 0)
	return u[0] + u[1].x
}
