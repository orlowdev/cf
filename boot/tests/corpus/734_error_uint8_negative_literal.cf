# a negative literal into an unsigned Uint8 is a genuine (forever) type error: `-1` is `-`
# applied to a literal (an Iarch expression), which is neither in Uint8's range nor its type
# expect: error
pub const main = () -> {
	const Uint8 x = -1
	return Iarch(x)
}
