# destructure a record position out of a tuple-returning call
# expect: exit 13
data P = { Iarch x, Iarch y }
const mk = (Iarch a, Iarch b): P -> P({ x: a, y: b })
const split = (Iarch n): (Iarch, P) -> (n, mk(n, n + 1))
pub const main = () -> {
	const (k, p) = split(6)
	return k + p.y  # 6 + 7
}
