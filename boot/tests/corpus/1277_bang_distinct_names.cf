# `foo` and `foo!` are DISTINCT identifiers — the trailing `!` is part of the name,
# so the two functions coexist and resolve independently.
# expect: exit 22
const dub = (Iarch x): Iarch -> x + x
const dub! = (Iarch x): Iarch -> x * x
pub const main = () -> dub(3) + dub!(4)
