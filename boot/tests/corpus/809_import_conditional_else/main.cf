# the condition is false (target is not Linux), so the `else` branch is selected
# expect: exit 42

import std::comptime::os
import std::sys::{ Os }

if os::target == Os.Linux then import nope::{ v }
else import impl::{ v }

pub const main = () -> v()
