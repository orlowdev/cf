# a value loop is an ordinary expression: returned directly, and an operand of `+`
# expect: exit 8
const first_over = (Iarch cap): Iarch -> {
	let i = 0
	return 1 + loop {
		if i * i > cap then <- i
		i = i + 1
	}
}
pub const main = () -> first_over(40)
