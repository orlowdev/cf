# cfcc scopes `defer` to a function's top level: a per-iteration count is rejected.
# expect: error
const f = (Iarch x): Iarch -> x
pub const main = () -> {
	loop {
		defer f(1)
		break
	}
	return 0
}
