# A value block may yield a Str (an arena pointer through the merge slot); `.len` reads through it.
# expect: exit 5
pub const main = () -> {
	let Iarch n = 1
	let Str s = if n == 1 then { <- "hello" } else { <- "" }
	return Iarch(s.len)
}
