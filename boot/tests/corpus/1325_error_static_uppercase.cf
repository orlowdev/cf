# A value name — including a `static` — must be lowercase-initial; an uppercase-initial name is a
# type. `POOL` is rejected.
# expect: error
static [64 Uint8] POOL
pub const main = () -> Iarch(POOL.len)
