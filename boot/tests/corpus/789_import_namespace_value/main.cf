# a value namespace import: `import "m" as math` reaches the module's exported
# functions as `math::name(args)`, rewritten to the mangled name on flatten
# expect: exit 42
import math

pub const main = () -> math::add(40, 2)
