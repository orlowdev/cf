# a value that needs 64 bits: 2^32 + 5, shifted right 32 → 1 (64-bit) vs 5 (32-bit overflow)
# expect: exit 1
pub const main = () -> {
	let Iarch x = 65536
	x = x * 65536
	x = x + 5
	x = x >> 32
	return x
}
