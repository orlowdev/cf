# a Str position binds an (immutable) Str local, usable via `.len`
# expect: exit 9

import std::mem::arena::growing_arena
const main_body! = (): Iarch -> {
	const (n, s) = (4, "hello")
	return n + s.len
}

pub const main = () -> {
	const a = growing_arena::of(4096)
	return main_body!() in a
}
