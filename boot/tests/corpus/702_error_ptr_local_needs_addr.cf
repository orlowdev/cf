# A `*Point` local's initializer must be a pointer (`&x` or a `*T`) — a bare record value no
# longer implicitly becomes a pointer (the 4c tightening).
# expect: error
data P = { Iarch x }
pub const main = () -> {
	const P q = { x: 1 }
	const *P p = q
	return p.x
}
