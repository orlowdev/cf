# a generic function's type argument is inferred from a nested call result, including a
# generic call nested in a generic call (`id(id(42))`)
# expect: exit 42
const id = ['T]('T v): 'T -> v
const dbl = (Iarch n): Iarch -> n + n
pub const main = (): Iarch -> id(dbl(21)) - id(id(0))
