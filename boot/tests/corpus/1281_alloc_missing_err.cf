# both-ways check: a function that holds trapped residue MUST be marked `!` — the missing marker
# is a compile error.
# check: alloc
# expect: error
data Point = { Iarch x, Iarch y }
const oops = (): Iarch -> {
	const Point p = { x: 7, y: 9 }
	return p.x
}
pub const main = () -> oops()
