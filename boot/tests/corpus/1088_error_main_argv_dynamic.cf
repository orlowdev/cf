# `main([Str] args)` needs the freestanding entry — the hosted C runtime passes raw
# argc/argv, so cfcc rejects the `[Str]` form under `--libc dynamic`
# cflags: --libc dynamic
# expect: error
pub const main = ([Str] args) -> Iarch(args.len)
