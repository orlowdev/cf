# a locally-declared generic applied to an IMPORTED type argument (List.1.m_Point mangle)
# expect: exit 7
import point::{ Point }
data Wrap['T] = { 'T inner }
pub const main = () -> {
	const Wrap[Point] w = { inner: Point({ x: 3, y: 4 }) }
	return w.inner.x + w.inner.y
}
