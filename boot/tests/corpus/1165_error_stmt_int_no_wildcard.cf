# a statement integer match without a `_` is never exhaustive
# expect: error
const f = (Iarch n): Iarch -> {
	let Iarch a = 0
	match n { 0 -> { a = 1 }, 1 -> { a = 2 } }
	return a
}
pub const main = () -> f(0)
