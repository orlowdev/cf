# A closure may capture an enclosing parameter (read-only here) by reference.
# expect: exit 37
const run = (Iarch seed): Iarch -> {
	const add = (Iarch k) -> seed + k
	return add(7)
}
pub const main = () -> run(30)
