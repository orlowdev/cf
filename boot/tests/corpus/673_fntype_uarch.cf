# A function value over `Uarch` — a non-`Iarch` function-type parameter and return.
# expect: exit 7
const apply = ((Uarch) -> Uarch f, Uarch x): Uarch -> f(x)
const id = (Uarch n): Uarch -> n
pub const main = () -> Iarch(apply(id, Uarch(7)))
