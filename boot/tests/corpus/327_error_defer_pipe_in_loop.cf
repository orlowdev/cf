# The top-level restriction holds for the pipe-tap form too: no `defer` in a loop.
# expect: error
const f = (Iarch x): Iarch -> x
pub const main = () -> {
	loop {
		let Iarch y = 1 |> defer f
		break
	}
	return 0
}
