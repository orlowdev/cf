# compound assignment on a record field target: `p.f op= y`
# expect: exit 33
import std::mem::arena::fixed_arena
data Point = { Iarch x, Iarch y }

const main_body! = (): Iarch -> {
	let Point p = { x: 3, y: 4 }
	p.x += 10
	p.y *= 5
	return p.x + p.y
}

pub const main = () -> {
	const a = fixed_arena::of(4096)
	return main_body!() in a
}
