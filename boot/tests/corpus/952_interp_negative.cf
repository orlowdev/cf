# negative itoa: "${x}" with x=-7 -> "-7"; m[0] is '-' (ASCII 45)
# expect: exit 45

const main_body! = (): Iarch -> {
	let Iarch x = -7
	const Str m = "${x}"
	return Iarch(m[0])
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
