# the right-hand side may be a tuple variable, evaluated once
# expect: exit 6

import std::mem::arena::fixed_arena
const main_body! = (): Iarch -> {
	const t = (1, 2, 3)
	const (a, b, c) = t
	return a + b + c
}

pub const main = () -> {
	const a = fixed_arena::of(4096)
	return main_body!() in a
}
