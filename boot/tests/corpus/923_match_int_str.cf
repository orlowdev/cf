# an integer-literal match yielding a Str (immutable `l` pointer through the merge slot)
# expect: exit 3
const name = (Iarch n): Str -> match n {
	1 -> "one",
	2 -> "two",
	_ -> "many",
}
pub const main = () -> Iarch(name(2).len)
