# a generic type flowing through a transitive module chain, diamond-deduped to one template
# expect: exit 55
import "mid" as { mk }
import "gen" as { Box }
pub const main = () -> {
	const Box[Iarch] b = mk(55)
	return b.v
}
