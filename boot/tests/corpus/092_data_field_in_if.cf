# field reads compose with control flow
# expect: exit 9
# check: root
data Point = { Iarch x, Iarch y }

const main_body! = (): Iarch -> {
	const Point p = { x: 5, y: 9 }
	return if p.x < p.y then p.y else p.x
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
