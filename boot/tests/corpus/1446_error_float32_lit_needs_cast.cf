# A bare float literal is `Float64` — a `Float32` binding wraps it in an explicit
# `Float32(...)` cast; there is no literal width adoption (prefer restriction).
# expect: error
pub const main = (): Iarch -> {
	const Float32 f = 1.5
	return 0
}
