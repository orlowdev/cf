# an integer-literal `match`: single literals, an or-pattern `1|2|3`, and a `_` default
# expect: exit 100
const classify = (Iarch n): Iarch -> match n {
	0 -> 100,
	1 | 2 | 3 -> 200,
	7 -> 300,
	_ -> 999,
}
pub const main = () -> classify(2) - classify(0)
