# a parameter is immutable — a compound assign to it is rejected (on a callee reached from main)
# expect: error
const bump = (Iarch n): Iarch -> {
	n += 1
	return n
}

pub const main = () -> bump(0)
