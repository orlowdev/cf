# a spread may sit between explicit elements (prepend/append around it)
# expect: exit 30

# check: root
const main_body! = (): Iarch -> {
	const t = (10, 20)
	const u = (5, ...t, 100)
	return u[1] + u[2]
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
