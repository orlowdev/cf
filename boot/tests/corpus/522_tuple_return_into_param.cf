# the full round trip: one function's tuple return feeds another's tuple parameter
# expect: exit 8
const dm = (Iarch a, Iarch b): (Iarch, Iarch) -> (a / b, a % b)
const useq = ((Iarch, Iarch) qr): Iarch -> qr[0] + qr[1]
pub const main = () -> useq(dm(23, 4))
