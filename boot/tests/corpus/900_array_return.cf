# a function returns a `[N Iarch]` fixed array (an arena pointer, copy-free)
# expect: exit 42
const build = (): [4 Iarch] -> [10, 20, 30, 42]

pub const main = () -> {
	const [4 Iarch] xs = build()
	return xs[3]
}
