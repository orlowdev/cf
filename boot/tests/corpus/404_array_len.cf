# `.len` is the comptime element count
# expect: exit 4

const main_body! = (): Iarch -> {
	const [4 Iarch] xs = [1, 2, 3, 4]
	return xs.len
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
