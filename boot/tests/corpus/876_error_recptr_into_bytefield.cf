# a `*Record` aggregate pointer is not a byte buffer — storing it into a `*[Uint8]` field rejects
# expect: error
data R = { Iarch y }
data Bad = { *[Uint8] src }
const f = (*R r): Iarch -> {
	const b = Bad({ src: r })
	return 0
}
pub const main = () -> 0
