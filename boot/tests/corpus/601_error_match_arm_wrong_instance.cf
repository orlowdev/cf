# a match arm naming a DIFFERENT instance than the scrutinee is rejected
# expect: error
union U['T] = { Pair((Iarch, 'T)), Empty }
const sum = (U[Iarch] u): Iarch -> match u {
	U[Unit].Pair(p) -> p[0],
	U.Empty -> 0,
}
pub const main = () -> sum(U[Iarch].Pair((1, 2)))
