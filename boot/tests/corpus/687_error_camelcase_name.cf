# a value name must be snake_case (ebnf § Identifiers: `var_name` is lowercase, no interior
# uppercase) — a camelCase name like `useVal` is rejected at the lexer
# expect: error
data Point = { Iarch x, Iarch y }
const useVal = (Point p): Iarch -> p.x
pub const main = () -> useVal({ x: 5, y: 6 })
