# nested `for` loops with distinct loop variables
# expect: exit 45
pub const main = () -> {
	const [2 Iarch] xs = [2, 3]
	const [2 Iarch] ys = [4, 5]
	let Iarch acc = 0
	for a in xs {
		for b in ys {
			acc += a * b
		}
	}
	return acc
}
