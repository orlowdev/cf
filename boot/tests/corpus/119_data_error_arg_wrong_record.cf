# passing the wrong record type for a record parameter is rejected
# expect: error
data A = { Iarch x }
data B = { Iarch y }
const f = (A a): Iarch -> a.x
pub const main = () -> {
	let B b = { y: 1 }
	return f(b)
}
