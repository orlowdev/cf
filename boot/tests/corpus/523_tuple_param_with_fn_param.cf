# a tuple-type parameter coexists with a function-type parameter (the `->` disambiguates)
# expect: exit 20
const apply = ((Iarch) -> Iarch f, (Iarch, Iarch) p): Iarch -> f(p[0]) + p[1]
const dbl = (Iarch n): Iarch -> n * 2
pub const main = () -> apply(dbl, (7, 6))
