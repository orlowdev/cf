# `Iarch` does not satisfy a union bound; body is bound-agnostic so the bound is the sole cause
# expect: error
union Color = { Red, Green }
pub const f = [Color 'T]('T c): Iarch -> 0
pub const main = () -> {
	return f[Iarch](5)
}
