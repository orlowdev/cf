# Uarch flows param -> compute -> local -> comparison -> if-value
# expect: exit 1
const halve = (Uarch n): Uarch -> n / Uarch(2)
pub const main = () -> {
	const Uarch big = halve(Uarch(1000))
	const Uarch d = if big > Uarch(400) then Uarch(1) else Uarch(0)
	return Iarch(d)
}
