# `main([Str] args)` — the freestanding `_start` builds a real cf `[Str]` from the OS's raw
# (argc, argv). Return the argument count (argv[0] + the two `# args`).
# args: alpha beta
# expect: exit 3
pub const main = ([Str] args) -> {
	return Iarch(args.len)
}
