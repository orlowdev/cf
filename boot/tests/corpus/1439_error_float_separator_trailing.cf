# A `_` digit separator sits only BETWEEN digits (ebnf § Numbers), in the fraction as in the
# integer part: a trailing `_` stops the float literal and the stray `_` is rejected.
# expect: error
pub const main = () -> Iarch(1.5_ * 2.0)
