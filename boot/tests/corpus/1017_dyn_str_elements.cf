# a `[Str]` dynamic array returned and consumed as a param; read element 0's length.
# expect: exit 3
const mk = (): [Str] -> {
	let [Str] ss = []
	ss = [...ss, "abc"]
	return ss
}
const firstlen = ([Str] ss): Iarch -> Iarch(ss[0].len)
pub const main = () -> firstlen(mk())
