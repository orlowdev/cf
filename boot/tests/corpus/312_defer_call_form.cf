# The call form `defer f(x)` schedules a call whose result is discarded; it runs
# at scope exit without disturbing the returned value.
# expect: exit 5
const noop = (Iarch x): Iarch -> x
const run = (): Iarch -> {
	defer noop(1)
	defer noop(2)
	return 5
}
pub const main = () -> run()
