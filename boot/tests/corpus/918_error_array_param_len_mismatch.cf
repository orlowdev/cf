# a by-value `[N T]` param checks the argument's length (the callee's comptime `.len`/`for`
# would otherwise read a phantom element past a too-short array)
# expect: error
const total = ([4 Iarch] xs): Iarch -> xs.len
pub const main = () -> {
	const [3 Iarch] a = [1, 2, 3]
	return total(a)
}
