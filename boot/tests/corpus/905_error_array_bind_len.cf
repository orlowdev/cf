# a `[N Iarch]` binding's length must match the array-returning call's length
# expect: error
const build = (): [4 Iarch] -> [1, 2, 3, 4]

pub const main = () -> {
	const [3 Iarch] xs = build()
	return xs[0]
}
