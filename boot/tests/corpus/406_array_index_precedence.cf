# index binds tighter than arithmetic
# expect: exit 38

# check: root
const main_body! = (): Iarch -> {
	const [3 Iarch] xs = [3, 5, 7]
	return xs[0] + xs[1] * xs[2]
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
