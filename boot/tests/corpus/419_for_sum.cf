# `for x in xs` iterates a fixed array
# expect: exit 10

# check: root
const main_body! = (): Iarch -> {
	const [4 Iarch] xs = [1, 2, 3, 4]
	let Iarch sum = 0
	for x in xs {
		sum += x
	}
	return sum
}

pub const main = () -> {
	const a = mem.arena.grow(4096)
	return main_body!() in a
}
