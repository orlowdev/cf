# both-ways: allocation inside a `defer` body requires the enclosing marker.
# expect: error
data P = { Iarch v }
const run = (): Iarch -> {
	defer {
		const P p = { v: 9 }
	}
	return 0
}
pub const main = () -> run()
