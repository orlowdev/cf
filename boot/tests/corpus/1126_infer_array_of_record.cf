# a type variable inferred to an array of a RECORD (`'T = [Point]`)
# expect: exit 42
data Point = { Iarch x }
const id = ['T]('T v): 'T -> v
pub const main = (): Iarch -> {
	let [Point] ps = []
	ps = [...ps, Point({ x: 42 })]
	const [Point] qs = id(ps)
	return qs[0].x
}
