# an aggregate `if`'s branches must yield the same type
# expect: error
data A = { Iarch x }
data B = { Iarch y }
const mk_a = (): A -> A({ x: 1 })
const mk_b = (): B -> B({ y: 2 })
const bad = (Iarch c): A -> if c > 0 then mk_a() else mk_b()
pub const main = () -> 0
