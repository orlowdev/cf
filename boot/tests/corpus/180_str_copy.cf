# a `const Str` local may bind another Str — sound because Str is immutable (aliasing, not a copy)
# expect: exit 10
pub const main = () -> {
	const Str a = "hello"
	const Str b = a
	return a.len + b.len
}
