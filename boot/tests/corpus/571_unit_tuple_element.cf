# a tuple may carry a unit element `()` (a word `0`); other positions index as usual
# expect: exit 5

const main_body! = (): Iarch -> {
	const t = (5, ())
	return t[0]
}

pub const main = () -> {
	const a = mem.arena.of(4096)
	return main_body!() in a
}
