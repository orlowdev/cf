# a name cannot be both imported and defined at the top level — the flatten's
# textual rewrite cannot disambiguate the two, so it is rejected
# expect: error
import "a" as { one }
const one = () -> 5

pub const main = () -> one()
