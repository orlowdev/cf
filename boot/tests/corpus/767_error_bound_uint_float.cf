# a `[Uint 'T]` bound rejects a float — Float64 is not a member of the unsigned union Uint
# expect: error
pub const widen = [Uint 'T]('T x): Iarch -> Iarch(x)
pub const main = () -> widen[Float64](1.5)
