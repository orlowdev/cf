# a union tuple element must be a fresh construction, not an aliased binding — rejected.
# expect: error
union N = { I(Iarch), Z }
pub const main = () -> {
	const N u = N.I(1)
	const t = (u, 3)
	return 0
}
