# a splatted field name colliding with another parameter is a duplicate-parameter error.
# expect: error
type G = { Iarch a, Iarch b }
const f = (G g, Iarch a): Iarch -> a
pub const main = () -> f(1, 2, 3)
