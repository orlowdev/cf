# a `*Str` FIELD is rejected for parity with the `*Str` param error (690): a pointer's pointee
# must be a record or union (§6.4); `Str` is a byte-vector, and a byte pointer is spelled `*[Uint8]`
# expect: error
data Bad = { Iarch n, *Str s }
pub const main = () -> 0
