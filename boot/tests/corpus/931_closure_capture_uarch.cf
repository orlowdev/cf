# a closure captures a `Uarch` by reference and reads it
# expect: exit 12
const run = (): Iarch -> {
	const Uarch u = Uarch(7)
	const f = (Iarch n) -> n + Iarch(u)
	return f(5)
}
pub const main = () -> run()
