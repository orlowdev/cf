# Corpus tests cf0 is expected to compile, one per line (a filename or a test
# directory basename under boot/cfcc/tests/). The differential harness compiles
# each with BOTH cfcc and cf0 and requires their runtime results to AGREE and to
# match the test's `# expect:` directive. As cf0's front end grows, more tests
# move here from the cfcc corpus.
#
# Tracer bullet: `main` returns a constant exit code (a literal, or a block with
# a single `return <int>`).
001_main_return.cf
002_return_value.cf
003_block_return.cf
008_comments.cf
# Integer arithmetic: precedence, parens, left-associativity, div/mod, unary neg.
016_arith_precedence.cf
017_arith_parens.cf
018_arith_left_assoc.cf
019_arith_div.cf
020_arith_mod.cf
021_unary_neg.cf
025_trailing_operator.cf
# Bitwise and shift operators.
026_bitand.cf
027_bitor.cf
028_bitxor.cf
029_shl.cf
030_shr.cf
# Comparison operators and value-producing `if … then … else`.
031_cmp_gt_true.cf
032_cmp_eq_true.cf
033_cmp_false.cf
# Short-circuit logical operators and `true`/`false`.
040_logical_and.cf
078_logical_or.cf
081_logical_precedence.cf
082_logical_normalize.cf
# Local bindings + block bodies: const/let, typed bindings, reassignment, and the
# self-reference / missing-return error cases.
041_locals_chain.cf
043_let_binding.cf
044_local_typed.cf
045_local_name_no_collision.cf
050_let_reassign.cf
046_local_self_reference.cf
049_block_no_return.cf
# Function parameters: `main`'s (argc, argv, envp) prefix — word and pointer
# params, params in expressions / locals / let-reassign, plus the arity, shadow,
# reassign, pointer-return, duplicate-name, and bad-name error cases.
004_argc.cf
005_argc_envp_block.cf
006_argc_only.cf
007_unused_params.cf
022_expr_with_argc.cf
042_local_uses_param.cf
053_reassign_from_param.cf
009_missing_main.cf
011_duplicate_param.cf
012_bang_in_name.cf
013_return_pointer.cf
014_too_many_params.cf
015_unknown_name.cf
048_local_shadows_param.cf
055_reassign_param.cf
# Function calls + multiple top-level functions: basic/forward-ref/nested/chained
# calls, zero-arg fns, recursion, else-if via a helper, params as call args,
# short-circuit skipping a call, plus arity / duplicate-fn / main-not-pub / no-main
# error cases.
057_call_basic.cf
058_call_forward_ref.cf
059_call_in_expr.cf
060_call_nested.cf
061_call_with_param.cf
062_call_chain.cf
072_else_if_chain.cf
074_recursion_factorial.cf
075_recursion_sum_param.cf
079_short_circuit_and.cf
080_short_circuit_or.cf
064_call_wrong_arity.cf
065_duplicate_function.cf
066_main_not_pub.cf
067_no_main.cf
# Typecheck phase: real Bool type + no-truthiness, integer-literal range, matching
# `if` branches, precedence sanity, block/if value positions, plus the type/binding
# error cases (out-of-range literal, pointer-in-expr, non-associative comparison,
# duplicate local, const reassignment, assign-to-undefined, unknown call).
023_block_expr.cf
036_prec_and_over_or.cf
037_prec_shift_below_add.cf
038_prec_cmp_below_bitand.cf
052_let_accumulate.cf
068_if_true.cf
069_if_false.cf
070_if_truthy_int.cf
071_if_as_value.cf
073_if_nested.cf
083_logical_in_if.cf
010_exit_out_of_range.cf
024_pointer_in_expr.cf
039_cmp_nonassoc.cf
047_duplicate_local.cf
054_reassign_const.cf
056_assign_undefined.cf
063_call_unknown.cf
# Unary logical `!` (Bool→Bool) and bitwise `~` (Int→Int).
034_lnot.cf
035_bnot.cf
# Numeric casts `T(x)` — identity in cf0's word-only codegen (`Iarch`/`Uarch`/`Int64`/…
# all a word), through locals, params, returns, arithmetic, and nested casts; plus the
# operand errors (a Bool, a pointer, or a record is not a scalar integer).
752_iarch_local.cf
754_iarch_param_return.cf
755_iarch_arithmetic.cf
756_iarch_interconvert.cf
182_uarch_local.cf
829_error_bool_cast.cf
289_error_cast_pointer.cf
290_error_cast_record.cf
# Data records (part 1): `data` decls with Iarch fields, construct via a typed
# binding + read by field (single/multi-field, order-independent literals, a field
# read as a call argument), and the unknown-field error.
084_data_construct_read.cf
085_data_single_field.cf
086_data_three_fields.cf
087_data_field_order_independent.cf
093_data_field_as_arg.cf
095_data_error_unknown_field.cf
# More records part 1: field values from params/locals, multiple records/types,
# fields in control flow, trailing comma; and the literal/declaration error cases
# (missing/extra/duplicate field, empty record, non-record body, record-as-int,
# undeclared type, missing annotation).
088_data_field_from_param.cf
089_data_field_from_local.cf
090_data_two_records.cf
091_data_two_types.cf
092_data_field_in_if.cf
094_data_trailing_comma.cf
096_data_error_missing_field.cf
097_data_error_extra_field.cf
098_data_error_dup_field_decl.cf
099_data_error_dup_field_literal.cf
100_data_error_record_as_int.cf
102_data_error_undeclared_type.cf
103_data_error_nonrecord_body.cf
105_data_error_empty_record.cf
106_data_error_no_annotation.cf
# Records part 2a: field mutation on a `let` record (`p.field = expr`), and its
# error cases (mutate a const record, an undeclared field, a non-record, or the
# whole record as a unit).
101_data_let_record_mutate.cf
109_data_mutate_multiple_fields.cf
110_data_mutate_from_own_field.cf
111_data_mutate_independent.cf
112_data_error_mutate_const.cf
113_data_error_mutate_unknown_field.cf
114_data_error_field_assign_on_int.cf
115_data_error_assign_whole_record.cf
# Record parameters (part 2b, by pointer — no arena, records don't escape): several
# records with distinct storage, a record built in a callee returning a scalar, a
# record passed by pointer and its fields read, a record + Iarch mix, a record
# passed through to another function, and the wrong-record-type argument error.
107_arena_many_records.cf
108_arena_record_in_callee.cf
116_data_record_param.cf
117_data_record_param_mixed.cf
118_data_record_param_passthrough.cf
119_data_error_arg_wrong_record.cf
# Tag-only unions + match: construct a member, match on the tag (first arm, full
# ladder, wildcard, multiline decl, union copy, union parameter); and the match
# error cases (non-exhaustive, wrong-union arm, unknown member, duplicate arm, arm
# after a wildcard).
196_union_match_first.cf
197_union_match_ladder.cf
198_union_match_wildcard.cf
199_union_multiline.cf
200_union_copy.cf
206_union_param.cf
201_error_union_nonexhaustive.cf
202_error_union_wrong_arm.cf
203_error_union_unknown_member.cf
204_error_union_dup_arm.cf
205_error_union_after_wildcard.cf
# The arena (mmap floor + cf_alloc; records now heap-allocated) and record returns:
# a function builds and returns a record by arena pointer, the caller adopts and
# reads/mutates it, and a record-returning body that is itself a call. Plus the
# record-parameter type errors (int for record, record for int, undeclared type).
123_data_record_return.cf
124_data_return_then_mutate.cf
125_data_return_call_result.cf
120_data_error_int_for_record_param.cf
121_data_error_record_for_int_param.cf
122_data_error_unknown_param_type.cf
# Explicit record construction `Type({ … })` as a value expression — in a return, an
# annotated/inferred binding, with field puns `{ x, y }`, and as a fresh value; plus
# the errors (a wrong field, a return-type mismatch, an undeclared record type).
447_construct_return.cf
448_construct_annotated.cf
449_construct_inferred.cf
450_construct_puns.cf
451_construct_fresh.cf
453_error_construct_bad_field.cf
455_error_construct_return_mismatch.cf
456_error_construct_unknown.cf
# More tag-only unions: a union return + a match arm yielding a union, a union
# argument type mismatch, and or-patterns (`A | B`) with their error cases (a member
# repeated across alternatives, mixed qualifiers, a wildcard as an alternative).
210_union_return.cf
211_error_union_arg_mismatch.cf
212_union_or_pattern.cf
213_error_or_dup_member.cf
214_error_or_mixed_qual.cf
215_error_wildcard_or.cf
# `==`/`!=` on two values of the same tag-only union compares their tags (type_system
# §5, structural equality) — the `is_*` predicate pattern; plus the errors: ordering
# (`<`) on a union, mixed unions, and a payload-carrying (boxed) union (recovered with
# `match`, not compared).
1076_union_eq.cf
1077_union_ne.cf
1078_union_eq_predicate.cf
1079_error_union_ordering.cf
1080_error_union_eq_mixed.cf
1081_error_boxed_union_eq.cf
# Payload unions (M1.2a: boxed {tag, payload} in the arena; construct + tag dispatch,
# no payload binding yet): construct payload members and match on the tag, a boxed
# union crossing a boundary by pointer (param + return), and the construction arity
# error.
207_union_payload_tag.cf
216_union_payload_return.cf
217_error_payload_arity.cf
# Payload BINDING in match (`Node.IntLit(v) -> v`): a positional binding read from
# the box, `_` ignoring a field, and the errors (pattern arity vs member, a repeated
# binding name).
219_union_payload_bind.cf
220_union_payload_bind_wildcard.cf
221_error_payload_arity_pattern.cf
222_error_payload_dup_bind.cf
# `[T]` dynamic arrays (`[Iarch]` core): empty/literal construction, `.len`, indexing,
# `for x in xs`, and self-growth append `xs = [...xs, e]` (via cf_dyn_push).
936_dyn_grow_len_for.cf
937_dyn_realloc_doubling.cf
938_dyn_initial_elements.cf
939_dyn_element_write.cf
# `[Record]` dynamic arrays: record elements ride as 8-byte arena pointers (`storel`/
# `loadl`), self-grown by append and read field-wise via `arr[i].field`.
942_dyn_record_elements.cf
# `loop { }` statements with break/continue (bare + guarded `if c then break`),
# nested loops, an early return after a loop, and the error cases (break/continue
# outside a loop, an unreachable statement after break, a guard that isn't a break).
132_loop_sum.cf
133_loop_continue.cf
134_loop_nested.cf
135_loop_early_return.cf
136_loop_bare_break.cf
138_loop_error_break_outside.cf
139_loop_error_continue_outside.cf
140_loop_error_unreachable_after_break.cf
141_loop_error_guard_needs_break.cf
# a statement-position `if cond then <stmt>` now guards a general statement, not only a
# loop exit: an early `return` guard coexisting with a `break` guard inside a loop (971),
# and the non-Bool guard-condition error (974, no truthiness). With an `else` branch it
# selects between two statements — `if c then return a else return b` (970), an
# `else if …` chain of returns (973), and the unreachable-after-both-return error (975).
971_if_early_return_in_loop.cf
974_error_if_stmt_non_bool.cf
970_if_else_return_terminal.cf
973_if_else_if_chain.cf
975_error_unreachable_after_terminal_if.cf
# `[Iarch]` dynamic arrays across function boundaries: a `[T]`-returning function
# whose result is adopted and indexed, a `[T]` parameter iterated with `for`, and the
# wrong-return-type error (a `[T]` return must be a dynamic array).
1019_dyn_call_binding.cf
1020_dyn_for_param.cf
1023_error_dyn_return_type.cf
# Dynamic-array element types threaded across function boundaries: a `[T]` return + param
# (`[Iarch]` and `[Str]`), a `[T]` param forwarded on, a `[Str]` element read via
# `ss[i].len`, and the element-mismatch errors (a wrong-element argument / spread append).
1016_dyn_return_param.cf
1018_dyn_param_forward.cf
1017_dyn_str_elements.cf
1021_error_dyn_elem_mismatch.cf
1041_error_dyn_spread_elem_mismatch.cf
# Value-level array spread `[...src, e]` — COPIES the source vector and appends, leaving the
# source unchanged (a fresh `[T]`, not in-place growth): in return position (1036), a binding
# (1037/948), a call argument (1038), and over a `[Record]` vector (1039). A spread in a
# return position whose function type is a scalar mismatches cleanly (949). Rejecting a `[N T]`
# FIXED-array source (1040) needs fixed-vs-dynamic tracking cf0 does not keep (a fixed array
# lowers to a plain dynamic array), so that error-test diverges from cf0.
1036_dyn_spread_return_copy.cf
1037_dyn_spread_binding.cf
948_dyn_spread_in_init.cf
1038_dyn_spread_arg.cf
1039_dyn_spread_records.cf
949_error_dyn_spread_value_position.cf
# String literals + the `Str` type: `.len` (byte count) on a literal / in an expression /
# on a parenthesised string; escape decoding (`\n \t \r \0 \\ \" \$`), embedded NUL,
# multiline, and literal `$`; `const Str` locals + aliasing copy; a `Str` parameter; and
# the errors (Str as an Int return / word bind / arithmetic operand / call arg, `.size`,
# a `let Str` reassignment, an unterminated literal, a bad escape).
142_string_len.cf
143_string_empty.cf
144_string_escapes.cf
145_string_embedded_nul.cf
146_string_len_in_expr.cf
147_string_quote_escape.cf
151_string_len_on_paren.cf
157_string_multiline.cf
158_string_escaped_dollar.cf
159_string_lone_dollar.cf
160_string_trailing_dollar.cf
180_str_copy.cf
181_str_param.cf
148_string_error_return.cf
149_string_error_word_bind.cf
150_string_error_arith.cf
152_string_error_field.cf
153_string_error_unterminated.cf
154_string_error_bad_escape.cf
156_string_error_as_arg.cf
# (179 `let Str` was a shared error test; cfcc now accepts `let Str` reassignment, but cf0's
# compiler still rejects it — a cfcc-only feature + cf0-source de-workaround — so 179 leaves the
# differential and lives only in the cfcc corpus as a positive test.)
# String interpolation `"…${name}…"` — a hole is a bare variable, stringified at runtime
# (integer via itoa, a Str inlined) and concatenated with the literal runs into a fresh
# Str; verified by `.len`. Plus the errors (an aggregate hole, and the malformed holes:
# a value/expression, empty `${}`, non-name, unterminated).
950_interp_int.cf
957_interp_adjacent_holes.cf
958_interp_uarch.cf
# String indexing `s[i]` reads the i-th byte — used to verify interpolated content:
# itoa digits, a negative sign, `0`, and a Str hole's inlined bytes.
951_interp_itoa_content.cf
952_interp_negative.cf
956_interp_zero.cf
953_interp_str_hole.cf
960_error_interp_aggregate.cf
161_string_error_interp_value.cf
162_string_error_interp_empty.cf
163_string_error_interp_not_name.cf
164_string_error_interp_unterminated.cf
# An interpolation hole may be a full EXPRESSION, not just a bare name: arithmetic `${a + b}`,
# a field access `${p.x}`, a call `${dbl(5)}`, and a Str-typed hole flowing through append. The
# stringified content is verified by indexing. Errors: a hole must be a SINGLE expression (1062),
# and may not be empty/whitespace-only (1063).
1058_interp_expr_arith.cf
1059_interp_expr_field_call.cf
1060_interp_expr_content.cf
1061_interp_expr_str_hole.cf
1062_error_interp_expr_multi.cf
1063_error_interp_expr_empty.cf
# M2 module resolution (module_system §8), brick 1 — destructured VALUE imports flattened
# across the module boundary: a called imported function (783), and importing the same
# module twice loading it once (782, dedup by resolved path). Type imports and visibility
# filtering are later bricks (a module's `pub data`/`pub union` needs pub-decl parsing).
783_import_value_with_args
782_import_same_module_twice
# Top-level `pub data`/`pub union` declarations (parsed past the optional `pub`) enable
# collision-free TYPE imports: a `pub data` record imported, constructed, field-read (784),
# and a `pub union` imported and matched by its members (785). Colliding type names across
# modules still need name mangling (786, a later brick).
784_import_type_record
785_import_type_union
# Name mangling on flatten: two modules each define a PRIVATE `helper` of the same name;
# each module's private functions are renamed with a per-module tag and its own call sites
# rewritten, so both coexist and each calls its own (40 + 2 = 42). Private record/union
# mangling (786) is a later brick.
1090_flatten_mangle_private
# Call-for-effect statements — a bare `f(args)` (and a guarded `if c then f(args)`)
# evaluated for effect, result discarded (the `die(…)`/`close(…)` shape cf0's source uses).
1091_call_for_effect.cf
# Statement-`if` with `{ block }` bodies (its own scope): a fall-through then-block, and an
# if/else where both blocks `return` (terminal — no trailing return needed). Pervasive in
# cf0's own source.
1092_stmt_if_block.cf
# `-> asm "…"` naked assembly-bodied functions (io.cf's syscall floor): the body is emitted
# verbatim into the asm floor (not QBE) with each `${param}` replaced by its arg register.
1093_asm_body.cf
# Top-level VALUE consts (`const NAME = <expr>`, no params) — modeled as a nullary function
# returning the value, a bare reference auto-called; used in arithmetic and as a call arg.
1094_value_const.cf
# Records with MIXED-WIDTH fields (the aggregate-representation upgrade): word fields
# (Bool/Iarch) and 8-byte-pointer fields (Str/[T]) in one record, laid out in uniform
# 8-byte slots, constructed and read back per-field. cf0's AST (ast.cf) is built on these.
1095_record_mixed_fields.cf
# Union members with MIXED-WIDTH payloads (aggregate wall, half 2): word/Str/[T]/tag-only
# payloads match-bound to locals of the payload type (1096), and a recursive union payload
# folded by a recursive match (1097) — the shape of cf0's own AST unions.
1096_union_mixed_payloads.cf
1097_union_recursive_payload.cf
# Chained field access `record.field.len` — a `[T]` field's length and a `Str` field's
# length read via a trailing `.len` (cf0's own emit/typecheck use `fn.params.len` etc.).
1098_chained_field_len.cf
# cf0's scalar codegen is 64-bit (Uarch/Iarch are arch-width): a value above 2^32 is
# computed and shifted, giving 1 (64-bit) where a 32-bit word would overflow to 5.
1099_scalar_64bit.cf
# Byte-buffer subsystem (built on the 64-bit widening): `[N Uint8]` buffers, byte index
# read/write, `*[Uint8]` params + record fields, and `s.bytes` (a Str's byte pointer) — the
# raw-buffer machinery cf0's lexer/resolver run on.
1100_byte_buffer.cf
1101_str_bytes.cf
# The address-of operator `&x` + `*Record` pointer parameters: a field is mutated through
# a `*Record` pointer and the change propagates to the caller (cf0's parser/emit use `*Parser`
# and `*Emit` this way).
1102_addr_record_ptr.cf
# Indexing a general expression base `r.xs[i]` — the `[i]` rides on a field read, not a
# bare name (the `p.toks[p.pos]` shape cf0's own parser needs); index is an expression.
1103_index_field_base.cf
# Statement-position `match` (a union tag-dispatch evaluated for effect, block arm bodies):
# 1104 arms mutate an outer `let` + a payload bind + `_`; 1105 exhaustive without `_`;
# 1106 a statement match is never terminal — a trailing `return` is still required (error).
1104_stmt_match_effect.cf
1105_stmt_match_terminal.cf
1106_stmt_match_needs_return.cf
# Interpolation holes are full expressions (`${a + b}`, `${p.n}`, `${xs[i]}`, a Str hole),
# lexed+parsed per hole — not just bare names.
1107_interp_expr_hole.cf
# The unsigned integer family (`Uarch` … as param/return/field types), the `Str(v)` seal of
# a `[Uint8]` vector, real field types for `arr[i].field` (incl. Str fields + chained
# `.len`), and `[Union]` element indexing.
1108_uarch_str_seal.cf
1109_index_field_real_type.cf
1110_union_element_array.cf
# The stack-cell model for mutable (`let`) aggregate locals — reassignment REPOINTS the
# binding: 1111 `xs = []` resets a `[T]` to a fresh empty array (old header intact for
# captures), 1112 a `let` union rebinds to a fresh value, 1113 a `let Str` repoints.
1111_dyn_reset_repoint.cf
1112_mutable_union_reassign.cf
1113_mutable_str_reassign.cf
1114_nested_field_index_chain.cf
# `main([Str] args)` — the freestanding `_start` builds a cf `[Str]` from the raw (argc,
# argv) via cf_build_args before calling main (the self-hosting floor gap).
1115_main_argv_strs.cf
# A `[Uint8]` dynamic array is byte-PACKED (stride 1, storeb/loadub), so `Str(v)` borrows
# its backing as contiguous bytes — what `intern` relies on. The 8-byte-slot bug here broke
# self-hosting; its fix reached the cf0 self-compilation FIXPOINT (gen1==gen2 output).
1116_dyn_uint8_packed.cf
1117_spread_into_new.cf
# Compound assignment `x op= y` (whole-name) desugars at parse to `x = x op y`: the
# arithmetic set (291/292), the bitwise + shift set (293), an accumulating loop (295),
# and rejections on a const/param/whole-record target (296-298). 299 (a const FIELD
# `p.x += 1`) is rejected too — cf0 has no field-level compound assignment yet.
291_compound_add.cf
292_compound_arith.cf
293_compound_bitwise.cf
295_compound_loop_accumulate.cf
296_error_compound_const.cf
297_error_compound_param.cf
298_error_compound_whole_record.cf
299_error_compound_const_field.cf
# Fixed-size arrays `[N Elem]` (a length-checked literal, represented as a dynamic array)
# and `for x in xs` iteration over them: sum/product accumulation (419/423/424/425), a
# value-if body (426), nested loops (421), and break/continue steering (420/422). Rejects
# a non-array iterable (427), a non-bare-name iterable (428), and a loop var that shadows
# an existing binding (429). Corpus 430 (sibling loops reusing a var) is intentionally NOT
# here: it asserts cfcc's function-scoped loop var, whereas cf0 block-scopes it.
419_for_sum.cf
420_for_break_continue.cf
421_for_nested.cf
422_for_break_early.cf
423_for_product.cf
424_for_single.cf
425_for_var_in_expr.cf
426_for_value_if.cf
427_error_for_non_array.cf
428_error_for_non_var.cf
429_error_for_var_collision.cf
# Base scalar tuples `(a, b, …)` (Iarch/Bool elements): comptime-literal indexing `t[k]`
# (475/479), grouping vs the collapsing one-tuple `(e,)` (480), and the rejection surface —
# runtime index (482), out-of-range (483), a mutable tuple (484), a non-scalar element
# (485 record / 487 array), returning a tuple where an Iarch is wanted (486), and `()` (488).
# Heterogeneous Str/record elements + `t[k].field` are the next tuple brick.
475_tuple_index.cf
479_tuple_index_in_expr.cf
480_tuple_grouping_preserved.cf
482_error_tuple_runtime_index.cf
483_error_tuple_index_oob.cf
484_error_tuple_mutable.cf
485_error_tuple_alias_record.cf
486_error_tuple_return.cf
487_error_tuple_array_element.cf
488_error_tuple_unit.cf
# Heterogeneous tuple elements (an Iarch with a fresh record / a Str) and `t[k].field` /
# `t[k].len` through them, including a nested record chain `t[1].in.v`.
476_tuple_hetero_record.cf
477_tuple_hetero_str.cf
478_tuple_two_records.cf
481_tuple_nested_via_record.cf
# Tuple destructuring `const/let (a, _, c) = <tuple>` — sugar for positional extraction:
# by-name binds (490/491/497), `_`/`_n` skips (492/493), a `let` scalar pattern (494), a
# Str position (495), a trailing comma (496), a record position (530); and rejections —
# arity over/under (498/499), non-tuple rhs (500), duplicate name (501), a `let`
# record/Str position (502/503), a name collision (504), a malformed skip `_foo` (505).
490_destructure_pair.cf
491_destructure_from_var.cf
492_destructure_skip.cf
493_destructure_skip_n.cf
494_destructure_let.cf
495_destructure_str_position.cf
496_destructure_trailing_comma.cf
497_destructure_two.cf
530_destructure_record_position.cf
498_error_destructure_arity_over.cf
499_error_destructure_arity_under.cf
500_error_destructure_non_tuple.cf
501_error_destructure_dup.cf
502_error_destructure_let_record.cf
503_error_destructure_let_str.cf
504_error_destructure_collision.cf
505_error_destructure_bad_skip.cf
# Tuple returns `: (A, B)` — the multi-value return: return a tuple literal / a tuple local
# / value-if positions (506/508/512), heterogeneous Str/record elements (509/510), bind the
# result whole then index (507) or destructure it (506/532), chained (511); and rejections —
# arity/shape mismatch (513), a non-tuple return (514), an element-type mismatch (515), an
# unknown element type (516), a one-element tuple type (517), and main returning a tuple (518).
506_tuple_return_destructure.cf
507_tuple_return_bind_whole.cf
508_tuple_return_from_local.cf
509_tuple_return_hetero_str.cf
510_tuple_return_hetero_record.cf
511_tuple_return_chained.cf
512_tuple_return_valueif_elems.cf
532_destructure_record_from_return.cf
513_error_tuple_return_shape.cf
514_error_tuple_return_nontuple.cf
515_error_tuple_return_elem_type.cf
516_error_tuple_return_unknown_type.cf
517_error_tuple_return_arity_one.cf
518_error_main_returns_tuple.cf
# Tuple parameters `(A, B) name` — passed by pointer, indexed at comptime positions; a
# heterogeneous Str/record element (520/521), a tuple return fed into a tuple param (522),
# and destructured in the body (524); rejections — arity/shape mismatch (525), a non-tuple
# argument (526), an element-type mismatch (527), a `'T` generic element in a non-generic
# function (528), and a one-element tuple type (529). (523, a tuple param beside a
# function-type param, needs function types — a later feature.)
519_tuple_param.cf
520_tuple_param_hetero_str.cf
521_tuple_param_record_elem.cf
522_tuple_return_into_param.cf
524_tuple_param_destructure.cf
525_error_tuple_param_shape.cf
526_error_tuple_param_nontuple.cf
527_error_tuple_param_elem_type.cf
528_error_tuple_param_generic_elem.cf
529_error_tuple_param_arity_one.cf
# Tuple spread `(a, ...src, b)` — splice a source tuple's positions (any position, multiple
# spreads, a lone `(...t)` copy): append/middle/two-spreads (533/534/535), heterogeneous
# and record-element preservation (536/538), a copy (537), building a tuple argument (539);
# rejections — a non-tuple spread source (540) and spreading a scalar element (541).
533_tuple_spread_append.cf
534_tuple_spread_middle.cf
535_tuple_spread_two.cf
536_tuple_spread_hetero.cf
537_tuple_spread_lone.cf
538_tuple_spread_record_elem.cf
539_tuple_spread_into_param.cf
540_error_tuple_spread_nontuple.cf
541_error_tuple_spread_int_elem.cf
# Record tuple fields `data R = { (A, B) f }` — construct, read + comptime-index the field
# (542), a heterogeneous field with `r.t[1].len` (543), whole-tuple field mutation on a
# `let` record `r.p = (…)` (544), and destructuring a record's tuple field `const (a,b) =
# r.p` (545); rejections — shape/element mismatch (549/550), a `'T` element (551), and a
# one-element tuple field (552). (553, a concrete tuple field in a GENERIC record, needs
# generics — a later cluster.)
542_record_tuple_field.cf
543_record_tuple_field_hetero.cf
544_record_tuple_field_mutate.cf
545_record_tuple_field_destructure.cf
549_error_record_tuple_field_shape.cf
550_error_record_tuple_field_elem.cf
551_error_tuple_field_generic_elem.cf
552_error_tuple_field_arity_one.cf
# Union tuple payloads `union B = { Wrap((A, B)), … }` — construct `B.Wrap((10,20))`, match
# and index the payload `B.Wrap(p) -> p[0]+p[1]` (546), a heterogeneous payload passed and
# matched (547); plus a positional two-arg payload `Two(A, B)` staying distinct from a
# one-tuple payload (548).
546_union_tuple_payload.cf
547_union_tuple_payload_param.cf
548_union_positional_vs_tuple.cf

# Nested tuples `((a,b), c)` — a tuple position may itself be a tuple, to any depth (the
# element type is carried structurally via the recursive `TupElem`, not a parsed string):
# index through levels `t[0][0]` (554), bind a nested position `const inner = t[0]` (555),
# destructure it (556), a nested tuple as param/return/record field (557/558/559), and a
# return→param roundtrip of the same shape (560); rejecting an index past a scalar leaf
# `t[1][0]` (561).
554_nested_tuple_value.cf
555_nested_tuple_bind.cf
556_nested_tuple_destructure.cf
557_nested_tuple_param.cf
558_nested_tuple_return.cf
559_nested_tuple_field.cf
560_nested_tuple_roundtrip.cf
561_error_nested_tuple_index_scalar.cf

# The `Unit` type — `()`, the zero-tuple (type_system §6.1): a first-class, bindable,
# zero-information value (word-repr, its own type). Bind + copy (562), return `Unit` +
# bind the result (563), `()` and `Unit` name the same return type (564), a valueless
# `return` desugars to `return ()` (569); rejections — no arithmetic (565), not an Iarch
# (566), a `Unit` fn yields only `()` (567), `main` must return the Iarch exit code so a
# `Unit` annotation (568) or a valueless `return` (570) there is rejected. AND unit IN a
# tuple: a `()` element indexes past as usual (571), binds to a `Unit` local (572),
# destructures (573), as a tuple return element (574) and a record's tuple field (575).
562_unit_value_bind.cf
563_unit_return.cf
564_unit_return_parens_type.cf
565_error_unit_arith.cf
566_error_unit_as_int.cf
567_error_unit_return_mismatch.cf
568_error_main_unit_annot.cf
569_unit_bare_return.cf
570_error_bare_return_main.cf
571_unit_tuple_element.cf
572_unit_element_bind.cf
573_unit_element_destructure.cf
574_unit_element_return_type.cf
575_unit_tuple_record_field.cf

# Fixed-width integer tower, brick 1 — the eight leaves (`Int8`…`Uint64`) as DISTINCT
# width/signedness types (not the register word `Iarch`): a signed/unsigned local held
# canonically sign/zero-extended in its `l` slot (709/715), an explicit truncating cast
# `Int8(x)`/`Uint64(x)` via a shl+sar/shr canonicalization (710/711/712/714/716),
# `Iarch(x)` readback (all), a bare literal ADOPTING a fixed annotation range-checked
# (713 fits, 725/726 overflow, 734 negative-into-unsigned), all eight coexisting (724),
# fixed params + returns with NO implicit widen/narrow (718; 731/733 reject a bare word,
# 732 a wrong-type binding), a `let` fixed local reassigning from a cast or an adopting
# literal (723), and a fixed value flowing through an `if`/`match` yield (719/720/721).
709_fixed_int8_local.cf
710_fixed_uint8_cast.cf
711_fixed_int8_narrow.cf
712_fixed_int16_cast.cf
713_fixed_uint16_literal.cf
714_fixed_int32_cast.cf
715_fixed_int64_local.cf
716_fixed_uint64_cast.cf
718_fixed_param_return.cf
719_fixed_if_yields.cf
720_fixed_if_yields_int64.cf
721_fixed_match_yields.cf
723_fixed_let_reassign.cf
724_fixed_all_eight.cf
725_error_int8_literal_range.cf
726_error_uint8_literal_range.cf
731_error_fixed_arg_mismatch.cf
732_error_fixed_type_mix.cf
733_error_fixed_return_mismatch.cf
734_error_uint8_negative_literal.cf

# Fixed-width integer tower, brick 2 — arithmetic, shifts, and comparison over the fixed
# leaves. An operator needs both operands the SAME fixed type (or one an adopting literal,
# 749); the result wraps to the type's width (`+`/`-`/`*`/`<<` — 727/736/746) via the same
# shl+sar/shr canonicalization; division/remainder/right-shift/comparison pick the SIGNED
# (`div`/`rem`/`sar`/`cslt…`) or UNSIGNED (`udiv`/`urem`/`shr`/`cult…`) variant by the type's
# signedness (737/738 div, 742 rem, 740/741 shift, 728/739/747 compare); `~` wraps (743) and
# unary `-` works on a signed value (744) but is rejected on an unsigned one (751); compound
# assignment on a fixed `let` (745); mixed-width (748) and word-vs-fixed (732) rejected.
727_fixed_arith_wrap.cf
728_fixed_cmp_unsigned.cf
736_fixed_signed_wrap.cf
737_fixed_udiv.cf
738_fixed_sdiv_negative.cf
739_fixed_signed_cmp.cf
740_fixed_ushr.cf
741_fixed_sar.cf
742_fixed_rem.cf
743_fixed_bitnot.cf
744_fixed_neg.cf
745_fixed_compound_assign.cf
746_fixed_int64_arith.cf
747_fixed_uint64_cmp.cf
748_error_fixed_mixed_width.cf
751_error_neg_unsigned.cf

# Fixed-width tower, tail — a fixed-width record FIELD initialized by a bare integer literal,
# which ADOPTS the field's type (range-checked, like a `let`): `data P = { Int8 x }`, `P({x:7})`,
# read via `Iarch(p.x)` (730). Plus a bare literal adopting a fixed OPERAND's type (749), and a
# fixed value rejected as an `if` condition — no truthiness (750).
730_fixed_field.cf
749_fixed_plus_int_literal.cf
750_error_fixed_condition.cf

# A pointer may not point to a scalar — `*Int8`/`*Iarch`/… is `*Scalar` (§6.4); only a
# record or union pointer (or a `*[Uint8]` byte pointer) is valid.
729_error_ptr_to_fixed.cf

# Value-yielding loops — a `loop { … }` in EXPRESSION position is a value: `<- v` yields
# `v` and breaks (a break-with-value). Accumulate-then-yield (459), the loop as an operand
# and a direct return (460), `continue` steering it (461), a bare `<- v` once (462), a
# guarded ladder (463), nested value loops feeding an outer yield (464), a value loop inside
# a statement loop (465), and no stack leak over 50000 iterations (466, merge slot hoisted);
# yielding a `Uarch` (707), a fixed-width value (722), and an `if`-value inside the loop
# (976). Rejections: a bare `break` in a value loop (467/468), `<-` outside any loop (469),
# in a statement `loop` (470) or a `for` (471), a value loop that never reaches `<-` (472),
# an unreachable statement after `<- v` (473), and a non-word (record) yield (474).
459_loop_value_sum.cf
460_loop_value_return.cf
461_loop_value_continue.cf
462_loop_value_once.cf
463_loop_value_guarded_ladder.cf
464_loop_value_nested.cf
465_loop_value_in_stmt_loop.cf
466_loop_value_no_leak.cf
467_error_loop_value_bare_break.cf
468_error_loop_value_guard_break.cf
469_error_yield_outside_loop.cf
470_error_yield_in_stmt_loop.cf
471_error_yield_in_for.cf
472_error_loop_value_no_yield.cf
473_error_loop_value_unreachable.cf
474_error_loop_value_record.cf
707_loop_yields_uarch.cf
722_fixed_loop_yields.cf
976_if_yield_in_value_loop.cf

# Floating point — `Float64` (IEEE double, QBE `d`) and `Float32` (single, `s`), a strict
# scalar type class distinct from the integers: no int/float mixing and no adoption (659/662/
# 671/672 reject). A `.`-literal is Float64 (kept as text, handed to QBE as `d_...`); `Float32`
# is always an explicit cast. Arithmetic `+ - * /` and comparison, locals + `let` reassign,
# params + returns, unary `-`, `_` digit separators, a huge integer part; casts across
# int/float/float; `%` rejected on floats (660), a float exit code rejected (661); floats
# flow through an `if`/`match`/`loop` value merge (703/705/706), each yielding `d`/`s`.
649_float_literal_arith.cf
650_float_mul_div.cf
651_float_local.cf
652_float_param_return.cf
653_float_compare.cf
654_float_int_roundtrip.cf
655_float_let_reassign.cf
656_float_neg.cf
657_float_separators.cf
658_float_uarch_cast.cf
659_error_float_mixed.cf
660_error_float_rem.cf
661_error_float_as_exit.cf
662_error_float_int_assign.cf
663_float_large_int_part.cf
664_float32_cast_arith.cf
665_float32_local.cf
666_float32_param_return.cf
667_float32_compare.cf
668_float32_from_int.cf
669_float32_float64_roundtrip.cf
670_float32_let_reassign.cf
671_error_float32_mixed_width.cf
672_error_float32_int_assign.cf
703_if_yields_float64.cf
705_match_yields_float64.cf
706_loop_yields_float64.cf
708_error_if_branch_type_mismatch.cf
717_fixed_int_float_int.cf

# Generics G1 — monomorphic generic FUNCTIONS with explicit type arguments. A generic
# function `id = ['T]('T x): 'T -> x` is a template; the monomorphization pass clones it per
# distinct type-argument set into a concrete instance (`id[Iarch]` → `id__Iarch`) and rewrites
# each call site. Identity across types (223), a generic reading a field of its argument (224),
# a generic calling another generic — transitive instantiation (225), instances deduped (226),
# a generic call as a statement (232). Errors: too many type args (228), type args on a
# non-generic function (229), an undeclared type variable (230), a per-instance body type error
# `x + 1` on a record (231).
223_generic_id.cf
224_generic_field.cf
225_generic_transitive.cf
226_generic_dedup.cf
232_generic_call_statement.cf
228_error_generic_argcount.cf
229_error_typeargs_nongeneric.cf
230_error_undeclared_tyvar.cf
231_error_generic_bad_instantiation.cf

# Generics G2 — type-argument INFERENCE. A generic call may omit its type arguments
# (`id(40)` instead of `id[Iarch](40)`); the monomorphization pass infers them from the
# argument types via a threaded local type environment (parameters + `let` bindings): a
# parameter whose declared type IS a type variable fixes it to the argument's type. Inference
# from a literal, a local variable of known type, a record, and a tag-only union member (227);
# inference through a forwarding generic (`wrap(x)` → `id(x)`, 233). Error: a type variable no
# parameter mentions cannot be inferred (234).
227_generic_inference.cf
233_generic_inference_forward.cf
234_error_infer_unfixed.cf

# Generics G3a — generic DATA (record) types. `data Pair['A, 'B] = { 'A a, 'B b }` is a
# template; the monomorph pass clones it per distinct type-argument set (`Pair[Iarch, Iarch]`
# → a concrete record `Pair$Iarch$Iarch`) and rewrites every type reference (param/return/local
# annotation, record literal). A type application is parsed as a canonical bracketed string
# and mangled by monomorph, recursing through nested applications (`Box[Pair[Iarch, Iarch]]`,
# 258). A generic record field (253), a generic fn returning a generic type (255 explicit / 256
# inferred), a generic-record field holding a record (257), and two instantiations of one
# template coexisting (260). Error: an undeclared type variable in a generic field (264).
253_generic_data_pair.cf
255_generic_fn_generic_type.cf
256_generic_type_inference.cf
257_generic_aggregate_arg.cf
258_generic_nested_constructor.cf
260_generic_two_instantiations.cf
264_error_generic_undeclared_tyvar.cf

# Generics G3b — generic UNION types. `union Maybe['V] = { Just('V), Nothing }` is a template;
# the monomorph pass clones it per type-argument set (`Maybe[Iarch]` → a concrete union
# `Maybe$Iarch`), mangling each member payload type. A generic union member is constructed
# `Maybe[Iarch].Just(42)` (parsed as a type-application member); a `match` arm names the union
# unqualified (`Maybe.Just`) and is rewritten to the scrutinee's concrete instance. Maybe (252),
# a RECURSIVE `List['T]` referencing itself (254, terminating via the mark-before-recurse
# guard), Either with two type params (259), a List of a record (261). Errors: wrong type-arg
# arity (262), a generic union used without type arguments (263).
252_generic_union_maybe.cf
254_generic_recursive_list.cf
259_generic_either.cf
261_generic_list_of_aggregate.cf
262_error_generic_arity.cf
263_error_generic_no_args.cf
553_generic_record_tuple_field.cf
590_generic_tuple_field.cf
593_generic_tuple_union_payload.cf
598_generic_union_tuple_payload_match.cf
599_generic_union_tuple_payload_unit.cf
# generic type-argument inference from non-trivial argument forms (shared type_of route):
# a field read (1118), match/for/destructure binders (1119-1121), a nested (incl generic-in-
# generic) call result (1122), and a generic union matched on a call result (1123).
1118_infer_from_field.cf
1119_infer_from_match_binder.cf
1120_infer_from_for_binder.cf
1121_infer_from_destructure.cf
1122_infer_from_nested_call.cf
1123_match_on_call_result.cf
1124_infer_for_binder_nonword.cf
# a type variable inferred to and instantiated at an ARRAY type (arrays as generic types):
# id over [Iarch]/[Record] (1125-1126), an array + a scalar in one call (1127), plus a
# nested array-of-array via a type variable (1128).
1125_infer_array_typevar.cf
1126_infer_array_of_record.cf
1127_infer_array_and_scalar.cf
1128_infer_array_typevar_nested.cf
1129_array_typevar_union.cf
# STRUCTURAL inference — a type variable read out of where it SITS in a parameter's type, not just
# the whole parameter: a `['T]` array element (1137), nested `[['T]]` (1138), flowing to a `['T]`
# return (1139), or a `('T, …)` tuple position (1140) including at a record argument (1141). Before,
# `infer_tyargs` matched only a parameter whose whole type was the variable, so these needed an
# explicit `[…]` type argument.
1137_infer_array_param.cf
1138_infer_array_param_nested.cf
1139_infer_array_param_return.cf
1140_infer_tuple_param.cf
1141_infer_tuple_param_record.cf
1142_infer_generic_hof_fn_arg.cf
# §8.2 union→superunion subtyping in a generic bound: a sub-union satisfies a bound naming its
# super-union (`Small <: Big` when `Big` splices `Small` by spread). Accepted for a fn bound with
# 'T threaded through the return (1143) and a generic data-decl bound (1144). The relation is a
# SUBSET, not overlap: a super-union (1145) or a partially-overlapping union (1146) is rejected.
# Before, bounds were reflexive only (`arg == bound`); reflexive/wrong-union stay 265-275.
1143_bound_subunion_spread.cf
1144_bound_subunion_decl.cf
1145_error_bound_superunion.cf
1146_error_bound_partial_overlap.cf
1147_error_bound_name_collision.cf
# generic BOUNDS `[Color 'T]` — a type parameter constrained to a union (type_system §8.5):
# bounded fn explicit/inferred (265/266), payload-union bound (267), bounds on generic data/union
# decls (271/275). Errors: wrong union (268), non-union arg (269), non-union bound (270), type
# bound violated (272), bound without a type variable (273), arithmetic on a bounded 'T (274).
265_generic_bound_fn.cf
266_generic_bound_inferred.cf
267_generic_bound_payload.cf
268_error_bound_wrong_union.cf
269_error_bound_int_arg.cf
270_error_bound_not_union.cf
271_generic_bound_type.cf
272_error_bound_type_violated.cf
273_error_bound_no_tyvar.cf
274_error_bound_not_narrowed.cf
275_generic_bound_union_decl.cf
# comptime VALUE parameters `[Iarch n]` (type_system §9.1) — a type-param slot carrying a
# compile-time integer that folds into the body: value fold (276), arithmetic (277), mixed with
# a type param (278), two instances (279), a value-param-sized buffer (280), Uarch (281). Errors:
# non-Iarch/Uarch value type (282), value param not inferable (283), type/value slot swaps (284/285).
276_valueparam_value.cf
277_valueparam_arith.cf
278_valueparam_mixed.cf
279_valueparam_two_instances.cf
280_valueparam_bufsize.cf
281_valueparam_uarch.cf
282_error_valueparam_bad_type.cf
283_error_valueparam_infer.cf
284_error_valueparam_type_in_value_slot.cf
285_error_valueparam_value_in_type_slot.cf
1130_valueparam_shadow.cf
# generic TUPLES — tuple type args including Unit (588/589), tuple fields with a Unit/record
# element (591/592), tuple params/returns with generic elements (594-597).
588_unit_type_arg_record.cf
589_unit_type_arg_function.cf
591_generic_tuple_field_unit.cf
592_generic_tuple_field_record.cf
594_generic_tuple_param.cf
595_generic_tuple_return.cf
596_generic_tuple_return_unit.cf
597_generic_tuple_param_record.cf
# comptime `type` aliases (type_system §7, token erasure) — scalar (602), record (603), Uarch
# (604), tuple (605), generic application (606), alias-of-alias (607), forward use (608), union
# (609); a generic `type` alias is rejected (612). A function-type alias (610) needs HOF params.
602_type_alias_scalar.cf
603_type_alias_record.cf
604_type_alias_uarch.cf
605_type_alias_tuple.cf
606_type_alias_generic_app.cf
607_type_alias_of_alias.cf
608_type_alias_forward.cf
609_type_alias_union.cf
612_error_type_alias_generic.cf
# a match arm may qualify by the union's exact INSTANCE (`U[Iarch].Pair`), interchangeable with
# the base name; a wrong instance is rejected (601).
600_generic_union_match_instance_qual.cf
601_error_match_arm_wrong_instance.cf
# Declaration spread: `data Ext = { ...Src, … }` splices record `Src`'s fields in place
# (basic, spread-only, several spreads, order-preserving, an aggregate field carried, and
# crossing a function boundary), with the rejections — an undeclared source (370), a union
# source into a record (371), and a name collision (372).
364_spread_decl_basic.cf
365_spread_only.cf
366_spread_multiple.cf
367_spread_order.cf
368_spread_aggregate_field.cf
369_spread_crosses_boundary.cf
370_error_spread_undeclared.cf
371_error_spread_union.cf
372_error_spread_duplicate.cf
# Union member spread: `union Big = { ...Src, … }` splices union `Src`'s members (basic
# tag-only, payload members bound in a match, order-preserving, several spreads, spread-only,
# a spliced payload that boxes the union, an aggregate payload, and a generic ENCLOSING union
# spreading a concrete source — the spliced members monomorphize, 397), with the rejections —
# an undeclared source (398), a record source into a union (399), and a name collision (400).
390_member_spread_basic.cf
391_member_spread_payload.cf
392_member_spread_order.cf
393_member_spread_multiple.cf
394_member_spread_only.cf
395_member_spread_boxes_union.cf
396_member_spread_aggregate_payload.cf
397_member_spread_generic_enclosing.cf
398_error_member_spread_undeclared.cf
399_error_member_spread_record.cf
400_error_member_spread_duplicate.cf
# generic SPREAD is rejected — a generic record/union template (373/401) or a generic
# application `...Name[...]` (374/402) cannot be spread into a declaration.
373_error_spread_generic_template.cf
374_error_spread_generic_application.cf
401_error_member_spread_generic_template.cf
402_error_member_spread_generic_application.cf
# Higher-order functions (v1 — a top-level function passed by NAME, called indirectly). A
# `(A, …) -> R` PARAMETER type carries a function value: `apply(inc, x)` (347), applied twice
# (348), a binary callee (349), a function-value parameter threaded on to another HOF (351),
# and a `Uarch`/`Float64`/`Float32` callee riding the right registers (673-675). A function
# type alias `type IntFn = (Iarch) -> Iarch` used as the parameter (610), and a tuple parameter
# coexisting with a function parameter — the `->` disambiguates (523). Rejections: an arity
# mismatch (353), a signature mismatch (354, `Uarch` return where `Iarch` is expected), a
# function reference used as a plain value (355), and a function type missing its `->` (358).
# (Closures — a local lambda or a capturing closure passed to a HOF — are a later brick.)
347_hof_apply.cf
348_hof_twice.cf
349_hof_binary.cf
350_hof_closure_arg.cf
351_hof_threading.cf
353_error_hof_arity.cf
354_error_hof_non_int.cf
355_error_hof_fn_as_value.cf
358_error_fntype_no_arrow.cf
# A capture-free local closure is a valid function VALUE — it has a plain code address, so it may
# be passed to a HOF like a top-level function (350); the lift rewrites the reference to the
# closure's lifted top-level function. A CAPTURING closure passed to a HOF is handled by INLINING
# the (single-expression) HOF at the call site with the closure substituted for the function
# parameter (the fake-closure model, memory_model §7): apply(addbase, 5) → addbase(5) → the lifted
# body with `base` threaded (352). Capture is by reference, so calls through the HOF mutate the
# enclosing variable (359) or a captured record (362); one HOF serves a top-level function and a
# closure both (360); and a closure threaded through a chain of HOFs still reduces to a direct call
# (361, relay → apply). An arity mismatch is rejected at the call site (363).
352_hof_capturing_closure.cf
359_hof_closure_mutate.cf
360_hof_mixed.cf
361_hof_transitive.cf
362_hof_record_capture.cf
363_error_hof_closure_arity.cf
1134_hof_closure_field_arg.cf
1135_error_hof_closure_recursive.cf
1136_error_hof_closure_dup_arg.cf
523_tuple_param_with_fn_param.cf
610_type_alias_fn_type.cf
673_fntype_uarch.cf
674_fntype_float64.cf
675_fntype_float32.cf
# The `|>` pipe (a parse-time desugar): `x |> f` is `f(x)` and `x |> f(a)` is `f(a, x)` —
# the piped value fills the callee's LAST positional slot. It is the loosest operator, so
# `1 + 2 |> inc` is `inc(1 + 2)` (304), and left-associative, so a chain reads left to right
# (302); a chain may wrap onto a leading-`|>` line (303). The target is a lowercase function
# name (300, 301, 305), optionally a local closure (335). Rejections: a PascalCase type target
# (308), an under-applied callee once the pipe arg counts toward arity (309), and a non-name
# target (307). (The `defer`-tap pipe form `|> defer f` is a later brick.)
300_pipe_basic.cf
301_pipe_under_applied.cf
302_pipe_chain.cf
303_pipe_multiline.cf
304_pipe_precedence.cf
305_pipe_arg_expr.cf
307_error_pipe_nonfunction.cf
308_error_pipe_type_target.cf
309_error_pipe_arity.cf
335_closure_pipe.cf
# `defer` (memory_model §6): schedules a call `defer f(x)` or a block `defer { … }` to run at
# scope exit, LIFO (310, 313), AFTER the return value is fixed — a word return snapshots (311)
# while a returned arena record observes the post-defer mutation (310, 342). A defer before a
# loop still fires at the return after it (314) or at a return inside it (315); it fires on an
# early return in an `if` branch too (972). The call form discards its result (312), and may
# call a local closure (336, 342). The defer.cf pass lowers these before emit. Rejections: a
# defer nested in a loop (316) or in another defer (317), a bare non-call/non-block `defer 5`
# (318) / bare name `defer f` (319), and a `return` inside a defer body (320).
310_defer_block_lifo.cf
311_defer_after_return_value.cf
312_defer_call_form.cf
313_defer_multiple_lifo.cf
314_defer_loop_return.cf
315_defer_return_in_loop.cf
316_error_defer_in_loop.cf
317_error_defer_nested.cf
318_error_defer_noncall.cf
319_error_defer_bare_name.cf
320_error_return_in_defer.cf
336_closure_defer.cf
342_closure_record_defer.cf
972_if_defer_early_return.cf
1131_defer_block_scope_reuse.cf
# `defer`-tap VALUES: `defer f(x)` in value position (322) — and its pipe form `x |> defer f`
# (306, 323) — both schedule f(x) at scope exit AND evaluate to the tapped last argument x. The
# tap is snapshotted once at the defer point, so `n |> defer tap` keeps n's value even after n is
# reassigned (323). A returned arena flows to its binding while its destroy is queued (321); a tap
# may sit mid-chain and forward its value (324). hoist_defertap.cf lifts these to a snapshot +
# scheduled defer before the defer lowering. Rejections: a `|> defer { … }` block taps nothing
# (325), a no-arg `defer f()` value has no tap (326), and a pipe-tap in a loop is not top-level (327).
306_pipe_defer_tap.cf
321_defer_pipe_arena.cf
322_defer_tap_value.cf
323_defer_tap_snapshot.cf
324_defer_pipe_midchain.cf
325_error_defer_pipe_block.cf
326_error_defer_value_noarg.cf
327_error_defer_pipe_in_loop.cf
1132_defer_tap_generic_callee.cf
1133_error_defer_tap_in_expr.cf
# Closures v1 (memory_model §7): a local `const f = (params) -> body` captures enclosing
# locals/parameters BY REFERENCE, lowered to a top-level function with the captures as hidden
# parameters (a scalar by its slot address, a record/`Str` by its pointer). A captured word is
# mutated and the enclosing scope observes it (328), a no-param closure (329), multiple captures
# read+mutated (330), a captured parameter (331), a single-expression body (332), a body calling
# a top-level function with a captured value (333), two closures sharing one variable (334), a
# captured record field mutated (337), a record + word captured together (343), a `const` record
# read (344), and a `Str` read (346). Rejections: a closure nested in a closure (338), a closure
# used as a plain value (339), a `let`-bound closure (340), and mutating a captured `const`
# scalar (341) or `const` record field (345). (defer, `|>` pipe, and passing a closure to a HOF
# are later bricks.)
328_closure_capture_mutate.cf
329_closure_no_param.cf
330_closure_multi_capture.cf
331_closure_capture_param.cf
332_closure_single_expr.cf
333_closure_calls_toplevel.cf
334_closure_two_share.cf
337_closure_capture_record.cf
343_closure_mixed_capture.cf
344_closure_const_record_read.cf
346_closure_capture_str.cf
338_error_closure_nested.cf
339_error_closure_as_value.cf
340_error_closure_let.cf
341_error_closure_mutate_const.cf
345_error_closure_mutate_const_record.cf
# EXPLICIT pointers to aggregates `*Record`/`*Union` (type_system §6.4) — the spelled-out form of
# cf0's already-by-pointer arena aggregates, so `*Point` is representationally identical to a bare
# `Point`. A `*Rec` parameter/return/field (686/688), a `*Point` field built with `&Constructor`
# (249), `&` of a local vs a constructor (1085), a `*Box` boxed-union pointer with match on the
# pointer + `&Box.Val(x)` (695), a `*[Uint8]` byte pointer as a param (859) and a record field
# (870), and field mutation through a `*Record` (872). Errors: a pointer to a scalar `*Iarch`/`*Str`
# (689/690/758) and `&` of a scalar (699). Recursive `*Node`/union-payload pointers and writable
# array borrows are later bricks.
249_ptr_field.cf
686_ptr_record_param_return.cf
688_ptr_passthrough.cf
695_ptr_union_param_match.cf
859_ptr_param_read.cf
860_ptr_param_write.cf
870_record_ptr_field_lexer.cf
872_field_assign_through_ptr.cf
1085_ptr_interconvert.cf
# a `*[Iarch]` WORD-array pointer record FIELD — the token-stream-bundled-in-a-record pattern. The
# field's element rides in `felem` so `t.cells[i]` strides by its width (a `*[Uint8]` field stays
# byte-packed, a `*[Iarch]` strides 8); a record literal stores an array field value's element base
# (data@0), like a `*[T]` argument (871). Errors: a `*[Str]`/non-scalar pointer-field element (874),
# and a field value that is neither a raw pointer nor a named array (875).
871_record_iarch_ptr_field.cf
874_error_bad_ptr_field.cf
875_error_ptr_field_wrong_value.cf
689_error_ptr_scalar_int.cf
690_error_ptr_scalar_str.cf
758_error_ptr_to_iarch.cf
699_error_addr_of_scalar.cf
1148_error_ptr_str_field.cf
1149_error_ptr_str_return.cf
# RECURSIVE / AST-shaped unions (type_system §8.4) — a union member payload holding a pointer to
# an aggregate: a self-referential `*List` linked list summed by recursion (694), a `*Point` record
# pointer as a payload (696), and the generic analogue `*List['T]` where concretization carries the
# pointee through (698). cf0's unions are already by-pointer, so a bare self-payload already worked;
# the star is elided to the same repr. Errors: a pointer to an all-tag-only union `*Maybe`/`*Flag`
# is a `*Scalar` (it lowers to an int tag) and is rejected (691/697); a boxed `*Box` stays legal (695).
694_ptr_recursive_list.cf
696_ptr_record_in_union_payload.cf
698_ptr_generic_recursive_list.cf
691_error_ptr_union_deferred.cf
697_error_ptr_tagonly_union.cf
# COMPOSE-OVER members (type_system §8.1 resolve-or-create): a bare union member whose name denotes
# a declared record or union composes over it — the member carries that type as its payload (the
# value IS the type, §8.3), reached as `U.Name(v)` / `match { U.Name(v) -> … }` on the existing
# payload path (a resolve-time desugar to a `Name(Name)` member). A name denoting nothing stays a
# nullary tag. Composing over a record (1150) or a union (1151); composing over a builtin scalar is
# deferred (209 — `union U = { Iarch, Nil }` rejected). Deliberately scope-sensitive (§8.1).
1150_compose_over_record.cf
1151_compose_over_union.cf
209_error_union_builtin_member.cf
# CHARACTER literals `'A'` / `'\n'` — a single byte, lexed straight to an integer token (the byte
# value), distinguished from a type variable `'T` by the closing apostrophe. The byte value (982),
# comparison against a Str byte with no cast (983), and the scan-for-a-char lexer pattern (985);
# plus a bare fixed-width operand out of range is rejected (986). Escapes: \n \t \r \0 \\ \'.
982_char_literals.cf
983_char_vs_str_byte.cf
985_char_in_buffer_scan.cf
1152_char_escapes.cf
986_error_char_out_of_range.cf
# MEMBER-TYPE annotations (type_system §8.1): a union member used as a TYPE (`Color.Red`,
# `Maybe[Iarch].Just`) denotes its union — cf0 collapses it to that union (a member value already
# widens to its union). A binding annotation (681), a parameter (682), and a return type (683);
# the parser preserves `Base.Member`, and a resolve pass (before monomorph) validates + collapses.
# Errors: a non-member of the union (684) and a record base, which has no members (685).
681_member_type_local.cf
682_member_type_param.cf
683_member_type_return.cf
684_error_member_type_unknown.cf
685_error_member_type_record_base.cf
# `Bool.True` / `Bool.False` as VALUE expressions — the explicit qualified form of the `true`/`false`
# literal sugar (type_system §8.6: `Bool` is the union `{ False, True }`, lowered to a 1-byte int). A
# parse-time desugar to the Bool literal (822/1153); any other `Bool.X` member is rejected (1154).
822_bool_explicit_member.cf
1153_bool_false_member.cf
1154_error_bool_bad_member.cf
# `match` on a Bool (type_system §8.6): cf0 treats a Bool scrutinee as the synthetic tag-only union
# `{ False, True }` (members from `bool_members()`, NOT injected into the tables) and dispatches on its
# tag (False=0/True=1) —
# an expression match (818), a statement match for effect (1155), with the same exhaustiveness rules
# (a partial match without `_` is rejected, 1156). `Bool` stays the primitive `TBool` for values and
# annotations. A user redeclaring the builtin `union Bool` is rejected (830).
818_bool_match.cf
1155_bool_match_stmt.cf
1156_error_bool_match_partial.cf
830_error_user_bool_union.cf
1157_error_bool_match_payload.cf
# explicit pointer LOCALS `const *Point p = &x` (type_system §6.4) — the local-binding form of the
# already-by-pointer aggregate: built with `&`, passed to a `*Point`/by-value `Point` param, read by
# field (692). Constrained: a `*` binding must be `const` (repointing a `let` pointer is a later
# brick, 701) and its initializer must be an address `&x`, not a bare aggregate value (702).
692_ptr_local.cf
701_error_ptr_local_let.cf
702_error_ptr_local_needs_addr.cf
# fixed-size arrays `[N Elem]` in aggregates (already supported; adding coverage): fixed-width
# elements as record fields (890) with mutation (891), a fixed array as a union payload (892),
# a fixed-array literal of fixed-width elements (907), captured by a closure (932), and a bare
# literal adopting a fixed element width (984). Errors: a `[N]`-without-element field (1030) and
# a fixed field built from a wrong-typed value (893).
890_fixed_record_fields.cf
891_fixed_field_mutate.cf
892_fixed_union_payload.cf
907_array_fixed_elements.cf
908_array_fixed_assign.cf
932_closure_capture_fixed.cf
984_literal_adopt_fixed.cf
893_error_fixed_field_type.cf
1030_error_fixed_array_field.cf
# top-level VALUE consts `const NAME = <literal>` (module_system) — a module-level constant modeled
# as a nullary function auto-called at each bare reference. Its return type is inferred from the
# literal: an Int (1003), a Str read via `.len` (1004), a Bool driving an `if` (1005), and a
# call-valued const with dead-code elimination (1006). Errors: an aggregate value (1008) and a
# generic value const (1009). A value const's `.len`/field auto-calls it (typecheck + emit).
1003_toplevel_const_int.cf
1004_toplevel_const_str.cf
1005_toplevel_const_bool.cf
1006_toplevel_const_call_and_dce.cf
1008_error_toplevel_const_aggregate.cf
1009_error_toplevel_const_generic.cf
# integer literal BASES (ebnf § Numbers) — hex `0xff`/`0xDE_AD` (either digit case, 637/638/645),
# octal `0o17` (639), binary `0b101`/`0b…` (640/646), mixed bases in an expr (641), a zero (642),
# and `_` digit separators BETWEEN digits (decimal 644). Errors: an empty base `0x` (643), and a
# trailing (`1_`, 647) or doubled (`1__0`, 648) separator, which leaves a stray `_` the parser rejects.
637_int_hex.cf
638_int_hex_upper.cf
639_int_octal.cf
640_int_binary.cf
641_int_bases_mixed.cf
642_int_hex_zero.cf
643_error_int_empty_base.cf
644_int_separator_decimal.cf
645_int_separator_hex.cf
646_int_separator_binary.cf
647_error_int_trailing_separator.cf
648_error_int_doubled_separator.cf
# STRING `match` — dispatch a `Str` scrutinee against constant string-literal patterns (keyword
# dispatch): or-patterns `"mul" | "imul"` and a required `_` default (962), an empty-string arm
# (963), arms that yield a `Str` (964). Compiled as a `cf_str_eq` compare-chain (no tag). Errors: a
# missing `_` — never exhaustive (965), a duplicate pattern (966), a non-`Str` scrutinee (967), and
# an interpolated pattern (968). Patterns ride in the arm's `members` under type_name "$str".
962_str_match_dispatch.cf
963_str_match_empty_and_default.cf
964_str_match_yields_str.cf
965_error_str_match_no_wild.cf
966_error_str_match_duplicate.cf
967_error_str_match_non_str.cf
968_error_str_match_interp_pattern.cf
# INTEGER-literal `match` — equality dispatch on a word/fixed-width scrutinee, the value analog of a
# string match: single literals + an or-pattern `1 | 2 | 3` + a `_` default (920), a negative literal
# and an `Int8` scrutinee (921), arms that yield a record (922) or a `Str` (923). Compiled as a `ceql`
# compare-chain (no tag). Errors: a duplicate literal (924), a missing `_` — never exhaustive (925),
# and mixing a union-member arm with integer literals (926). Patterns ride in `members` (normalized to
# decimal) under type_name "$int". (An integer SUB-pattern in a union payload — 995/999 — is a follow-up.)
920_match_int_literal.cf
921_match_int_negative.cf
922_match_int_record.cf
923_match_int_str.cf
924_error_match_int_dup.cf
925_error_match_int_no_wildcard.cf
926_error_match_int_mixed.cf
# NESTED payload sub-patterns — a match arm constrains a union member's payload beyond the outer tag:
# a member GUARD on a union-typed field (`Expr.Bin(Add, l, r)`, 994), an INTEGER guard (`M.A(0)`, 995),
# a guard on a BOXED field union (996), a mix of guard+bind+`_` (997), and a bare-or-qualified owner-
# ruled guard (`Op.Add`/`Sub`, 1014). A guarded arm only PARTIALLY covers its member: 995 needs no `_`
# (a later unguarded `M.A(n)` fully covers A). Errors: a deep sub-pattern — one level only (998), an
# integer guard on a non-int field (999), a member guard on a non-union field (1000), non-exhaustive
# when a member is only guard-covered (1001), an unreachable guard after a full cover (1002), a wrong-
# union qualifier (1015). Guards ride in the arm's `binds` (`=Member`/`#int`), skipped when binding.
994_nested_match_member_guard.cf
995_nested_match_int_guard.cf
996_nested_match_boxed_field.cf
997_nested_match_mixed.cf
1014_nested_match_qualified.cf
998_error_nested_match_deep.cf
999_error_nested_match_int_nonint.cf
1000_error_nested_match_member_nonunion.cf
1001_error_nested_match_nonexhaustive.cf
1002_error_nested_match_unreachable.cf
1015_error_nested_match_wrong_qual.cf
# plain dynamic array `[T]` basics — an array literal with a `[N Elem]` annotation, `.len`, runtime
# indexing (405), index precedence (406), a loop-sum (407), expression elements (408), index-of-index
# (409), a `let` array (410). Errors: element assign to a `const`/borrowed array (411), a fixed-array
# literal of the wrong length (412/413), indexing a non-array (414), an UNANNOTATED array literal
# binding (415), a bad array field (416), an empty `[]` without an inferrable annotation (417/418).
403_array_basic.cf
404_array_len.cf
405_array_runtime_index.cf
406_array_index_precedence.cf
407_array_loop_sum.cf
408_array_expr_elements.cf
409_array_index_of_index.cf
410_let_array.cf
411_error_array_index_assign.cf
412_error_array_len_short.cf
413_error_array_len_long.cf
414_error_index_non_array.cf
415_error_array_unannotated.cf
416_error_array_bad_field.cf
417_error_array_empty.cf
418_error_array_spread.cf
# Uarch (unsigned pointer-width) as an operable integer: locals + unsigned div/rem (885), a `let`
# accumulator mutated in a loop (886), Uarch flowing param->compute->if-value (887), and Uarch
# record fields read/arith through a `*Region` pointer (888). (cf0 tracks Uarch as the register
# word like Iarch, so an un-cast `Uarch + Iarch` mix — test 889 — is NOT yet rejected: making Uarch
# a distinct unsigned type conflicts with its word use in comptime value params + syscall args.)
885_uarch_locals_arith.cf
886_uarch_let_loop.cf
887_uarch_param_compute.cf
888_uarch_record_field.cf
# value-level record SPREAD `{ ...src, field: v }` — an immutable update: copy a record variable's
# fields, then apply overrides. A single-field update (377), a bare copy (378), a genuine copy that
# doesn't alias the source (379), multi-override (380), a copied tag-only-union field (381), an
# overridden aggregate field (382), a `let` source (383). Errors: spread not first (384), two
# spreads (385), a wrong-type source (386), a non-variable source (387), a COPIED aggregate field
# that would alias (388), and a field overridden twice (389).
377_value_spread_update.cf
378_value_spread_bare_copy.cf
379_value_spread_is_copy.cf
380_value_spread_multi_override.cf
381_value_spread_union_field.cf
382_value_spread_override_aggregate.cf
383_value_spread_from_let.cf
384_error_value_spread_not_first.cf
385_error_value_spread_two.cf
386_error_value_spread_wrong_type.cf
387_error_value_spread_not_var.cf
388_error_value_spread_copied_aggregate.cf
389_error_value_spread_override_twice.cf
# inline NESTED record literals — a bare `{ … }` as a FIELD VALUE, typed by the field's declared
# record type (a `Record` with an empty type_name, resolved by typecheck + emit): a one-level nest
# (977), nesting to any depth (978), and a mix of a nested literal and a constructor-call field
# (980). Error: a bare `{…}` on a non-record field (981). (A bare literal in RETURN position — `({…})`
# — is deferred: it needs the return type threaded through emit.)
977_nested_record_literal.cf
978_nested_record_deep.cf
980_nested_record_mixed.cf
981_error_record_literal_wrong_field.cf
# a bare `({…})` record literal in RETURN / BINDING position, typed by the CONTEXT (the return
# annotation or the binding annotation) rather than a field: a value-body `-> ({…})` (431),
# aggregate fields from calls (432), a fresh value per return (433), `return ({…})` in a block
# (434), arbitrary field expressions (435), and an annotated binding `const Point p = ({…})`
# (441). Errors: an unknown/missing field (436/440), a record literal for a non-record return
# (437), and an unannotated `const x = ({…})` with no record context (438).
431_record_literal_body.cf
432_record_literal_nested.cf
433_record_literal_fresh.cf
434_record_literal_in_block.cf
435_record_literal_expr_fields.cf
441_record_literal_paren_binding.cf
436_error_record_literal_bad_field.cf
437_error_record_literal_int_return.cf
438_error_record_literal_no_context.cf
440_error_record_literal_missing_field.cf
# grouped-parameter `type X = { … }` (type_system §7) — record-shaped parameter bundles. A `(X g)`
# parameter SPLATS into the group's fields (used bare in the body), and a `{ … }` call argument
# expands to positional field values in the group's declared order (resolve desugar, consumed before
# typecheck). Basic (618), a trailing ordinary arg (619), two groups (620), a record field (621) /
# field-order (622) / tuple field (631) group member, inside a generic fn (632), trailing comma (636).
# Errors: empty (623), duplicate field (624), a field default (625), a name colliding with a `data`
# (626), a field colliding with another param (627), a generic group (628), used as a return (629) or
# a record field (630), a positional call (633), a missing (634) or extra (635) group-literal field.
618_type_group_basic.cf
619_type_group_with_trailing.cf
620_type_group_two_groups.cf
621_type_group_record_field.cf
622_type_group_field_order.cf
631_type_group_tuple_field.cf
632_type_group_in_generic.cf
636_type_group_trailing_comma.cf
623_error_type_group_empty.cf
624_error_type_group_dup_field.cf
625_error_type_group_default.cf
626_error_type_group_collision.cf
627_error_type_group_field_collides_param.cf
628_error_type_group_generic.cf
629_error_type_group_as_return.cf
630_error_type_group_as_field.cf
633_error_type_group_positional_call.cf
634_error_type_group_missing_field.cf
635_error_type_group_extra_field.cf
1158_error_type_group_spread.cf
1159_error_type_group_union_collision.cf
# `[T]` dynamic-array record FIELDS and union payloads — a `[Token]`/`[Iarch]`/`[Str]` field read
# by index + `.len`, threaded by `*Ctx` (1024), read/for/pass-through (1025), a `[Str]` field (1026),
# a `[T]` union payload (1029), and a `['T]` field in a GENERIC record with a `*Ctx[Iarch]` param
# (1032). Growing/reassigning a `[T]` field in place (1027/1028) is NOT rejected here — cf0's own
# monomorph relies on it (`m.type_keys = [...m.type_keys, k]` through `*MonoCtx`), so cf0 SUPPORTS
# dyn-field grow via the arena and those two error-tests diverge from cf0. Assigning a field's `.len`
# (1031) is read-only and rejected at parse.
1024_dyn_field_ctx.cf
1025_dyn_field_for_pass.cf
1026_dyn_field_str.cf
1029_dyn_union_payload_decl.cf
1032_dyn_field_generic.cf
1031_error_assign_dyn_field_len.cf
# `*[Iarch]` word-array pointer PARAMETERS — the worklist/token-stream pattern. The element
# name rides in the param's `elem` so indexing strides by its width: a `*[Uint8]` stays
# byte-packed, a `*[Iarch]` strides 8 (`loadl`/`storel`). A fixed-/dynamic-array argument passes
# its element base (data@0). Read (863), write-through (864), a 64-bit round-trip (865),
# forwarding onward (866), and inside a generic (867). A raw pointer has no comptime length, so
# `.len` (868) and `for … in` (869) are rejected. A `*[Iarch]` record FIELD (871) is a follow-up.
863_iarch_ptr_param_read.cf
864_iarch_ptr_param_write.cf
865_iarch_ptr_param_64bit.cf
866_iarch_ptr_param_chain.cf
867_iarch_ptr_param_generic.cf
868_error_iarch_ptr_len.cf
869_error_iarch_ptr_for.cf
# STATEMENT-position integer-literal matches — the tail of the integer-match brick (920-926), which
# only wired the EXPRESSION position. A standalone `match` dispatched for effect now accepts integer
# literal arms with an or-pattern and a mandatory `_` (1162); without a `_` it is never exhaustive (1165).
1162_stmt_match_int_literal.cf
1165_error_stmt_int_no_wildcard.cf
# STATEMENT-position guarded matches — the tail of the nested-payload-guard brick (994-1002), which
# only wired the EXPRESSION position. A standalone `match` dispatched for effect now honors payload
# sub-pattern GUARDS: a member guard `Bin(Add,l,r)` (1160), an integer guard `A(0)` that falls through
# to a later unguarded arm for the same tag (1161). A member only guard-covered is non-exhaustive
# (1163); a guarded arm after a full unguarded cover is unreachable (1164).
1160_stmt_match_member_guard.cf
1161_stmt_match_int_guard.cf
1163_error_stmt_guard_nonexhaustive.cf
1164_error_stmt_guard_unreachable.cf
# NON-bare-variable array arguments to a `*[T]` raw pointer — the last tail of the `*[T]` param/field
# bricks (863-869/871), which only accepted a bare-variable array. `emit_ptr_arg` lowers an array to
# its data pointer by SHAPE (via `init_agg_type`); typecheck's `is_ptr_arr_arg` accepts exactly the
# shapes emit can classify: an array LITERAL (1166), a record FIELD read (1167), a live field borrow
# written through (1168, `let`-owned so the write is permitted), a CALL result (1169), a non-variable
# array stored into a `*[T]` FIELD (1170), an INDEX into a `[[T]]` (1171), a field read on a CALL result
# (`mk().items`, 1173), and an indexed element's field (`bs[1].items`, 1174). A raw-pointer expression
# (a `$ptr` field/param) still rides through unchanged, so forwarding is unaffected. An array yielded
# opaquely by an `if`/`match`/value-`loop` is REJECTED (1172) — emit cannot classify its header, so it
# is a clean error, not a miscompile. NOTE: cf0 still defers ALL lifetime checking, so a `*[T]` field
# rehomed to outlive its target (region-outlives) and a write through a `const`-owned array are not yet
# caught here — pre-existing gaps, unchanged by this brick.
1166_ptr_param_array_literal.cf
1167_ptr_param_field_arg.cf
1168_ptr_param_field_write.cf
1169_ptr_param_call_arg.cf
1170_ptr_field_stores_field_array.cf
1171_ptr_param_nested_index.cf
1172_error_ptr_param_if_array.cf
1173_ptr_param_call_field.cf
1174_ptr_param_index_field.cf
# Field access on an `if`/`match` that YIELDS an aggregate — `init_agg_type` now recovers the
# branch/arm aggregate type (was lost to "", so a later `.field` dereferenced `recs[-1]` and cf0
# SEGFAULTED). A field read on an if-record (`(if … ).v`, 1175), a record bound from a match of outer
# variables (1176), an aggregate bound from a match PAYLOAD (`M.Some(x) -> x`, recovered from the
# member's payload type via the arm's bind env, 1177), and that payload aggregate returned from a
# function then field-accessed (1178) all work. `field_offset` keeps a `ri<0` guard as a fail-loud
# net for any remaining unresolved-aggregate form (e.g. a value-`loop` yielding an aggregate) — a
# clean error, never a crash. (Pre-existing bug, unrelated to the `*[T]` bricks; needed a two-generation
# reseed because the If-recursion also retypes cf0's own `const Str X = if …` bindings.)
1175_if_record_field.cf
1176_match_record_binding_field.cf
1177_match_payload_agg_field.cf
1178_match_payload_agg_return_field.cf
# Value-yielding `loop` producing an AGGREGATE — the merge slot is one 8-byte word, which already
# fits an arena pointer, so a record (1179), a union (1180), or a Str (1181) now flows through `<- v`
# (typecheck's scalar-only gate relaxed; emit's `init_agg_type` gains a `loop` case that recovers the
# yield's aggregate type via `loop_yield_agg_*`, walking if/match/block like the scalar yield walk and
# reading a match arm's body in its payload-bind env). A `<- if … then R(…) else R(…)` yielding a
# record on both branches works (1182). A tuple/array yield stays rejected — its binding needs
# element/telem metadata the single slot does not carry (1183, array).
1179_loop_value_record.cf
1180_loop_value_union.cf
1181_loop_value_str.cf
1182_loop_value_record_conditional.cf
1183_error_loop_value_array.cf
