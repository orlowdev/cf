# Corpus tests cf0 is expected to compile, one per line (a filename or a test
# directory basename under boot/cfcc/tests/). The differential harness compiles
# each with BOTH cfcc and cf0 and requires their runtime results to AGREE and to
# match the test's `# expect:` directive. As cf0's front end grows, more tests
# move here from the cfcc corpus.
#
# Tracer bullet: `main` returns a constant exit code (a literal, or a block with
# a single `return <int>`).
001_main_return.cf
002_return_value.cf
003_block_return.cf
008_comments.cf
# Integer arithmetic: precedence, parens, left-associativity, div/mod, unary neg.
016_arith_precedence.cf
017_arith_parens.cf
018_arith_left_assoc.cf
019_arith_div.cf
020_arith_mod.cf
021_unary_neg.cf
025_trailing_operator.cf
# Bitwise and shift operators.
026_bitand.cf
027_bitor.cf
028_bitxor.cf
029_shl.cf
030_shr.cf
# Comparison operators and value-producing `if … then … else`.
031_cmp_gt_true.cf
032_cmp_eq_true.cf
033_cmp_false.cf
# Short-circuit logical operators and `true`/`false`.
040_logical_and.cf
078_logical_or.cf
081_logical_precedence.cf
082_logical_normalize.cf
# Local bindings + block bodies: const/let, typed bindings, reassignment, and the
# self-reference / missing-return error cases.
041_locals_chain.cf
043_let_binding.cf
044_local_typed.cf
045_local_name_no_collision.cf
050_let_reassign.cf
046_local_self_reference.cf
049_block_no_return.cf
# Function parameters: `main`'s (argc, argv, envp) prefix — word and pointer
# params, params in expressions / locals / let-reassign, plus the arity, shadow,
# reassign, pointer-return, duplicate-name, and bad-name error cases.
004_argc.cf
005_argc_envp_block.cf
006_argc_only.cf
007_unused_params.cf
022_expr_with_argc.cf
042_local_uses_param.cf
053_reassign_from_param.cf
009_missing_main.cf
011_duplicate_param.cf
012_bang_in_name.cf
013_return_pointer.cf
014_too_many_params.cf
015_unknown_name.cf
048_local_shadows_param.cf
055_reassign_param.cf
# Function calls + multiple top-level functions: basic/forward-ref/nested/chained
# calls, zero-arg fns, recursion, else-if via a helper, params as call args,
# short-circuit skipping a call, plus arity / duplicate-fn / main-not-pub / no-main
# error cases.
057_call_basic.cf
058_call_forward_ref.cf
059_call_in_expr.cf
060_call_nested.cf
061_call_with_param.cf
062_call_chain.cf
072_else_if_chain.cf
074_recursion_factorial.cf
075_recursion_sum_param.cf
079_short_circuit_and.cf
080_short_circuit_or.cf
064_call_wrong_arity.cf
065_duplicate_function.cf
066_main_not_pub.cf
067_no_main.cf
# Typecheck phase: real Bool type + no-truthiness, integer-literal range, matching
# `if` branches, precedence sanity, block/if value positions, plus the type/binding
# error cases (out-of-range literal, pointer-in-expr, non-associative comparison,
# duplicate local, const reassignment, assign-to-undefined, unknown call).
023_block_expr.cf
036_prec_and_over_or.cf
037_prec_shift_below_add.cf
038_prec_cmp_below_bitand.cf
052_let_accumulate.cf
068_if_true.cf
069_if_false.cf
070_if_truthy_int.cf
071_if_as_value.cf
073_if_nested.cf
083_logical_in_if.cf
010_exit_out_of_range.cf
024_pointer_in_expr.cf
039_cmp_nonassoc.cf
047_duplicate_local.cf
054_reassign_const.cf
056_assign_undefined.cf
063_call_unknown.cf
# Unary logical `!` (Bool→Bool) and bitwise `~` (Int→Int).
034_lnot.cf
035_bnot.cf
# Numeric casts `T(x)` — identity in cf0's word-only codegen (`Iarch`/`Uarch`/`Int64`/…
# all a word), through locals, params, returns, arithmetic, and nested casts; plus the
# operand errors (a Bool, a pointer, or a record is not a scalar integer).
752_iarch_local.cf
754_iarch_param_return.cf
755_iarch_arithmetic.cf
756_iarch_interconvert.cf
182_uarch_local.cf
829_error_bool_cast.cf
289_error_cast_pointer.cf
290_error_cast_record.cf
# Data records (part 1): `data` decls with Iarch fields, construct via a typed
# binding + read by field (single/multi-field, order-independent literals, a field
# read as a call argument), and the unknown-field error.
084_data_construct_read.cf
085_data_single_field.cf
086_data_three_fields.cf
087_data_field_order_independent.cf
093_data_field_as_arg.cf
095_data_error_unknown_field.cf
# More records part 1: field values from params/locals, multiple records/types,
# fields in control flow, trailing comma; and the literal/declaration error cases
# (missing/extra/duplicate field, empty record, non-record body, record-as-int,
# undeclared type, missing annotation).
088_data_field_from_param.cf
089_data_field_from_local.cf
090_data_two_records.cf
091_data_two_types.cf
092_data_field_in_if.cf
094_data_trailing_comma.cf
096_data_error_missing_field.cf
097_data_error_extra_field.cf
098_data_error_dup_field_decl.cf
099_data_error_dup_field_literal.cf
100_data_error_record_as_int.cf
102_data_error_undeclared_type.cf
103_data_error_nonrecord_body.cf
105_data_error_empty_record.cf
106_data_error_no_annotation.cf
# Records part 2a: field mutation on a `let` record (`p.field = expr`), and its
# error cases (mutate a const record, an undeclared field, a non-record, or the
# whole record as a unit).
101_data_let_record_mutate.cf
109_data_mutate_multiple_fields.cf
110_data_mutate_from_own_field.cf
111_data_mutate_independent.cf
112_data_error_mutate_const.cf
113_data_error_mutate_unknown_field.cf
114_data_error_field_assign_on_int.cf
115_data_error_assign_whole_record.cf
# Record parameters (part 2b, by pointer — no arena, records don't escape): several
# records with distinct storage, a record built in a callee returning a scalar, a
# record passed by pointer and its fields read, a record + Iarch mix, a record
# passed through to another function, and the wrong-record-type argument error.
107_arena_many_records.cf
108_arena_record_in_callee.cf
116_data_record_param.cf
117_data_record_param_mixed.cf
118_data_record_param_passthrough.cf
119_data_error_arg_wrong_record.cf
# Tag-only unions + match: construct a member, match on the tag (first arm, full
# ladder, wildcard, multiline decl, union copy, union parameter); and the match
# error cases (non-exhaustive, wrong-union arm, unknown member, duplicate arm, arm
# after a wildcard).
196_union_match_first.cf
197_union_match_ladder.cf
198_union_match_wildcard.cf
199_union_multiline.cf
200_union_copy.cf
206_union_param.cf
201_error_union_nonexhaustive.cf
202_error_union_wrong_arm.cf
203_error_union_unknown_member.cf
204_error_union_dup_arm.cf
205_error_union_after_wildcard.cf
# The arena (mmap floor + cf_alloc; records now heap-allocated) and record returns:
# a function builds and returns a record by arena pointer, the caller adopts and
# reads/mutates it, and a record-returning body that is itself a call. Plus the
# record-parameter type errors (int for record, record for int, undeclared type).
123_data_record_return.cf
124_data_return_then_mutate.cf
125_data_return_call_result.cf
120_data_error_int_for_record_param.cf
121_data_error_record_for_int_param.cf
122_data_error_unknown_param_type.cf
# Explicit record construction `Type({ … })` as a value expression — in a return, an
# annotated/inferred binding, with field puns `{ x, y }`, and as a fresh value; plus
# the errors (a wrong field, a return-type mismatch, an undeclared record type).
447_construct_return.cf
448_construct_annotated.cf
449_construct_inferred.cf
450_construct_puns.cf
451_construct_fresh.cf
453_error_construct_bad_field.cf
455_error_construct_return_mismatch.cf
456_error_construct_unknown.cf
# More tag-only unions: a union return + a match arm yielding a union, a union
# argument type mismatch, and or-patterns (`A | B`) with their error cases (a member
# repeated across alternatives, mixed qualifiers, a wildcard as an alternative).
210_union_return.cf
211_error_union_arg_mismatch.cf
212_union_or_pattern.cf
213_error_or_dup_member.cf
214_error_or_mixed_qual.cf
215_error_wildcard_or.cf
# `==`/`!=` on two values of the same tag-only union compares their tags (type_system
# §5, structural equality) — the `is_*` predicate pattern; plus the errors: ordering
# (`<`) on a union, mixed unions, and a payload-carrying (boxed) union (recovered with
# `match`, not compared).
1076_union_eq.cf
1077_union_ne.cf
1078_union_eq_predicate.cf
1079_error_union_ordering.cf
1080_error_union_eq_mixed.cf
1081_error_boxed_union_eq.cf
# Payload unions (M1.2a: boxed {tag, payload} in the arena; construct + tag dispatch,
# no payload binding yet): construct payload members and match on the tag, a boxed
# union crossing a boundary by pointer (param + return), and the construction arity
# error.
207_union_payload_tag.cf
216_union_payload_return.cf
217_error_payload_arity.cf
# Payload BINDING in match (`Node.IntLit(v) -> v`): a positional binding read from
# the box, `_` ignoring a field, and the errors (pattern arity vs member, a repeated
# binding name).
219_union_payload_bind.cf
220_union_payload_bind_wildcard.cf
221_error_payload_arity_pattern.cf
222_error_payload_dup_bind.cf
# `[T]` dynamic arrays (`[Iarch]` core): empty/literal construction, `.len`, indexing,
# `for x in xs`, and self-growth append `xs = [...xs, e]` (via cf_dyn_push).
936_dyn_grow_len_for.cf
937_dyn_realloc_doubling.cf
938_dyn_initial_elements.cf
939_dyn_element_write.cf
# `[Record]` dynamic arrays: record elements ride as 8-byte arena pointers (`storel`/
# `loadl`), self-grown by append and read field-wise via `arr[i].field`.
942_dyn_record_elements.cf
# `loop { }` statements with break/continue (bare + guarded `if c then break`),
# nested loops, an early return after a loop, and the error cases (break/continue
# outside a loop, an unreachable statement after break, a guard that isn't a break).
132_loop_sum.cf
133_loop_continue.cf
134_loop_nested.cf
135_loop_early_return.cf
136_loop_bare_break.cf
138_loop_error_break_outside.cf
139_loop_error_continue_outside.cf
140_loop_error_unreachable_after_break.cf
141_loop_error_guard_needs_break.cf
# a statement-position `if cond then <stmt>` now guards a general statement, not only a
# loop exit: an early `return` guard coexisting with a `break` guard inside a loop (971),
# and the non-Bool guard-condition error (974, no truthiness). With an `else` branch it
# selects between two statements — `if c then return a else return b` (970), an
# `else if …` chain of returns (973), and the unreachable-after-both-return error (975).
971_if_early_return_in_loop.cf
974_error_if_stmt_non_bool.cf
970_if_else_return_terminal.cf
973_if_else_if_chain.cf
975_error_unreachable_after_terminal_if.cf
# `[Iarch]` dynamic arrays across function boundaries: a `[T]`-returning function
# whose result is adopted and indexed, a `[T]` parameter iterated with `for`, and the
# wrong-return-type error (a `[T]` return must be a dynamic array).
1019_dyn_call_binding.cf
1020_dyn_for_param.cf
1023_error_dyn_return_type.cf
# Dynamic-array element types threaded across function boundaries: a `[T]` return + param
# (`[Iarch]` and `[Str]`), a `[T]` param forwarded on, a `[Str]` element read via
# `ss[i].len`, and the element-mismatch errors (a wrong-element argument / spread append).
1016_dyn_return_param.cf
1018_dyn_param_forward.cf
1017_dyn_str_elements.cf
1021_error_dyn_elem_mismatch.cf
1041_error_dyn_spread_elem_mismatch.cf
# String literals + the `Str` type: `.len` (byte count) on a literal / in an expression /
# on a parenthesised string; escape decoding (`\n \t \r \0 \\ \" \$`), embedded NUL,
# multiline, and literal `$`; `const Str` locals + aliasing copy; a `Str` parameter; and
# the errors (Str as an Int return / word bind / arithmetic operand / call arg, `.size`,
# a `let Str` reassignment, an unterminated literal, a bad escape).
142_string_len.cf
143_string_empty.cf
144_string_escapes.cf
145_string_embedded_nul.cf
146_string_len_in_expr.cf
147_string_quote_escape.cf
151_string_len_on_paren.cf
157_string_multiline.cf
158_string_escaped_dollar.cf
159_string_lone_dollar.cf
160_string_trailing_dollar.cf
180_str_copy.cf
181_str_param.cf
148_string_error_return.cf
149_string_error_word_bind.cf
150_string_error_arith.cf
152_string_error_field.cf
153_string_error_unterminated.cf
154_string_error_bad_escape.cf
156_string_error_as_arg.cf
# (179 `let Str` was a shared error test; cfcc now accepts `let Str` reassignment, but cf0's
# compiler still rejects it — a cfcc-only feature + cf0-source de-workaround — so 179 leaves the
# differential and lives only in the cfcc corpus as a positive test.)
# String interpolation `"…${name}…"` — a hole is a bare variable, stringified at runtime
# (integer via itoa, a Str inlined) and concatenated with the literal runs into a fresh
# Str; verified by `.len`. Plus the errors (an aggregate hole, and the malformed holes:
# a value/expression, empty `${}`, non-name, unterminated).
950_interp_int.cf
957_interp_adjacent_holes.cf
958_interp_uarch.cf
# String indexing `s[i]` reads the i-th byte — used to verify interpolated content:
# itoa digits, a negative sign, `0`, and a Str hole's inlined bytes.
951_interp_itoa_content.cf
952_interp_negative.cf
956_interp_zero.cf
953_interp_str_hole.cf
960_error_interp_aggregate.cf
161_string_error_interp_value.cf
162_string_error_interp_empty.cf
163_string_error_interp_not_name.cf
164_string_error_interp_unterminated.cf
# M2 module resolution (module_system §8), brick 1 — destructured VALUE imports flattened
# across the module boundary: a called imported function (783), and importing the same
# module twice loading it once (782, dedup by resolved path). Type imports and visibility
# filtering are later bricks (a module's `pub data`/`pub union` needs pub-decl parsing).
783_import_value_with_args
782_import_same_module_twice
# Top-level `pub data`/`pub union` declarations (parsed past the optional `pub`) enable
# collision-free TYPE imports: a `pub data` record imported, constructed, field-read (784),
# and a `pub union` imported and matched by its members (785). Colliding type names across
# modules still need name mangling (786, a later brick).
784_import_type_record
785_import_type_union
# Name mangling on flatten: two modules each define a PRIVATE `helper` of the same name;
# each module's private functions are renamed with a per-module tag and its own call sites
# rewritten, so both coexist and each calls its own (40 + 2 = 42). Private record/union
# mangling (786) is a later brick.
1090_flatten_mangle_private
# Call-for-effect statements — a bare `f(args)` (and a guarded `if c then f(args)`)
# evaluated for effect, result discarded (the `die(…)`/`close(…)` shape cf0's source uses).
1091_call_for_effect.cf
# Statement-`if` with `{ block }` bodies (its own scope): a fall-through then-block, and an
# if/else where both blocks `return` (terminal — no trailing return needed). Pervasive in
# cf0's own source.
1092_stmt_if_block.cf
# `-> asm "…"` naked assembly-bodied functions (io.cf's syscall floor): the body is emitted
# verbatim into the asm floor (not QBE) with each `${param}` replaced by its arg register.
1093_asm_body.cf
# Top-level VALUE consts (`const NAME = <expr>`, no params) — modeled as a nullary function
# returning the value, a bare reference auto-called; used in arithmetic and as a call arg.
1094_value_const.cf
# Records with MIXED-WIDTH fields (the aggregate-representation upgrade): word fields
# (Bool/Iarch) and 8-byte-pointer fields (Str/[T]) in one record, laid out in uniform
# 8-byte slots, constructed and read back per-field. cf0's AST (ast.cf) is built on these.
1095_record_mixed_fields.cf
# Union members with MIXED-WIDTH payloads (aggregate wall, half 2): word/Str/[T]/tag-only
# payloads match-bound to locals of the payload type (1096), and a recursive union payload
# folded by a recursive match (1097) — the shape of cf0's own AST unions.
1096_union_mixed_payloads.cf
1097_union_recursive_payload.cf
# Chained field access `record.field.len` — a `[T]` field's length and a `Str` field's
# length read via a trailing `.len` (cf0's own emit/typecheck use `fn.params.len` etc.).
1098_chained_field_len.cf
# cf0's scalar codegen is 64-bit (Uarch/Iarch are arch-width): a value above 2^32 is
# computed and shifted, giving 1 (64-bit) where a 32-bit word would overflow to 5.
1099_scalar_64bit.cf
# Byte-buffer subsystem (built on the 64-bit widening): `[N Uint8]` buffers, byte index
# read/write, `*[Uint8]` params + record fields, and `s.bytes` (a Str's byte pointer) — the
# raw-buffer machinery cf0's lexer/resolver run on.
1100_byte_buffer.cf
1101_str_bytes.cf
# The address-of operator `&x` + `*Record` pointer parameters: a field is mutated through
# a `*Record` pointer and the change propagates to the caller (cf0's parser/emit use `*Parser`
# and `*Emit` this way).
1102_addr_record_ptr.cf
# Indexing a general expression base `r.xs[i]` — the `[i]` rides on a field read, not a
# bare name (the `p.toks[p.pos]` shape cf0's own parser needs); index is an expression.
1103_index_field_base.cf
# Statement-position `match` (a union tag-dispatch evaluated for effect, block arm bodies):
# 1104 arms mutate an outer `let` + a payload bind + `_`; 1105 exhaustive without `_`;
# 1106 a statement match is never terminal — a trailing `return` is still required (error).
1104_stmt_match_effect.cf
1105_stmt_match_terminal.cf
1106_stmt_match_needs_return.cf
# Interpolation holes are full expressions (`${a + b}`, `${p.n}`, `${xs[i]}`, a Str hole),
# lexed+parsed per hole — not just bare names.
1107_interp_expr_hole.cf
# The unsigned integer family (`Uarch` … as param/return/field types), the `Str(v)` seal of
# a `[Uint8]` vector, real field types for `arr[i].field` (incl. Str fields + chained
# `.len`), and `[Union]` element indexing.
1108_uarch_str_seal.cf
1109_index_field_real_type.cf
1110_union_element_array.cf
# The stack-cell model for mutable (`let`) aggregate locals — reassignment REPOINTS the
# binding: 1111 `xs = []` resets a `[T]` to a fresh empty array (old header intact for
# captures), 1112 a `let` union rebinds to a fresh value, 1113 a `let Str` repoints.
1111_dyn_reset_repoint.cf
1112_mutable_union_reassign.cf
1113_mutable_str_reassign.cf
1114_nested_field_index_chain.cf
# `main([Str] args)` — the freestanding `_start` builds a cf `[Str]` from the raw (argc,
# argv) via cf_build_args before calling main (the self-hosting floor gap).
1115_main_argv_strs.cf
# A `[Uint8]` dynamic array is byte-PACKED (stride 1, storeb/loadub), so `Str(v)` borrows
# its backing as contiguous bytes — what `intern` relies on. The 8-byte-slot bug here broke
# self-hosting; its fix reached the cf0 self-compilation FIXPOINT (gen1==gen2 output).
1116_dyn_uint8_packed.cf
1117_spread_into_new.cf
# Compound assignment `x op= y` (whole-name) desugars at parse to `x = x op y`: the
# arithmetic set (291/292), the bitwise + shift set (293), an accumulating loop (295),
# and rejections on a const/param/whole-record target (296-298). 299 (a const FIELD
# `p.x += 1`) is rejected too — cf0 has no field-level compound assignment yet.
291_compound_add.cf
292_compound_arith.cf
293_compound_bitwise.cf
295_compound_loop_accumulate.cf
296_error_compound_const.cf
297_error_compound_param.cf
298_error_compound_whole_record.cf
299_error_compound_const_field.cf
# Fixed-size arrays `[N Elem]` (a length-checked literal, represented as a dynamic array)
# and `for x in xs` iteration over them: sum/product accumulation (419/423/424/425), a
# value-if body (426), nested loops (421), and break/continue steering (420/422). Rejects
# a non-array iterable (427), a non-bare-name iterable (428), and a loop var that shadows
# an existing binding (429). Corpus 430 (sibling loops reusing a var) is intentionally NOT
# here: it asserts cfcc's function-scoped loop var, whereas cf0 block-scopes it.
419_for_sum.cf
420_for_break_continue.cf
421_for_nested.cf
422_for_break_early.cf
423_for_product.cf
424_for_single.cf
425_for_var_in_expr.cf
426_for_value_if.cf
427_error_for_non_array.cf
428_error_for_non_var.cf
429_error_for_var_collision.cf
# Base scalar tuples `(a, b, …)` (Iarch/Bool elements): comptime-literal indexing `t[k]`
# (475/479), grouping vs the collapsing one-tuple `(e,)` (480), and the rejection surface —
# runtime index (482), out-of-range (483), a mutable tuple (484), a non-scalar element
# (485 record / 487 array), returning a tuple where an Iarch is wanted (486), and `()` (488).
# Heterogeneous Str/record elements + `t[k].field` are the next tuple brick.
475_tuple_index.cf
479_tuple_index_in_expr.cf
480_tuple_grouping_preserved.cf
482_error_tuple_runtime_index.cf
483_error_tuple_index_oob.cf
484_error_tuple_mutable.cf
485_error_tuple_alias_record.cf
486_error_tuple_return.cf
487_error_tuple_array_element.cf
488_error_tuple_unit.cf
# Heterogeneous tuple elements (an Iarch with a fresh record / a Str) and `t[k].field` /
# `t[k].len` through them, including a nested record chain `t[1].in.v`.
476_tuple_hetero_record.cf
477_tuple_hetero_str.cf
478_tuple_two_records.cf
481_tuple_nested_via_record.cf
# Tuple destructuring `const/let (a, _, c) = <tuple>` — sugar for positional extraction:
# by-name binds (490/491/497), `_`/`_n` skips (492/493), a `let` scalar pattern (494), a
# Str position (495), a trailing comma (496), a record position (530); and rejections —
# arity over/under (498/499), non-tuple rhs (500), duplicate name (501), a `let`
# record/Str position (502/503), a name collision (504), a malformed skip `_foo` (505).
490_destructure_pair.cf
491_destructure_from_var.cf
492_destructure_skip.cf
493_destructure_skip_n.cf
494_destructure_let.cf
495_destructure_str_position.cf
496_destructure_trailing_comma.cf
497_destructure_two.cf
530_destructure_record_position.cf
498_error_destructure_arity_over.cf
499_error_destructure_arity_under.cf
500_error_destructure_non_tuple.cf
501_error_destructure_dup.cf
502_error_destructure_let_record.cf
503_error_destructure_let_str.cf
504_error_destructure_collision.cf
505_error_destructure_bad_skip.cf
# Tuple returns `: (A, B)` — the multi-value return: return a tuple literal / a tuple local
# / value-if positions (506/508/512), heterogeneous Str/record elements (509/510), bind the
# result whole then index (507) or destructure it (506/532), chained (511); and rejections —
# arity/shape mismatch (513), a non-tuple return (514), an element-type mismatch (515), an
# unknown element type (516), a one-element tuple type (517), and main returning a tuple (518).
506_tuple_return_destructure.cf
507_tuple_return_bind_whole.cf
508_tuple_return_from_local.cf
509_tuple_return_hetero_str.cf
510_tuple_return_hetero_record.cf
511_tuple_return_chained.cf
512_tuple_return_valueif_elems.cf
532_destructure_record_from_return.cf
513_error_tuple_return_shape.cf
514_error_tuple_return_nontuple.cf
515_error_tuple_return_elem_type.cf
516_error_tuple_return_unknown_type.cf
517_error_tuple_return_arity_one.cf
518_error_main_returns_tuple.cf
# Tuple parameters `(A, B) name` — passed by pointer, indexed at comptime positions; a
# heterogeneous Str/record element (520/521), a tuple return fed into a tuple param (522),
# and destructured in the body (524); rejections — arity/shape mismatch (525), a non-tuple
# argument (526), an element-type mismatch (527), a `'T` generic element in a non-generic
# function (528), and a one-element tuple type (529). (523, a tuple param beside a
# function-type param, needs function types — a later feature.)
519_tuple_param.cf
520_tuple_param_hetero_str.cf
521_tuple_param_record_elem.cf
522_tuple_return_into_param.cf
524_tuple_param_destructure.cf
525_error_tuple_param_shape.cf
526_error_tuple_param_nontuple.cf
527_error_tuple_param_elem_type.cf
528_error_tuple_param_generic_elem.cf
529_error_tuple_param_arity_one.cf
# Tuple spread `(a, ...src, b)` — splice a source tuple's positions (any position, multiple
# spreads, a lone `(...t)` copy): append/middle/two-spreads (533/534/535), heterogeneous
# and record-element preservation (536/538), a copy (537), building a tuple argument (539);
# rejections — a non-tuple spread source (540) and spreading a scalar element (541).
533_tuple_spread_append.cf
534_tuple_spread_middle.cf
535_tuple_spread_two.cf
536_tuple_spread_hetero.cf
537_tuple_spread_lone.cf
538_tuple_spread_record_elem.cf
539_tuple_spread_into_param.cf
540_error_tuple_spread_nontuple.cf
541_error_tuple_spread_int_elem.cf
# Record tuple fields `data R = { (A, B) f }` — construct, read + comptime-index the field
# (542), a heterogeneous field with `r.t[1].len` (543), whole-tuple field mutation on a
# `let` record `r.p = (…)` (544), and destructuring a record's tuple field `const (a,b) =
# r.p` (545); rejections — shape/element mismatch (549/550), a `'T` element (551), and a
# one-element tuple field (552). (553, a concrete tuple field in a GENERIC record, needs
# generics — a later cluster.)
542_record_tuple_field.cf
543_record_tuple_field_hetero.cf
544_record_tuple_field_mutate.cf
545_record_tuple_field_destructure.cf
549_error_record_tuple_field_shape.cf
550_error_record_tuple_field_elem.cf
551_error_tuple_field_generic_elem.cf
552_error_tuple_field_arity_one.cf
# Union tuple payloads `union B = { Wrap((A, B)), … }` — construct `B.Wrap((10,20))`, match
# and index the payload `B.Wrap(p) -> p[0]+p[1]` (546), a heterogeneous payload passed and
# matched (547); plus a positional two-arg payload `Two(A, B)` staying distinct from a
# one-tuple payload (548).
546_union_tuple_payload.cf
547_union_tuple_payload_param.cf
548_union_positional_vs_tuple.cf

# Nested tuples `((a,b), c)` — a tuple position may itself be a tuple, to any depth (the
# element type is carried structurally via the recursive `TupElem`, not a parsed string):
# index through levels `t[0][0]` (554), bind a nested position `const inner = t[0]` (555),
# destructure it (556), a nested tuple as param/return/record field (557/558/559), and a
# return→param roundtrip of the same shape (560); rejecting an index past a scalar leaf
# `t[1][0]` (561).
554_nested_tuple_value.cf
555_nested_tuple_bind.cf
556_nested_tuple_destructure.cf
557_nested_tuple_param.cf
558_nested_tuple_return.cf
559_nested_tuple_field.cf
560_nested_tuple_roundtrip.cf
561_error_nested_tuple_index_scalar.cf

# The `Unit` type — `()`, the zero-tuple (type_system §6.1): a first-class, bindable,
# zero-information value (word-repr, its own type). Bind + copy (562), return `Unit` +
# bind the result (563), `()` and `Unit` name the same return type (564), a valueless
# `return` desugars to `return ()` (569); rejections — no arithmetic (565), not an Iarch
# (566), a `Unit` fn yields only `()` (567), `main` must return the Iarch exit code so a
# `Unit` annotation (568) or a valueless `return` (570) there is rejected. AND unit IN a
# tuple: a `()` element indexes past as usual (571), binds to a `Unit` local (572),
# destructures (573), as a tuple return element (574) and a record's tuple field (575).
562_unit_value_bind.cf
563_unit_return.cf
564_unit_return_parens_type.cf
565_error_unit_arith.cf
566_error_unit_as_int.cf
567_error_unit_return_mismatch.cf
568_error_main_unit_annot.cf
569_unit_bare_return.cf
570_error_bare_return_main.cf
571_unit_tuple_element.cf
572_unit_element_bind.cf
573_unit_element_destructure.cf
574_unit_element_return_type.cf
575_unit_tuple_record_field.cf

# Fixed-width integer tower, brick 1 — the eight leaves (`Int8`…`Uint64`) as DISTINCT
# width/signedness types (not the register word `Iarch`): a signed/unsigned local held
# canonically sign/zero-extended in its `l` slot (709/715), an explicit truncating cast
# `Int8(x)`/`Uint64(x)` via a shl+sar/shr canonicalization (710/711/712/714/716),
# `Iarch(x)` readback (all), a bare literal ADOPTING a fixed annotation range-checked
# (713 fits, 725/726 overflow, 734 negative-into-unsigned), all eight coexisting (724),
# fixed params + returns with NO implicit widen/narrow (718; 731/733 reject a bare word,
# 732 a wrong-type binding), a `let` fixed local reassigning from a cast or an adopting
# literal (723), and a fixed value flowing through an `if`/`match` yield (719/720/721).
709_fixed_int8_local.cf
710_fixed_uint8_cast.cf
711_fixed_int8_narrow.cf
712_fixed_int16_cast.cf
713_fixed_uint16_literal.cf
714_fixed_int32_cast.cf
715_fixed_int64_local.cf
716_fixed_uint64_cast.cf
718_fixed_param_return.cf
719_fixed_if_yields.cf
720_fixed_if_yields_int64.cf
721_fixed_match_yields.cf
723_fixed_let_reassign.cf
724_fixed_all_eight.cf
725_error_int8_literal_range.cf
726_error_uint8_literal_range.cf
731_error_fixed_arg_mismatch.cf
732_error_fixed_type_mix.cf
733_error_fixed_return_mismatch.cf
734_error_uint8_negative_literal.cf

# Fixed-width integer tower, brick 2 — arithmetic, shifts, and comparison over the fixed
# leaves. An operator needs both operands the SAME fixed type (or one an adopting literal,
# 749); the result wraps to the type's width (`+`/`-`/`*`/`<<` — 727/736/746) via the same
# shl+sar/shr canonicalization; division/remainder/right-shift/comparison pick the SIGNED
# (`div`/`rem`/`sar`/`cslt…`) or UNSIGNED (`udiv`/`urem`/`shr`/`cult…`) variant by the type's
# signedness (737/738 div, 742 rem, 740/741 shift, 728/739/747 compare); `~` wraps (743) and
# unary `-` works on a signed value (744) but is rejected on an unsigned one (751); compound
# assignment on a fixed `let` (745); mixed-width (748) and word-vs-fixed (732) rejected.
727_fixed_arith_wrap.cf
728_fixed_cmp_unsigned.cf
736_fixed_signed_wrap.cf
737_fixed_udiv.cf
738_fixed_sdiv_negative.cf
739_fixed_signed_cmp.cf
740_fixed_ushr.cf
741_fixed_sar.cf
742_fixed_rem.cf
743_fixed_bitnot.cf
744_fixed_neg.cf
745_fixed_compound_assign.cf
746_fixed_int64_arith.cf
747_fixed_uint64_cmp.cf
748_error_fixed_mixed_width.cf
751_error_neg_unsigned.cf

# Fixed-width tower, tail — a fixed-width record FIELD initialized by a bare integer literal,
# which ADOPTS the field's type (range-checked, like a `let`): `data P = { Int8 x }`, `P({x:7})`,
# read via `Iarch(p.x)` (730). Plus a bare literal adopting a fixed OPERAND's type (749), and a
# fixed value rejected as an `if` condition — no truthiness (750).
730_fixed_field.cf
749_fixed_plus_int_literal.cf
750_error_fixed_condition.cf

# A pointer may not point to a scalar — `*Int8`/`*Iarch`/… is `*Scalar` (§6.4); only a
# record or union pointer (or a `*[Uint8]` byte pointer) is valid.
729_error_ptr_to_fixed.cf

# Value-yielding loops — a `loop { … }` in EXPRESSION position is a value: `<- v` yields
# `v` and breaks (a break-with-value). Accumulate-then-yield (459), the loop as an operand
# and a direct return (460), `continue` steering it (461), a bare `<- v` once (462), a
# guarded ladder (463), nested value loops feeding an outer yield (464), a value loop inside
# a statement loop (465), and no stack leak over 50000 iterations (466, merge slot hoisted);
# yielding a `Uarch` (707), a fixed-width value (722), and an `if`-value inside the loop
# (976). Rejections: a bare `break` in a value loop (467/468), `<-` outside any loop (469),
# in a statement `loop` (470) or a `for` (471), a value loop that never reaches `<-` (472),
# an unreachable statement after `<- v` (473), and a non-word (record) yield (474).
459_loop_value_sum.cf
460_loop_value_return.cf
461_loop_value_continue.cf
462_loop_value_once.cf
463_loop_value_guarded_ladder.cf
464_loop_value_nested.cf
465_loop_value_in_stmt_loop.cf
466_loop_value_no_leak.cf
467_error_loop_value_bare_break.cf
468_error_loop_value_guard_break.cf
469_error_yield_outside_loop.cf
470_error_yield_in_stmt_loop.cf
471_error_yield_in_for.cf
472_error_loop_value_no_yield.cf
473_error_loop_value_unreachable.cf
474_error_loop_value_record.cf
707_loop_yields_uarch.cf
722_fixed_loop_yields.cf
976_if_yield_in_value_loop.cf

# Floating point — `Float64` (IEEE double, QBE `d`) and `Float32` (single, `s`), a strict
# scalar type class distinct from the integers: no int/float mixing and no adoption (659/662/
# 671/672 reject). A `.`-literal is Float64 (kept as text, handed to QBE as `d_...`); `Float32`
# is always an explicit cast. Arithmetic `+ - * /` and comparison, locals + `let` reassign,
# params + returns, unary `-`, `_` digit separators, a huge integer part; casts across
# int/float/float; `%` rejected on floats (660), a float exit code rejected (661); floats
# flow through an `if`/`match`/`loop` value merge (703/705/706), each yielding `d`/`s`.
649_float_literal_arith.cf
650_float_mul_div.cf
651_float_local.cf
652_float_param_return.cf
653_float_compare.cf
654_float_int_roundtrip.cf
655_float_let_reassign.cf
656_float_neg.cf
657_float_separators.cf
658_float_uarch_cast.cf
659_error_float_mixed.cf
660_error_float_rem.cf
661_error_float_as_exit.cf
662_error_float_int_assign.cf
663_float_large_int_part.cf
664_float32_cast_arith.cf
665_float32_local.cf
666_float32_param_return.cf
667_float32_compare.cf
668_float32_from_int.cf
669_float32_float64_roundtrip.cf
670_float32_let_reassign.cf
671_error_float32_mixed_width.cf
672_error_float32_int_assign.cf
703_if_yields_float64.cf
705_match_yields_float64.cf
706_loop_yields_float64.cf
708_error_if_branch_type_mismatch.cf
717_fixed_int_float_int.cf

# Generics G1 — monomorphic generic FUNCTIONS with explicit type arguments. A generic
# function `id = ['T]('T x): 'T -> x` is a template; the monomorphization pass clones it per
# distinct type-argument set into a concrete instance (`id[Iarch]` → `id__Iarch`) and rewrites
# each call site. Identity across types (223), a generic reading a field of its argument (224),
# a generic calling another generic — transitive instantiation (225), instances deduped (226),
# a generic call as a statement (232). Errors: too many type args (228), type args on a
# non-generic function (229), an undeclared type variable (230), a per-instance body type error
# `x + 1` on a record (231).
223_generic_id.cf
224_generic_field.cf
225_generic_transitive.cf
226_generic_dedup.cf
232_generic_call_statement.cf
228_error_generic_argcount.cf
229_error_typeargs_nongeneric.cf
230_error_undeclared_tyvar.cf
231_error_generic_bad_instantiation.cf

# Generics G2 — type-argument INFERENCE. A generic call may omit its type arguments
# (`id(40)` instead of `id[Iarch](40)`); the monomorphization pass infers them from the
# argument types via a threaded local type environment (parameters + `let` bindings): a
# parameter whose declared type IS a type variable fixes it to the argument's type. Inference
# from a literal, a local variable of known type, a record, and a tag-only union member (227);
# inference through a forwarding generic (`wrap(x)` → `id(x)`, 233). Error: a type variable no
# parameter mentions cannot be inferred (234).
227_generic_inference.cf
233_generic_inference_forward.cf
234_error_infer_unfixed.cf
